# FUN_00403F50 evidence

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

- VA: `0x00403F50`
- RVA: `0x00003F50`
- Exact contiguous body ends at `0x00404F80`; size `0x1031` / 4145 bytes.
- `0x00404F81..0x00404F8F` is compiler `INT3` padding; the next aligned body begins at `0x00404F90`.
- Calling convention: `thiscall`; `ECX` is retained as the owner object. The body returns with plain `ret`, so there are no callee-cleaned stack arguments.
- This is the next ordered body after `FUN_00403AF0`.

## High-level machine-supported role

The body is a large resource/conversion setup routine. This is supported by its repeated construction of path strings and repeated calls into already recovered recursive walker `FUN_00403640`, together with embedded resource-type names. A narrower original source-level name is not assigned yet.

## Fixed literals and paths

The following literals are directly referenced by this body and have been verified from the target bytes:

- `0x0137AF88`: `"_WIN"`
- `0x0137AF80`: `".txt"`
- `0x0137AF74`: `"\\convertLog"`
- `0x0137AF60`: `"rTexture"`
- `0x0137AF5C`: `"\\id"`
- `0x0137AF54`: `"\\effect"`
- `0x0137AF48`: `"rEffectList"`
- `0x0137AF38`: `"rCommonEffect"`
- `0x0137AF2C`: `"rAdhesion"`
- `0x0137AF20`: `"rScheduler"`
- `0x0137AF14`: `"rAHEffect"`
- `0x0137AF04`: `"rEffectStrip"`

The nearby target string block continues with resource paths/types including `"sound\\bgm\\oto"`, `"rSoundSourceMusic"`, and `".sngw"`, followed by numerous `etc\\...` resource names. Their presence is discovery evidence only; each literal must still be tied to an executed instruction before being attributed to this function.

## Repeated walker calls

Direct calls to `FUN_00403640` are present throughout the body. Verified call sites include:

- `0x00404281`, key `rTexture`
- `0x0040436E`, key `rTexture`
- `0x0040445B`, key `rEffectList`
- `0x00404476`, key `rCommonEffect`
- `0x00404491`, key `rAdhesion`
- `0x004044AC`, key `rScheduler`
- `0x004044C7`, key `rAHEffect`
- `0x004044E2`, key `rEffectStrip`
- `0x004045D0`, `0x00404779`, and later repeated groups again dispatch `rTexture` and the effect-family keys against newly constructed base paths.

Every listed call uses the same owner object in `ECX`, a constructed base path as the first stack argument, zero as the middle argument, and the resource-type key as the third argument, matching the recovered three-argument `FUN_00403640` contract.

## Object / lifetime behavior

- At entry, the routine creates several reference-counted string/path temporaries. Null string objects consistently map to the empty literal at `0x0137AA5C`.
- It allocates a `0x130`-byte object through `0x01199490` early in the body and stores the resulting pointer into owner state later in the setup path.
- Reference-counted temporaries use the same release mechanism seen in the preceding functions: decrement the first dword and, on zero, dispatch through global `0x01652F28`, vtable slot `+0x18`.
- The final path releases its retained temporary, sets `AL=1`, and returns at `0x00404F80`.

## Status

`FUN_00403F50` is **DISASSEMBLED / evidence-complete for this sequential pass**. Its boundaries, no-stack-argument `thiscall` shape, success return, resource literals, and the listed `FUN_00403640` calls are proven. The body is intentionally not promoted to `FAST_PASS` yet: it is 4145 bytes long and contains multiple reference-counted object constructions and owner-field assignments whose concrete types are not yet constrained enough for a faithful non-opaque C++ reconstruction. The next ordered analysis point is `0x00404F90`.
