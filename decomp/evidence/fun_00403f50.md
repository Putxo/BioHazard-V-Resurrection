# FUN_00403F50 evidence

- VA: `0x00403F50`
- RVA: `0x00003F50`
- Size: `3856` bytes in the ordered candidate inventory, ending at the next baseline candidate `0x00404E60`.
- Calling convention: `thiscall`; `ecx` is the owner object and the recovered host wrapper models the observed cleanup argument explicitly.

## Instruction evidence

- The prologue constructs several string temporaries and allocates `0x130` bytes through the external allocator at `0x01199490`.
- The allocated state pointer is stored into the owner at offset `+0x19C`.
- Active/fallback text is read through `0x016E1D3C + 0x1028`, with fallback literal `0x0137AA5C`.
- The body repeatedly calls `FUN_00403640` (`0x00403640`) with the same owner object and a sequence of resource suffix literals.
- Repeated literal groups include `0x0137AF60`, `0x0137AEF0`, `0x0137AEE8`, `0x0137AEE0`, `0x0137AED8`, `0x0137AECC`, `0x0137AEC4`, and `0x0137AEB8`.
- Group/tag literals include `0x0137AF48`, `0x0137AEAC`, `0x0137AEA0`, `0x0137AE94`, `0x0137AE88`, `0x0137AE78`, and tail calls with `0x0137AF20`/`0x0137AE5C`.
- The epilogue decrements/releases owned temporary references and clears the owner state pointer.

## Host reconstruction

The recovered host form exposes allocation, release, and the repeated `FUN_00403640`-style resource scans through `FUN_00403F50_Services`. It preserves the `0x130` state allocation, owner `+0x19C` state lifetime, active-name root selection, grouped repeated scan calls, and final cleanup without embedding engine code or binary payloads.

## Validation

- Implemented in `src/recovered/fun_00403f50.cpp`.
- Covered by `tests/unit/test_fun_00403f50.cpp`.
- PR #152 completed 5/5 CI checks and was merged into `integration` as `78cc0075ff03ceb013705dfa6f89ce7127af9042`.
- PR #153 promoted the function to `FAST_PASS_VALIDATED` after the validated integration merge.
