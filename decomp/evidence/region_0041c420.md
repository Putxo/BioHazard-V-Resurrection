# Ordered decompilation evidence — 0x0041C420 through 0x0041CDE0

## FUN_0041C420
- Debug/UI rendering routine. Draws fixed labels/resources via `0x005145F0`, then iterates two entries from owner `+0x2C` and lookup tables `0x015689D0/0x015689B8`, selecting font/global `0x01652E48/0x01652E4C` according to owner `+0x28`; renders paired lines with fixed coordinates/spacing.

## FUN_0041C5E0
- Very large global bootstrap/resource-registration routine.
- Repeatedly acquires named/global resources through `0x00D14C10(global 0x016E1D3C, global_slot, literal,1)`, installs them into global UI/system `0x0165A1F0` through setters (`0x004B1310`, `0x004AFD20`, `0x004AFD00`, `0x004AFD40`, `0x004AFEB0`, etc.), then releases temporary references with `0x00D25060`.
- Initializes multiple selector slots through `0x004AFEF0` for indices 0..8 with common float constant.
- Acquires additional paired resources and installs them into global service `0x0165C170`, selects literal according to `0x004E6F40` result (notably cases 9/10), stores a resource at service `+0x2F630`, and continues extensive UI/resource bootstrap.
- Uses stack-local string buffers and numerous literals `0x0137CFxx`; this is clearly a startup/resource-binding initializer rather than gameplay calculation.
- Body extends to the next aligned post-padding candidate `0x0041CDE0`.

Next ordered candidate `0x0041CDE0`. No VERIFIED/MATCHED claim.