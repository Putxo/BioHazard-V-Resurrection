# Ordered decompilation evidence — 0x00418A90 through 0x004190D0

## FUN_00418A90
- `thiscall`; large initialization/recalculation of owner progression/stat ranges.
- Reads current-user mode/index `+0x273C8`, selects 16-byte preset from table `0x015687E8`; overrides parameters for global modes 1,2,5,3,4 and root low flags.
- Populates/clamps repeated owner stat records from `+0x370` through `+0x418`, plus aggregate fields `+0x420/+0x424`, `+0x42C/+0x430`, `+0x434/+0x438/+0x43C`.
- Uses fixed caps including `0x2AF7`, factor 10, integer ratio constants; for active gameplay modes reconciles current-user value `+0x273CC` via `FUN_00416160`.
- Iterates four current-user per-player records; applies `FUN_00416220`, optional penalty `FUN_004162E0(...,-0x4B0,1)` under root `0x80000`, normalizes min/max/current fields and derived ratio fields.
- Calls additional owner recomputation `0x00415E90`; synchronizes owner `+0x42C` back into current-user field `+0x273CC`.
- Strong semantics: initialize/recompute progression/scoring ranges from mode, profile and active-player state.

Next ordered candidate `0x004190D0`. No VERIFIED/MATCHED claim.