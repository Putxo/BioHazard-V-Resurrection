# Ordered decompilation evidence — 0x0041D270 through 0x0041D660

- `0x0041D270`: tiny paired-field accessor loading object DWORDs `+0x20/+0x24` into EAX/EDX.
- `0x0041D280`: returns byte `this+0x07`.
- `0x0041D290`: constructor for small dynamic pointer-array container; installs vtable `0x0137EBD4`, zeros count/capacity/ownership byte/storage pointer.
- `0x0041D2B0`: returns constant address `0x01760348`.
- `0x0041D2C0`: clears container storage via allocator/service `0x01653274`, zeros storage/count/capacity.
- `0x0041D2F0`: owning-container clear: invokes deleting virtual function on each non-null stored object, frees pointer array, zeros fields.
- `0x0041D350`: remove-by-pointer; finds matching element, optionally deletes it when ownership byte `+0x0C` set, shifts following pointers left, decrements count.
- `0x0041D3B0`: remove-by-index; optional delete, shifts tail left, decrements count.
- `0x0041D400`: reserve/grow pointer-array capacity to caller count: allocates `count*4` aligned 0x10, zeroes new storage, copies existing pointers, frees old storage, updates storage/capacity.
- `0x0041D460`, `0x0041D4A0`, `0x0041D4E0`, `0x0041D520`, `0x0041D560`, `0x0041D5A0`, `0x0041D5E0` and following sibling constructors through the next boundary: initialize the same ~0x28-byte record layout from five caller args, set WORD type field `+0x04` to distinct constants (`1,2,4,6,0x0A,0x0C,0x14,...`), copy common fields at `+0,+0x06,+0x08,+0x0C,+0x10`, and zero trailing DWORDs `+0x14..+0x24`.
- This cluster is a family of typed command/event-record constructors sharing one binary layout.

Next ordered aligned candidate `0x0041D660`. No VERIFIED/MATCHED claim.