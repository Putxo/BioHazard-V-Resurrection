# FUN_00407B70 evidence

## Target and candidate membership

- Exact target executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- VA: `0x00407B70`; RVA: `0x00007B70`.
- The task's literal local V6 membership row is `31600,4225904,1`. Membership is checked by the exact second-column decimal value `4225904`, which is `0x00407B70`; no address is admitted merely by proximity or row numbering.
- The large `work/analysis/function_candidates.csv` is intentionally untracked in Git, so this row is preserved as explicit task evidence rather than replaced by a repository guess.
- Remote preflight found no `FUN_00407B70` row in `database/functions.csv` or `database/claims.csv` on `main` or `integration`, no matching remote branch, and no matching PR before reservation.
- Reserved branch: `decomp/00407b70-next`.

## Exact instruction and boundary evidence

Canonical exact-target evidence in `decomp/evidence/candidate_00407b60_rejection.md` records GNU and LLVM decoders agreeing on the surrounding instruction stream:

```text
00407B70  mov eax,[ecx+102Ch]
00407B76  test eax,eax
00407B78  je 00407B7Eh
00407B7A  add eax,8
00407B7D  ret
00407B7E  mov eax,0137AA5Ch
00407B83  ret
```

The corresponding 20-byte x86 body is:

```text
8B 81 2C 10 00 00 85 C0 74 04 83 C0 08 C3 B8 5C AA 37 01 C3
```

Body SHA-256: `3ed77ff705f632f16973a0bf6698395136fc7270feaea6c5093802c46341c1ed`.

The immediately preceding real helper `FUN_00407B50` ends at `0x00407B63`, followed by alignment padding; the rejection evidence independently records a direct CALL reference to `0x00407B70`, confirming that this is the next callable entry. The next aligned real helper begins at `0x00407B90`.

## Recovered contract

The function is a compact read-only x86 `thiscall` accessor:

1. read the 32-bit value at `this+0x102C`;
2. if it is zero, return the exact fallback token/address `0x0137AA5C`;
3. otherwise return the loaded 32-bit value plus `8`.

The host representation deliberately uses `std::uint32_t` rather than a native host pointer. This preserves the 32-bit EAX value and modulo-`2^32` behavior of `add eax,8` on both 32-bit and 64-bit build hosts. No concrete string/container class name is inferred.

## Tests

`tests/unit/test_fun_00407b70.cpp` covers:

- the native null early branch and exact fallback;
- the non-null `+8` path;
- 32-bit unsigned ADD wraparound.

No harness-only service or callback guard is necessary because the native helper has no external call or dereference beyond reading its own observed field.

## Inventory accounting and scope

`FUN_00407B70` is a real function and a supplied/verified member of the raw V6 universe. No exclusion is created and the denominator remains `79,782`.

This recovery is exclusive to `FUN_00407B70`. It does not modify `FUN_00407A90`, `FUN_00407B30`, or `FUN_00407B50`; their parallel branches/PRs are preserved.

No EXE, DLL, proprietary asset, `.obj`, or binary product is committed.
