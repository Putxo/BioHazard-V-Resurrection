# Ordered decompilation evidence — 0x0040E930 through 0x0040ED70

## FUN_0040E930
- `thiscall`; large global state machine keyed by global state DWORD `+0x2C` (0..13) and owner timer/counter `this+0x360/+0x35C`.
- Runs only when low root flags `+0x1E4F4 & 3` are clear; decrements owner timer by frame delta from global `0x0165BAD0+0x28`, clamps to zero.
- Uses global service `0x016E03CC`, helpers `0x004A50C0`, `0x004A78D0`, `0x0049DDC0`, `0x00AC0F00`, and sibling `0x00409A6F0` to drive transitions.
- Manipulates global states including `+0x2C = 1,2,3,6,10,13`, global selected index `+0x30`, and reset fields `+0x5CC/+0x570`.
- One path consumes bit entries in global `+0x30`, skipping `+0x5DC`, and passes per-slot structure rooted `+0x4D4` to `0x00409450`.
- Other paths invoke `0x0052FFF0`, `0x00AC0A90`, `0x00523430`, `0x004A4EF0` and reset/restart owner timer using constants `0x0137E510` / `0x013825D0`.
- Strong semantics: central timed session/match transition state machine.

## FUN_0040ED70
- `thiscall`, bool return; substantial readiness/transition predicate.
- If root flag `+0x1E4F4 & 2` is set, accepts only root substate `+0x1E5C0` values 1/2 with global byte `+0x56C == 0`; sets flag `0x800` via `0x0041B100`, calls sibling `0x0040A3F0`, returns true.
- Otherwise rejects when owner byte `+0x365` or root flag `0x80` is set, clears owner `+0x368`, then branches on root mode flags/global mode.
- For root flag `&1`, requires global `+0x1C == 1` and byte `+0x56C == 0`, sets owner code `+0x368=0x0B`, calls `0x004A4EF0(0)`, changes owner state byte `+0x04` to `0x0E`, returns true.
- Other branches inspect global modes `0/3/4`, synchronization fields `0x016E03CC+0x12E0/+0x12E4/+0x12EC`, and root flags `0x400/0x2000`; may request masks `0x10000000,0x200,0x40000000,0x20000000` through `0x004093D0`, clear owner flags, and return true.
- Function continues through `0x0040F011`; remaining tail handled in next pass.

No VERIFIED/MATCHED claim.