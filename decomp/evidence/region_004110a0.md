# Ordered decompilation evidence — 0x004110A0 through 0x00411330

## FUN_004110A0
- `thiscall`, one integer arg, `ret 0x04`.
- Active only for global modes other than `0/3/4`, when `+0x574==+0x578` and owner phase `+0x4D4==0`.
- Under owner lock adds integer arg (float-converted) to owner timer `+0x4DC`.
- If global byte `+0x56C` set, dispatches `0x004A0100(global 0x016E03CC,0x1D,arg)`; otherwise calls earlier `FUN_0040BF20(arg)`.

## FUN_00411140
- `thiscall`, one slot-like arg, `ret 0x04`.
- Similar mode/synchronization gates plus global `+0x678 != 3`.
- Maps arg via `0x00AC13B0`, then under owner lock starts per-slot timer using `FUN_0040BFD0(mapped)` and dispatches `0x004A0100(...,0x1E,mapped)`.

## FUN_004111C0
- `thiscall`, three stack args, `ret 0x0C`; per-slot scoring/accounting update.
- Under owner lock, slot index is scaled by `0x40`; adds amount arg to `+0x6B4`, increments counters `+0x690/+0x698`, resets timer `+0x6A0` from constant `0x01569F3C`, sets active byte `+0x694=1`.
- Uses mode-specific threshold tables `0x01569FC4/0x0156A094` and value tables `0x01569FB0/0x0156A080` to derive an additional score from current streak/count.
- If timed state `+0x6A4` active, enforces minimum derived score 1000 and increments `+0x6AC`; adds derived score to `+0x6B0`.
- Finishes with global statistic refreshes `0x004E9670/0x004E96E0`.

## FUN_00411330
- `thiscall`; large event/interaction classifier operating on validated player object and caller-supplied event object.
- Rejects player state `+0x2ED8==2`; obtains slot byte `+0x2ECC`.
- Traverses event/object hierarchy via `0x00AC8310` and `0x00407C00` using type tokens `0x016B1024` and `0x016AF0F4`; resolves related player slot and rejects same grouping via global `+0x4C0`.
- Classifies event using fields around event `+0xA4/+0xA7/+0x40` and target `+0x1100`, lookup tables `0x01569C30`, `0x01569EE0..F6`, distance calculation `0x00B68F10`, and thresholds `0x01569ED4/D8/DC`.
- Produces a signed classification/value, adjusts it according to event flags, writes owner per-slot handle/value at `+0x498`, and when `0x00AC1210` permits constructs a compact event record for global service `0x016E03CC` (`0x004A0A20` on synchronized path; alternate dispatch continues later).
- Body continues to boundary near `0x00411640`.

No VERIFIED/MATCHED claim.