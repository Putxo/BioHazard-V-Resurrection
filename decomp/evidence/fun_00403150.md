# FUN_00403150 evidence

- VA: `0x00403150`
- RVA: `0x00003150`
- Size: `1264` bytes in the ordered candidate inventory, ending at the next baseline candidate `0x00403640`.
- Calling convention: `cdecl`; the body uses three stack arguments and returns with plain `ret`.

## Instruction evidence

- `0x00403156` loads a stack argument and `0x0040315D` passes it with table/global `0x01652EF0` to an external opener. A null result branches to the epilogue at `0x004032FD`.
- `0x00403176` constructs a stack object/iterator; `0x004031EE` and `0x004032D9` probe it repeatedly through the same external boolean routine.
- The active global name is read through `0x016E1D3C + 0x1028`, with fallback literal `0x0137AA5C`.
- `0x004031A3`/`0x004031AF` build a bounded `0x104` byte formatted header. Negative formatter return triggers the error-reporting path at `0x004031C1` through `0x004031E6`.
- The loop body at `0x00403210` obtains an entry name, falls back to the empty literal for null, and compares the first bytes against literals at `0x0137AB48` and `0x0137AB44`, matching the `"."` and `".."` skip behavior.
- `0x0040324C` through `0x00403262` formats a child path into a bounded stack buffer; a negative formatter result emits the same error path.
- If the stack flag at `0x0040327D` is set, the function recursively calls `0x00403150`.
- Otherwise the leaf path is post-processed, resolved/created through the global engine object at `0x016E1D3C`, and the resolved object is activated when non-null.

## Host reconstruction

The recovered host form exposes the opener, iterator, error reporter, recursive descent decision, leaf resolver, and activation as `FUN_00403150_Services`. It preserves the null-open early exit, bounded formatting, `"."`/`".."` filtering, recursive call, extension trimming, resolve/create call, activation side effect, and iterator cleanup.
