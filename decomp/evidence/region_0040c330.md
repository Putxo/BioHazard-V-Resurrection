# Ordered decompilation evidence — 0x0040C330 through 0x0040C4E0

## FUN_0040C330
- `thiscall`, no stack args, large initialization/transition routine.
- Sets global state byte `+0x28 = 1`; calls global service `0x016E2044 -> 0x004730B0`.
- Repeatedly brackets mutations of global flags at `root+0x1E4F4` with imported synchronization calls through IAT `0x01371178` / `0x013710E8` when root byte `+0x1C` or global byte `0x016E2090` requires it.
- Sets flag `0x2000`, clears `0x200`, clears `0x80`; sets external byte `0x0165B6E0+0x5EA0 = 1`.
- If flag `0x100000` is present, derives one of two player bits from global object `0x0165BBB0`, stores it to global state `+0x478` and `+0x47C`, and sets root flag `0x80000`.
- Calls `0x0048C8A0(global_state,1,0.0f)`, increments `this+0x04`, clears `this+0x05`.
- Strong semantics: synchronized global-mode initialization followed by local state-machine advance.

## FUN_0040C4E0
- `thiscall`; state machine controlled by byte `this+0x05`.
- State 1 waits for conditions in global object `0x016E03CC` (`+0x131C == 0`, `+0x12B8 != 0`) then increments `this+0x04` and clears substate.
- State 0 performs a long readiness gate: global mode must be 0, root flag `0x400` set, `0x004A4D50` false, global `+0x1304 == 0`, global state `+0x570 == 0`, `0x00AC0A70(1) != 2`, `0x004099A0(global 0x0165BBB0)` false, and global byte `+0x798` clear.
- Uses player/value from `0x016E2FD0+0x630` with services `0x00530630`, `0x004E4F80`, `0x00530670`.
- If service result is 1, invokes `0x00AC0A90(global_state,0,0)`.
- Success path calls `0x004A3F90`, `0x004A75B0(...,2,0)`, `0x004E6E90`, `0x0041BE30`; sets global state `+0x2C=1`, `+0x570=2`, then increments `this+0x05`.
- Failed/not-ready gates instead increment outer byte `this+0x04` and reset `this+0x05`.
- Conservative interpretation: asynchronous session/game-state transition coordinator.

Next ordered aligned entry: `0x0040C680`. No VERIFIED/MATCHED claim.