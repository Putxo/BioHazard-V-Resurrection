# FUN_004065D0 evidence

## Target

- Evidence was decoded directly from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x004065D0`, RVA `0x000065D0`.
- Candidate body: 42 bytes (`0x2A`), through the `ret` at `0x004065F9`.
- Candidate byte SHA-256: `3320bddb841c9bc8039bbb9e108f1e8c1bc956e4868dfaa13a9c170559348e24`.
- `0x004065FA..0x004065FF` is `INT3` padding. The separately assigned `FUN_00406600` starts at `0x00406600` and is not part of this recovery.

## Instruction evidence

```text
004065D0  68 EC AF 37 01          push 0x0137AFEC
004065D5  68 00 2E 65 01          push 0x01652E00
004065DA  E8 61 29 D9 00          call 0x01198F40
004065DF  6A 00                   push 0
004065E1  6A 00                   push 0
004065E3  6A 05                   push 5
004065E5  68 D8 77 56 01          push 0x015677D8
004065EA  68 B8 BC 37 01          push 0x0137BCB8
004065EF  6A FF                   push -1
004065F1  E8 FA FB FF FF          call 0x004061F0
004065F6  83 C4 20                add esp,0x20
004065F9  C3                      ret
004065FA  CC                      int3
004065FB  CC                      int3
004065FC  CC                      int3
004065FD  CC                      int3
004065FE  CC                      int3
004065FF  CC                      int3
```

The first cdecl call receives `(0x01652E00, 0x0137AFEC)`. The second call forwards six cdecl arguments to the separately recovered `FUN_004061F0`. The single `add esp,0x20` cleans the two marker-call arguments plus all six resource-dispatch arguments.

## Referenced data

- `0x0137AFEC` = `>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n`.
- `0x0137BCB8` = `Image\\Archive\\ShowTitleResource`.
- `0x015677D8` is the direct-table token and the pushed count is exactly `5`.
- The resolved-table pointer/count pair is exactly `(0, 0)`.
- The message index is exactly `-1` (`0xFFFFFFFF`).

The five native direct-table records at `0x015677D8` are:

| # | path pointer | resource token | path |
|---:|---:|---:|---|
| 0 | `0x0137B9F8` | `0x01692E3C` | `id\\jpn\\title\\top_play00` |
| 1 | `0x0137B9F8` | `0x016E2758` | `id\\jpn\\title\\top_play00` |
| 2 | `0x0137BCD8` | `0x01692E3C` | `id\\jpn\\show_E3\\e3_stage` |
| 3 | `0x0137BCF0` | `0x01692E3C` | `id\\jpn\\show_E3\\tgs_control` |
| 4 | `0x0137BD0C` | `0x01692E3C` | `id\\jpn\\menu\\top05` |

These table entries are evidence for the observed forwarding contract only; the portable wrapper keeps the table address opaque and does not reinterpret the separately recovered `FUN_004061F0` implementation.

## Caller evidence

A direct `CALL rel32` scan of the exact `.text` image finds one direct call to `0x004065D0`, at `0x00509B49`. The caller first compares the current resource name with the same archive literal:

```text
00509B37  mov edi,0137BCB8h
00509B3C  mov esi,ebp
00509B3E  mov ecx,20h
00509B43  xor eax,eax
00509B45  repz cmps byte ptr [esi],byte ptr [edi]
00509B47  jne 00509B58h
00509B49  call 004065D0h
00509B4E  pop ebp
```

No argument is pushed for the call and the callee uses a plain `ret`, confirming a `cdecl void(void)` wrapper contract.

## Reconstructed behavior and early exits

The native body has no conditional branch and no native early return. It always:

1. emits the fixed diagnostic marker using logger/context token `0x01652E00` and format token `0x0137AFEC`;
2. calls `FUN_004061F0(-1, "Image\\Archive\\ShowTitleResource", 0x015677D8, 5, 0, 0)`;
3. returns.

For portable host tests, the reconstruction adds a null-services guard and a null-dispatch guard. The latter occurs after the optional trace callback, preserving the native side-effect order as closely as possible. A missing host trace callback does not suppress resource dispatch. These guards are explicitly tested and are not claimed as native control flow.

`FUN_004065A0`, `FUN_00406600`, and `FUN_00406620` are parallel assignments and are not modified by this recovery.

## Tests

`tests/unit/test_fun_004065d0.cpp` verifies:

- portable null-services early exit;
- missing-dispatch early exit after the trace callback;
- dispatch still occurs when the trace callback is absent;
- exact logger and marker-format tokens;
- exact `-1` message index;
- exact `Image\\Archive\\ShowTitleResource` archive literal;
- direct table token `0x015677D8` and count `5`;
- exact null/zero resolved-table pair;
- trace-before-dispatch ordering.
