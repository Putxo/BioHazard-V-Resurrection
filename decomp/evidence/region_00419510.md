# Ordered decompilation evidence — 0x00419510 through 0x00419990

## FUN_00419510
- `thiscall`; per-frame global timer/state update.
- If global phase `+0x678==0`, enables player manager, decrements owner timer `+0x4BC` by frame delta; expiry sets global phase 1 and disables player manager.
- Otherwise, unless root flag `0x800`, runs `FUN_0040B6B0(delta)`, decrements global timer `+0x67C` unless root `+0x1E550 & 0x2000` or phase 3, calls `FUN_0040B9E0(false)`.
- For each active player bit calls `FUN_00410770(slot,delta)`, `FUN_0040BB30(slot,delta)`, `FUN_00417120(slot,delta)`.
- At global phase 3 computes final result via `FUN_00410CD0` for modes 2/5 or `FUN_004108F0` otherwise, calls `FUN_0040BE40`, forces owner state 8.

## FUN_004196A0
- `thiscall`; calls central updater `FUN_0040E930`, waits global vtable predicate `0x0046F4A0` false and optional owner runtime object readiness.
- Resets service `0x00509800`; for current-user IDs other than `0x263/0x267`, calls sibling `0x0040A930(1)` and debug/setup `FUN_00418540(3)`, then forces owner state 10 with substate 9 or 10 according to return sign.
- Alternate path advances owner state, resets owner `+0x504` and float `+0x508`.

## FUN_00419760
- `thiscall`; major reset/init routine.
- Calls `FUN_00418A90`, synchronously clears root flags `0x2000` and `0x80000`, resets many owner request/state fields (`+0x40,+0x104,+0x58,+0x6C,+0x5C/+0x60/+0x64=-1`) and invokes `FUN_0040F750`.
- Clears global counters `+0x584/+0x588/+0x58C`, owner `+0x120/+0x124`.
- Replaces one of owner objects `+0x4FC` or `+0xE8` depending on global byte `+0x798`, via factories `0x0082ED50` / `0x00875BB0`, sets ownership flag `0x2000`, registers type `0x1A`.
- Calls `FUN_00418540(3)`, resets global/UI timing, clears owner visibility byte `+0x448`, zeroes 0x30-byte global block at `+0x590`, advances owner state and sets substate `+0x06=2`.

## FUN_00419990
- Next coordinator begins; continuation next.

No VERIFIED/MATCHED claim.