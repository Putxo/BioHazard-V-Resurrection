# FUN_00402DF0 evidence

- VA: `0x00402DF0`
- RVA: `0x00002DF0`
- Size: `128` bytes in the ordered candidate inventory, ending before `0x00402E70`.
- Calling convention: `thiscall`; `ecx` is the object, stack argument at `[esp+4]`.

## Instruction evidence

- `0x00402DFB` pushes the stack argument and formats into `this + 0x54` using the literal at `0x0137AAE0`.
- `0x00402E0A` reads the global object at `0x016E1D3C`, then `+0x102C`; non-null values are advanced by eight bytes, otherwise the fallback literal `0x0137AA5C` is used.
- `0x00402E27` through `0x00402E33` formats a stack-local message from the active global name and the newly formatted `this + 0x54` name with the literal at `0x0137AAD4`.
- `0x00402E3D` calls an external message/reporting routine with that stack-local text.
- `0x00402E4B` through `0x00402E53` call an external registry lookup/creation routine with `create = 1`, registry literal `0x016E216C`, and the formatted object name.
- If that call returns non-null, `0x00402E5F` dispatches a follow-up method on the returned object.

## Host reconstruction

The recovered host form keeps the observable dataflow and side effects behind `FUN_00402DF0_Services`: name formatting, active-name selection with fallback, message reporting, registry lookup/create, and optional activation of the resolved object. External engine routines and literal formats are injected instead of guessed.
