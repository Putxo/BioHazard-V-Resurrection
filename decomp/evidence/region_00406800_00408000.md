# Sequential decomp evidence — 0x00406800..0x00408000

Target: exact RE5DX9 1.1.0 Dev SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Confirmed aligned entries

`00406800`, `00406820`, `00406840`, `00406870`, `004068A0`, `004068D0`, `004068F0`, `00406910`, `00406AD0`, `00406B20`, `00406CA0`, `00406CD0`, `00406DB0`, `00406F20`, `00406F60`, `00406FA0`, `00406FE0`, `00407020`, `004070C0`, `00407250`, `00407320`, `004076E0`, `004078A0`, `00407960`, `00407A30`, `00407A70`, `00407A90`, `00407AF0`, `00407B10`, `00407B30`, `00407B50`, `00407B70`, `00407B90`, `00407BC0`, `00407C00`, `00407C40`, `00407C60`, `00407CD0`, `00407CF0`, `00407D10`, `00407D90`, `00407DC0`, `00407E10`, `00407E60`, `00407E70`, `00407E90`, `00407EA0`, `00407EB0`, `00407EC0`, `00407ED0`, `00407EE0`, `00407EF0`, `00407F00`, `00407F50`, `00407F60`, `00407F70`, `00407F80`, `00407F90`, `00407FA0`, `00407FB0`, `00407FC0`, `00407FD0`.

## FUN_00407A70

Exact compact `thiscall` size/count helper:

- reads dword `this+0x04` as begin pointer.
- if begin is null, returns 0.
- otherwise returns signed `(this+0x08 - begin) >> 2`.
- this is structurally a count of 4-byte elements; concrete container type remains unknown.

## FUN_00407A90

- accepts one stack argument interpreted as requested size/count.
- zero input tail-jumps to aligned allocator `0x01251471` with size 0 and alignment `0x10`.
- nonzero path performs an unsigned overflow/limit check before the same allocator path; failure enters target exception/error helpers `0x01251B3D` and `0x01251D5C` with metadata tokens `0x01563F18` / `0x01541F48`.
- source conversion deferred until the exact exception contract is mapped.

## FUN_00407AF0

- `thiscall`, `ret 4`.
- forwards its stack argument to `0x01251B8B`, then installs vtable token `0x01541F48` at `this+0` and returns `this`.
- constructor/base-constructor relationship is clear; semantic class name remains unknown.

## FUN_00407B10 / FUN_00407B30

Both are conditional Win32/import wrappers over a structure whose byte `this+0x1C` acts as a force/active flag. If that byte is nonzero, or global byte `0x016E2090` is nonzero, they pass `this+4` to different IAT calls (`0x01371178` and `0x013710E8` respectively). Otherwise they return without side effects.

## FUN_00407B50 / FUN_00407B70

String-like accessors:

- `00407B50` reads pointer `this+0x1028`; `00407B70` reads pointer `this+0x102C`.
- if non-null, return pointer+8.
- if null, return fallback literal/token `0x0137AA5C`.

## FUN_00407B90

- deleting-destructor shape: installs vtable `0x01541F48`, calls `0x01251BE3`, tests bit 0 of its stack flag, conditionally frees `this` via `0x0125145C`, returns `this`, `ret 4`.

## FUN_00407BC0 / FUN_00407C00

Both walk a linked chain obtained from virtual slot `+0x10` of `this` and compare each node dword `+0x04` against dword `+0x04` of the supplied key object.

- `00407BC0` returns boolean true/false when a matching node exists.
- `00407C00` returns `this` when a match exists and null otherwise.
- nodes advance through dword `node+0x10`.
- both use `ret 4`.

## Fixed-address accessor family

The following functions are exact `mov eax, imm32; ret` bodies and are recovered in `region_00407e60.cpp` without assigning semantic global names:

- `00407E60 -> 0x01657B90`
- `00407E90 -> 0x01657BB4`
- `00407EA0 -> 0x01657BD8`
- `00407EB0 -> 0x01657BFC`
- `00407EC0 -> 0x01657C20`
- `00407ED0 -> 0x01657C44`
- `00407EE0 -> 0x01657C68`
- `00407EF0 -> 0x01657C8C`
- `00407F50 -> 0x01657CB0`
- `00407F60 -> 0x01657CD4`
- `00407F70 -> 0x01657CF8`
- `00407F80 -> 0x01657D1C`
- `00407F90 -> 0x01657D40`
- `00407FA0 -> 0x01657D64`
- `00407FB0 -> 0x01657D88`
- `00407FC0 -> 0x01657DAC`

Each body is 6 bytes followed by INT3 alignment padding. These are exact enough for FAST_PASS after build/test integration; they are not semantic LINKED mappings.

## FUN_00407E70

- one stack argument, `ret 4`.
- calls `FUN_00407A90(arg, 0)` using cdecl stack cleanup internally; exact wrapper purpose remains unnamed.

## FUN_00407F00

Destructor-like routine over the same ~`0x1A0` object family seen earlier. It tears down embedded object at `this+0x158` through `0x0119C3B0`, invokes an IAT operation on `this+8`, rotates vtable tokens `0x0137AA9C`, `0x0137A990`, `0x0137A97C`, and conditionally frees `this` through allocator owner global `0x01641A0C` virtual slot `+0x18` when deletion flag bit 0 is set.

## FUN_00407FD0

Entry begins a reference-counted insertion/assignment path. It constructs a temporary through `0x00402360`, handles null/non-null destination ownership, increments a referenced object's leading dword when adopting it, and continues beyond `0x00408000`; recovery continues in the next sequential region.

## Status

The fixed-address accessor family has source and unit-test files. Other functions remain DISASSEMBLED or evidence-only until dependencies and exact exception/import contracts are represented.