# Ordered decompilation evidence — 0x00414510 through 0x00414A40

## FUN_00414510
- `thiscall`; very large state machine keyed by owner byte `+0x05` up to `0x20`, with jump tables `0x004149E4/0x00414A18`.
- Forces player-manager mode `0x00523380(1)` each invocation.
- Coordinates global transition selectors 10 and 11 (`0x004A1E60/0x004A1970/0x00495640`), timer reset, UI vtable calls, root flag groups `0x800`, `0xFFE02000`, `0x40000000`, `0x80000`, and service resets.
- In mode 1 calls `0x0041BA40`, clears root flag `0x20`, invokes `FUN_0040DBE0`.
- Uses sibling `FUN_0040B3D0`, predicates `0x00409480/0x00AC1240`, stores mode/result in owner `+0x4E4`; uses owner `+0x4E8/+0x4EC` as phase/mask fields.
- Table `0x015689A8` maps mode/phase to effect ID passed to `FUN_0040B4A0`.
- Iterates up to four player handles, checks input/services, builds owner mask `+0x4EC`, waits on `0x00494600`, `0x0050F320`, runs timed wait `this+0x30`, and later restores root flags through `0x0041B1A0`.
- Terminal path sets owner state byte `+0x04=2`, resets UI/global service, calls `FUN_00412920` and `FUN_0040B440`.
- Strong semantics: high-level session/mode transition state machine.

Next ordered candidate `0x00414A40`. No VERIFIED/MATCHED claim.