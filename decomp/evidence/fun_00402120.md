# FUN_00402120 evidence

## Candidate

- RVA: `0x00002120`
- VA: `0x00402120`
- Size: `304` bytes through alignment and adjacent local helper bodies before next V6 candidate `0x00402250`.
- Candidate source: V6 direct-call-target entry from the ordered candidate universe.

## Entry disassembly

```asm
00402120  mov ecx, dword ptr [0x01641AA8]
00402126  mov eax, dword ptr [ecx]
00402128  mov edx, dword ptr [esp + 4]
0040212C  mov eax, dword ptr [eax + 0x14]
0040212F  push 0x10
00402131  push edx
00402132  call eax
00402134  ret
```

## Adjacent bodies in the same public candidate span

The bytes from `0x00402150` onward contain aligned local helper bodies before the next public V6 candidate. They are not counted separately here because they are not separate rows in the canonical public candidate inventory used by the dashboard.

Important nearby bodies include:

- `0x00402150`: allocate `0x28` bytes with `0x10` alignment, construct through `0x006803D0`, then run several global service calls and clear a byte at `[ [0x016E1660] + 0x1E4EC ] + 0x28`.
- `0x004021C0`: calls service `0x00D056F0` with global `0x016E1690`, then passes global `0x016E7014` through service `0x00D075D0`.
- `0x004021F0`: calls constructor/helper `0x00D02A20`, writes vtable-like pointer `0x0137A9C4`, and returns `this`.
- `0x00402210`: allocation wrapper for the same `0x28`-byte object, returning null if allocation fails.
- `0x00402240`: returns fixed address `0x016E1638`.

## Recovered behavior

The public entrypoint is an aligned allocation thunk. It loads the global allocator/service pointer at `0x01641AA8`, dispatches virtual slot `+0x14`, passes the requested size and fixed alignment `0x10`, and returns the allocated pointer.
