# FUN_00404F90 evidence

- VA: `0x00404F90`
- RVA: `0x00004F90`
- Candidate-block size: `1920` bytes in the ordered V6 candidate inventory, ending at next candidate `0x00405710`.
- The direct entry body returns at `0x00405017`; `0x00405018..0x0040501F` is `INT3` padding.
- The range `0x00405020..0x0040570F` contains aligned helper bodies and padding that are not separate V6 candidate rows in this ordered pass. They remain documented as part of this candidate block instead of being silently ignored.
- No stack arguments and no `ECX` object use are visible at the direct `0x00404F90` entry; the function returns with plain `ret`.

## Entry-body instruction evidence

- `0x00404F91..0x00404F9B` calls `0x01198F40` with base `0x01652E00` and marker literal `0x0137AFEC`, the `">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>"` separator.
- `0x00404FA3..0x00404FDF` iterates from offset `0` to `0x180` in `8`-byte steps, reading pairs from `0x01567310 + offset` / `0x01567314 + offset`.
- Each table pair is submitted through global `0x016E1D3C` call `0x00D14C10` with `enabled=1`.
- Non-null results are finalized through `0x00D25060`.
- `0x00404FE1..0x00404FEB` repeats the same `0x0137AFEC` marker call.
- `0x00404FF0..0x00404FFF` calls recovered `FUN_00403150` with literals `0x0137AFE4`, `0x0137AFD0`, and `0x0137AFC0`, corresponding to extension `".sngw"`, resource type `"rSoundSourceMusic"`, and path `"sound\\bgm\\oto"`.
- `0x00405004..0x0040500E` calls `0x01198F40` with base `0x01652E00` and final marker literal `0x0137AF90`, the `"<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<"` separator.
- `0x00405016..0x00405017` pops `esi` and returns.

## Host reconstruction

The recovered host function models this entry as a table-driven registration batch. It preserves the two begin/end marker calls, the exact `0x180 / 8 = 48` table-entry iteration count, the enabled registration side effect, the three-literal group registration, and the final marker call.

## Internal helper range note

After the entry return, the same candidate interval contains repeated allocator/constructor-style helper bodies beginning at `0x00405020`, `0x00405080`, `0x004050E0`, and later aligned addresses, plus small dispatcher bodies such as `0x00405260` and `0x004052E0`. These are noted here because the denominator advances from `0x00404F90` directly to `0x00405710`; this pass does not publish separate progress rows for non-candidate internal aligned bodies.

## Validation

- Implemented in `src/recovered/fun_00404f90.cpp`.
- Covered by `tests/unit/test_fun_00404f90.cpp`.
- PR #158 completed 5/5 CI checks and was merged into `integration` as `5e4fd0b32f2c9270ca6804447812a2cda6c7ee15`.
- PR #159 promoted the function to `FAST_PASS_VALIDATED` after the validated integration merge.
