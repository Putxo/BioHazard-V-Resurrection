# FUN_00404F90 evidence

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

- VA `0x00404F90`, RVA `0x00004F90`.
- Exact body ends at `0x00405017`; size `0x88` / 136 bytes.
- `0x00405018..0x0040501F` is `INT3` padding; next aligned body begins at `0x00405020`.
- No stack arguments and no `ECX` object use are visible at entry; the function returns with plain `ret`.

## Proven behavior

- Logs literal `0x0137AFEC`, exactly `">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>"`, through logger `0x01198F40` with context `0x01652E00`.
- Iterates a fixed `0x180`-byte table beginning at `0x01567310` in 8-byte records: exactly 48 records.
- For each record it loads the two dwords at offsets `+0` and `+4`, pushes constant `1`, and calls `0x00D14C10` on global object `0x016E1D3C`.
- If `0x00D14C10` returns non-null, that returned object becomes `ECX` for a call to `0x00D25060`.
- After all 48 records, it logs `0x0137AFEC` again.
- It then calls already recovered `FUN_00403150` with exact literals:
  - path `"sound\\bgm\\oto"` (`0x0137AFC0`)
  - resource type `"rSoundSourceMusic"` (`0x0137AFD0`)
  - extension `".sngw"` (`0x0137AFE4`)
- Finally it logs literal `0x0137AF90`, exactly `"<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<"`, and returns.

## Status

`FUN_00404F90` is **DISASSEMBLED / evidence-complete for the sequential pass**. The fixed 48-record iteration and the `FUN_00403150` sound-resource call are proven. The contracts of `0x00D14C10` and `0x00D25060` remain unnamed, so no speculative source wrapper is introduced yet. The next ordered aligned body is `0x00405020`.
