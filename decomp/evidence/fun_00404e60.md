# FUN_00404E60 evidence

- VA: `0x00404E60`
- RVA: `0x00004E60`
- Size: `304` bytes in the ordered candidate inventory, ending at the next baseline candidate `0x00404F90`.
- The executable body returns at `0x00404F80`; `0x00404F81..0x00404F8F` is `INT3` padding.
- Calling convention: `thiscall`; `ecx` is retained in `esi` as the owner object. The function returns success in `AL`.

## Instruction evidence

- `0x00404E61..0x00404E7D` loads the active-name root from `0x016E1D3C + 0x1028`, falling back to empty literal `0x0137AA5C`.
- `0x00404E82` constructs a temporary string from the active-name root.
- `0x00404E87..0x00404E90` appends literal `0x0137AF54`, the `\\effect` path suffix.
- The function then issues six direct calls to `FUN_00403640` with zero as the middle argument, the owner in `ecx`, and the constructed effect path as the base:
  - `0x00404EB1`, key literal `0x0137AF48` (`rEffectList`)
  - `0x00404ED2`, key literal `0x0137AF38` (`rCommonEffect`)
  - `0x00404EF3`, key literal `0x0137AF2C` (`rAdhesion`)
  - `0x00404F14`, key literal `0x0137AF20` (`rScheduler`)
  - `0x00404F35`, key literal `0x0137AF14` (`rAHEffect`)
  - `0x00404F56`, key literal `0x0137AF04` (`rEffectStrip`)
- `0x00404F5B..0x00404F7B` releases the retained temporary string through the same refcount/global-dispatch path used by surrounding recovered functions.
- `0x00404F7D..0x00404F80` sets `AL=1`, pops the saved stack slot, and returns.

## Host reconstruction

The recovered host function models the routine as an effect-family scan dispatcher. It preserves the active-root/fallback selection, `\\effect` suffix, ordered six-key dispatch into the already recovered `FUN_00403640` behavior, and success return without embedding executable bytes or proprietary game assets.
