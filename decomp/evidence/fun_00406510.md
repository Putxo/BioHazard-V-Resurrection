# FUN_00406510 evidence

## Target

- Evidence was taken only from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x00406510`, RVA `0x00006510`.
- Candidate body: 33 bytes (`0x21`), through the `ret` at `0x00406530`.
- Candidate byte SHA-256: `cdc4c4eb743d7535bccbebd47fa45f11769ad6e9488932ecc5eb9906d27573e6`.
- `0x00406531..0x0040653F` is `INT3` padding; the next aligned root begins at `0x00406540`.

## Instruction evidence

```text
00406510  mov eax,[esp+04h]
00406514  push 05h
00406516  push 015676A8h
0040651B  push 0Dh
0040651D  push 01567640h
00406522  push 0137B8ACh
00406527  push eax
00406528  call 004061F0h
0040652D  add esp,18h
00406530  ret
```

The wrapper therefore accepts one caller-cleaned dword argument and forwards six cdecl arguments to the separately assigned `0x004061F0`:

1. incoming selector;
2. archive literal at `0x0137B8AC`;
3. primary table token `0x01567640`;
4. primary count `13`;
5. secondary table token `0x015676A8`;
6. secondary count `5`.

The literal at `0x0137B8AC` is `Image\\Archive\\ShowGameResource`.

The table boundaries are internally consistent with the pushed counts: `0x015676A8 - 0x01567640 = 0x68 = 13 * 8`, and the following table begins after five 8-byte records at `0x015676D0`.

## Table evidence

The 13 primary records at `0x01567640` are 8-byte `(name pointer, resource token)` pairs:

```text
0  stage\\game                         016E2758
1  sound\\bgm\\st_camera              016E2830
2  id\\jpn\\menu\\top20               01692E3C
3  id\\jpn\\menu\\top21               01692E3C
4  id\\jpn\\menu\\top23               01692E3C
5  id\\jpn\\menu\\top24               01692E3C
6  id\\jpn\\menu\\top25               01692E3C
7  id\\jpn\\menu\\top30               01692E3C
8  id\\jpn\\menu\\top31               01692E3C
9  id\\jpn\\night_scope\\cp_night_scope_360 016E2758
10 id\\jpn\\show_E3\\e3_com            01692E3C
11 id\\jpn\\cockpit\\XfSSKeyItem       016E289C
12 sound\\se\\wp\\se_wp2b              016E2908
```

The five secondary records at `0x015676A8` are:

```text
0 id\\cockpit\\act_btn_360    01692E3C
1 id\\cockpit\\cp_life_360    01692E3C
2 id\\slot\\cp_bukislot_360   01692E3C
3 id\\scope\\cp_scope_360     01692E3C
4 id\\cockpit\\cp_map_360     01692E3C
```

These records are evidence for the constants passed by the wrapper; the reconstruction does not duplicate the implementation of `0x004061F0` or reinterpret its table-processing internals.

## Caller evidence

A direct `CALL rel32` scan of the exact `.text` image finds one direct call to `0x00406510`, at `0x00509A5C`.

Immediately before that call the caller:

```text
00509A49  mov eax,[0165BBB0h]
00509A4E  mov ecx,[eax+20h]
00509A51  imul ecx,ecx,00008C74h
00509A57  mov edx,[ecx+eax+70h]
00509A5B  push edx
00509A5C  call 00406510h
00509A61  add esp,04h
```

The caller first compares its active resource name with the same `Image\\Archive\\ShowGameResource` literal. It then passes the active-slot field at `+0x70` and performs caller cleanup, confirming the one-dword cdecl contract.

## Early exits and reconstruction choice

The original 33-byte machine-code wrapper has no conditional branch or early-return path: it always forwards to `0x004061F0` and returns. The portable host reconstruction adds only null-service and null-dispatch guards so unit tests can execute without the original engine. Those two harness-only early exits are explicitly tested and are not claimed as original binary control flow.

Because `FUN_004061F0` is assigned in parallel, this recovery deliberately treats it as an injectable opaque dispatch service. No code, metadata, or behavior belonging to `FUN_004061F0` is modified.
