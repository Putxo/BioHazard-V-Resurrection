# FUN_00421CC0 evidence

- VA: `0x00421CC0`.
- Directly decoded from the verified RE5DX9 1.1.0 Dev executable (SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`).
- Function ends at `0x00422258`; `0x00422259..0x0042225F` are INT3 padding. Both normal epilogues use `ret 0x04`, proving one stack argument.
- Large local frame: `sub esp,0x514`. The sole argument is loaded into EBP and null-tested immediately; null exits without spawning anything.
- Builds/refcounts temporary string-like handles through `FUN_00402360`, `0x00B8C8D0`, and cleanup paths using allocator/global `0x01652F28`.
- Tests a sequence of literals at `0x0137F2E8`, `0x0137F2E0`, `0x0137F2D8`, `0x0137F2D4`, `0x0137F2D0`, `0x0137F2C8`, and `0x0137F2C0`. The first matching branch selects one of several spawn/configuration paths.
- Several branches allocate `0x230` bytes and construct through `0x00680A10`; others allocate `0x220` through `FUN_00402120` and construct via `0x00680AA0`; fallback allocates `0x210` and constructs via `0x00652590`.
- Common object setup sets bit `0x2000` in field `+0x04`, initializes per-player/mask state, passes the original argument to `0x00EA4FC0`, adjusts flags at `+0xE0`, and enqueues the object through manager global `0x016E1690` / `0x00D05520`.
- Two branches create two objects in a loop (`edi=0..1`) and set byte `+0x210` to distinguish variants 0/1. Other branches create a single `0x220` object and set DWORD `+0x214` to 0/1.
- Fallback `0x210` path calls `0x0041D8C0` with byte `[0x016E3040+0x29A]` before enqueueing.
- Calling convention is consistent with stdcall-style one-argument cleanup. Precise gameplay class names are intentionally unresolved until constructor/vtable semantics are established.
