# FUN_004085F0 evidence

## Candidate membership and preflight

Task-supplied exact V6 row:

~~~text
34288,4228592,1
~~~

This is the supplied exact row 135 of function_candidates.csv.
Membership is determined solely from the exact second decimal column:
4228592 == 0x004085F0. The first decimal column is the RVA and
34288 == 0x000085F0.

FUN_004085F0 is a real function root, so the canonical denominator remains
**79,782** and no false-positive removal is created.

Remote preflight found no FUN_004085F0 implementation or claim on main or
integration, no matching branch and no matching PR. The exclusive reservation
is decomp/004085f0-next.

Parallel preservation is explicit:

- FUN_00408550 was already public/FAST_PASS_VALIDATED;
- FUN_004085D0 was present on integration during initial preflight and then
  reached FAST_PASS_VALIDATED through PR #402 while 85F0 analysis was still
  local;
- because the 85F0 branch was still empty, it was fast-forwarded to the live
  integration commit bced6433d7f516a29b8817801277e9b1589472ab before its
  first write.

## Exact private target

- Product: Resident Evil 5 PC 1.1.0 Dev.
- Exact executable size: 19,977,216 bytes.
- Exact executable SHA-256:
  323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815.
- PE32 x86 image base: 0x00400000.
- .text VA: 0x00401000; raw offset: 0x400.
- VA: 0x004085F0; RVA: 0x000085F0.
- Native body: 0x004085F0..0x0040860A, exactly **27 bytes**.
- Native-body SHA-256:
  f3c2d761dad0c7e45b14b63e1622a0c57d608989598780fc6deef35a288a6cb9.
- Preceding FUN_004085D0 ends at 0x004085E4; 0x004085E5..0x004085EF
  is INT3 padding.
- 0x0040860B..0x0040860F is INT3 padding before the next root 0x00408610.
- 32-byte root-plus-padding span 0x004085F0..0x0040860F SHA-256:
  0d818bd9486c3538229f31230fb23e2bc88b20dd0003e00ad19520b687a6c936.

The executable was used only for local hashing, disassembly and reference
analysis. It is not committed or uploaded.

Exact body bytes:

~~~text
80 7c 24 04 00 74 0a 81 49 04 00 20 00 00 c2 04
00 81 61 04 ff df ff ff c2 04 00
~~~

GNU objdump and LLVM llvm-objdump independently agree on every instruction
boundary.

## Exact native instruction stream

~~~text
004085F0  80 7C 24 04 00              cmp byte ptr [esp+04h],00h
004085F5  74 0A                       je 00408601h
004085F7  81 49 04 00 20 00 00        or dword ptr [ecx+04h],00002000h
004085FE  C2 04 00                    ret 4
00408601  81 61 04 FF DF FF FF        and dword ptr [ecx+04h],FFFFDFFFh
00408608  C2 04 00                    ret 4
~~~

The routine has exactly two native return paths:

- nonzero low byte: set bit 13 / mask 0x2000 and return;
- zero low byte: clear bit 13 / mask 0x2000 and return.

There is no native null/service guard and no external call.

## ABI and object field

ECX is the object. The function reads a single stack argument only as a byte
and returns with ret 4, so the exact least-assumptive portable signature is:

~~~text
void FUN_004085F0(
    FUN_004085F0_Object& object,
    uint8_t enabled)
~~~

Only dword this+0x04 is touched. A minimal observed layout is therefore:

~~~text
+0x00  opaque/reserved dword
+0x04  flags dword
~~~

The native bit operation preserves all other 31 bits exactly.

## Direct callers

A complete rel32 scan of the exact .text finds exactly **2** direct calls:

~~~text
0056B762  push 1
0056B764  mov eax,[ebp-10h]
0056B767  mov ecx,[eax+038Ch]
0056B76D  call 004085F0h
~~~

and:

~~~text
0056E269  mov ecx,[ebp-10h]
0056E26C  cmp dword ptr [ecx+0350h],0
...
0056E26C  cmp dword ptr [ecx+038Ch],0
0056E273  je 0056E2A0h
0056E275  push 0
0056E277  mov edx,[ebp-10h]
0056E27A  mov ecx,[edx+038Ch]
0056E280  call 004085F0h
0056E285  mov eax,[ebp-10h]
0056E288  mov ecx,[eax+038Ch]
...
0056E29B  mov eax,[edx+30h]
0056E29E  call eax
~~~

The first lifecycle site sets the flag immediately after storing the object at
owner+0x38C. The second clears it before a subsequent virtual call on that
same object.

## Reflective property registration proves the semantic name

The exact executable also contains one absolute little-endian pointer to
0x004085F0, at .text instruction 0x00D1273E:

~~~text
00D12718  mov dword ptr [esp+14h],01388FB4h
00D12720  mov word ptr [esp+18h],3
00D12727  mov word ptr [esp+1Ah],80h
00D1272E  mov dword ptr [esp+1Ch],edi
00D12732  mov dword ptr [esp+20h],004C4E10h
00D1273A  mov dword ptr [esp+24h],esi
00D1273E  mov dword ptr [esp+28h],004085F0h
...
00D12756  call 00409750h
~~~

RDATA 0x01388FB4 is the literal C string:

~~~text
Fix
~~~

The paired getter registered in the same descriptor is 0x004C4E10:

~~~text
004C4E10  8B 41 04        mov eax,[ecx+04h]
004C4E13  C1 E8 0D        shr eax,0Dh
004C4E16  24 01           and al,01h
004C4E18  C3              ret
~~~

Getter-body SHA-256:
6f573e936fce330aec01503f33a33bbe97dac94f3257d6da5d8ab6deb91e3f2b.

Thus the setter is not merely inferred as an arbitrary bit toggle: the exact
registration table names the exposed boolean property **Fix**, its getter
reads bit 13, and FUN_004085F0 sets/clears that exact bit.

## Tests

tests/unit/test_fun_004085f0.cpp covers:

- the native nonzero branch and its early ret 4;
- the native zero branch and its separate ret 4;
- exact set mask 0x00002000;
- exact clear mask 0xFFFFDFFF;
- preservation of every unrelated flag bit;
- preservation of the preceding opaque dword;
- noncanonical nonzero byte 0x80, proving the native byte-test semantics;
- idempotent repeated set and clear operations.

No host-only service seam is needed because the function performs no external
call and only mutates its observed in-object flags field.

An isolated C++20 build/test passes with:

~~~text
-Wall -Wextra -Wpedantic -Werror
~~~

## Scope

Only FUN_004085F0 is implemented here. Existing validated FUN_00408550 and
FUN_004085D0 are preserved from integration, along with all other parallel
work.

No EXE, DLL, proprietary asset, .obj, ROM/image, or binary product is
committed.
