# FUN_00401860 evidence

Target: RE5DX9 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Boundary

- entry: `0x00401860`
- end: `0x00401871` (`ret`)
- exact size: 18 bytes
- `0x0040185F` is `INT3`
- `0x00401872..0x0040187F` are `INT3`
- next clear entry: `0x00401880`

## Exact control flow

```asm
00401860  mov ecx,[esp+4]
00401864  xor al,al
00401866  test ecx,ecx
00401868  je  00401871
0040186A  mov eax,[ecx]
0040186C  mov edx,[eax+4]
0040186F  jmp edx
00401871  ret
```

The sole stack argument is treated as an object/interface pointer. Null returns false in `AL`; non-null loads the vtable and tail-dispatches through slot `+0x04`.

## Xref / provider evidence

A direct caller exists at `0x0086D804`. Immediately before the call, `0x0086D7FD` invokes IAT `0x013714CC`, verified from the PE import table as `steam_api.dll!SteamUser`, pushes its return value, calls `FUN_00401860`, then tests `AL`.

This proves the observed caller feeds a SteamUser interface pointer into the helper. The exact historical Steamworks method name for vtable slot `+0x04` is not required for the fast-pass contract and is deliberately not asserted here.

Module/subsystem remain `UNKNOWN`.
