# Ordered decompilation evidence — 0x0040F500 through 0x0040F9A0

## FUN_0040F500
- `thiscall`; tracks root flag `0x800` in owner DWORD `this+0x3C`.
- On rising edge, requests flag groups `0xFFE02000` and `0x8000`, calls global services `0x004B6510` and `0x004C5300`.
- On falling edge, synchronously clears corresponding groups from root `+0x1E500` and calls `0x004B6500`.
- Edge-triggered global feature/mode flag synchronizer.

## FUN_0040F600
- `thiscall`, two integer stack args, `ret 0x08`; inactive for global modes `1/2/5`.
- Under owner lock, when lower<=upper, recomputes four pairs of owner numeric thresholds/ranges using ratios and a scale of 1000.
- Writes fields `+0x390/+0x394`, `+0x3BC/+0x3C0`, `+0x3E8/+0x3EC`, `+0x414/+0x418`; upper-side calculations add 999 for inclusive/ceiling-style bounds.
- Range-to-scaled-threshold conversion.

## FUN_0040F750
- Iterative initializer over per-player owner blocks around `+0x450/+0x484/+0x4A8`; clears state bytes and initializes sentinel DWORDs to `-1`.
- Resolves/validates player objects through `0x005231E0/0x00523300` and inspects descriptor/type chains. Body includes player-type-specific setup; exact complete semantics require a later refinement pass.

## FUN_0040F7B0 (entry within previous cluster)
- `thiscall`, one player object/index arg; returns 16-bit-ish selector.
- Reads descriptor field bits 8..23 and searches descriptor chain for type token `0x01696568`.
- If matching object has nonzero `+0x206C`, state `+0x205C` remaps return to `0x0D` or `0x0E`; otherwise returns extracted selector.

## FUN_0040F840
- `thiscall`, two player indices, `ret 0x08`.
- Requires owner per-player active byte `this+0x450+first` and equal global slot grouping `global+0x4C0[first] == global+0x4C0[second]`.
- Resolves first player and rejects if `0x009C7030` says blocked.
- Under owner lock, uses per-player float `this+0x474+first*4` and, for each eligible participant, forwards global player handle `+0x490[index]` to `0x004E4FA0`, applies float via `0x0041B5F0`/`0x0041B590`, and finalizes with `0x004E5030` (second path also `0x004E9340`).
- Clears owner active byte and float, then calls `0x009D3860(player,0x14)`.
- Per-player paired update/commit routine.

## FUN_0040F9A0
- `thiscall`, one player object arg; gated by `0x00AC1240`, and in mode 1 additionally `0x00AC11D0`.
- Uses player byte `+0x2ECC` as slot, owner float `+0x488[slot]`, and global handle `+0x490[slot]` to perform a service update through `0x004E4FA0`, `0x0041B6B0`, `0x004E5030`.
- Then obtains a related player/object via `0x009B6770(arg,0)` and may repeat a related slot update for that object.
- Body continues to next aligned boundary near `0x0040FAE0`.

No VERIFIED/MATCHED claim.