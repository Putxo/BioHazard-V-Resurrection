# Sequential decomp evidence — 0x00409800..0x0040B000

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Entry map

Confirmed aligned/INT3-delimited entries include `00409800`, `00409820`, `00409840`, `00409850`, `00409870`, `00409880`, `00409890`, `004098B0`, `004098D0`, `00409900`, `00409930`, `00409960`, `00409980`, `004099A0`, `004099D0`, `00409A00`, `00409A20`, `00409A30`, followed by larger routines through `0040AFC0`. Larger bodies remain sequentially bounded but are not source-converted until their dependencies are mapped.

## FUN_00409800

Takes one stack pointer argument. Calls that object's virtual slot `+0x10`, reads dword `result+0x20`, returns bits 8..23: `(value >> 8) & 0xFFFF`.

## FUN_00409820

`thiscall` predicate: returns true iff byte `this+0x20` is nonzero and byte `this+0x04 == 2`.

## FUN_00409840

`thiscall`, `ret 4`; returns dword at `this + 0x2C + index*0x20`.

## FUN_00409850

`thiscall`, `ret 4`; stack byte argument controls bit 1 of dword `this+0x08` using the compiler's xor-mask idiom. Semantically only bit `0x2` is replaced with `(argument & 1) << 1`; all other bits are preserved.

## FUN_00409870 / FUN_00409880

- `00409870` returns the current dword stored at global `0x016E3040`.
- `00409880` returns address `this+0x1310`.

## FUN_00409890 / 004098B0 / 004098D0

All select a per-index block using dword `this+0x20` and stride `0x8C74`, base `this+0x38`. `00409890` tail-jumps to `0x00B7A310`; `004098B0` calls `0x00B7CA00` then tail-jumps with the owner to `0x004E7A70`; `004098D0` calls `0x00B7A310` and then selects a second per-index block with stride `0x5940`, base `this+0x23210`, tail-jumping to `0x00AC07B0`.

## Bitset helpers

### FUN_00409900

Uses active index `this+0x20`, stride `0x8C74`, reads dword at per-index offset `+0x15C`, and returns that dword masked by `1 << stack_arg`.

### FUN_00409930

Uses active index and a bitset rooted at `this+0x16C`; computes word index `arg >> 5`, bit index `arg & 31`, and returns the selected dword masked by `1 << bit_index`. Per-active-index stride in dwords is `0x231D` (equivalent byte span `0x8C74`).

## Numeric field helpers

The field at `this + active_index*0x5940 + 0x273D8` is used repeatedly:

- `00409960`: returns unsigned value divided by 100. Compiler uses magic multiply `0x51EB851F` and shift 5.
- `00409980`: returns unsigned value modulo 100 using `div 0x64` and remainder EDX.
- `004099A0`: returns true for values in `[0x258,0x2BC)` or exactly `0x323`.
- `004099D0`: returns true for values in `[0x320,0x384)`.

No gameplay-semantic name is assigned to this field yet.

## FUN_00409A00 / 00409A20 / 00409A30

- `00409A00`: true iff either byte `this+0x5B80` or byte `this+0x5B81` is nonzero.
- `00409A20`: exact no-op `ret`.
- `00409A30`: exact tail jump to `0x011994B0`.

## Remaining sequential bodies

Entries beginning `00409A40`, `00409AA0`, `00409B00`, `00409D30`, `00409D40`, `00409D90`, `00409E40`, `00409EA0`, `00409ED0`, `00409F00`, `00409F70`, `00409F90`, `00409FB0`, `00409FE0`, `0040A080`, `0040A0F0`, `0040A240`, `0040A260`, `0040A3F0`, `0040A430`, `0040A450`, `0040A510`, `0040A670`, `0040A6F0`, `0040A930`, `0040AA40`, `0040AAE0`, `0040ABE0`, `0040AC00`, `0040AC10`, `0040AC80`, `0040ACC0`, `0040AD60`, `0040ADF0`, `0040AE70`, `0040AF40`, and `0040AFC0` are retained in strict address order for the next pass. They are not skipped; this file records the exact point at which detailed basic-block reconstruction continues.
