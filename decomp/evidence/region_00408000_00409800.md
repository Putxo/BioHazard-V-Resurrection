# Sequential decomp evidence — continuation through 0x00409800

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

The apparent `0x00408000` decode is inside the body beginning at `0x00407FD0`; it is **not** promoted as an independent entry. Subsequent entries below are backed by INT3/alignment boundaries.

## Compact allocator / flag helpers

### FUN_004085D0

Loads allocator owner from global `0x01641A0C`, dispatches virtual slot `+0x14` with caller-supplied size and fixed alignment `0x10`, returns the allocator result.

### FUN_004085F0

`thiscall`, `ret 4`; stack byte argument toggles bit `0x2000` in dword `this+4`: nonzero sets the bit, zero clears it.

### FUN_00408610

Exact tail jump to `0x0119F610`.

### Fixed/global accessors

- `00408620 -> address token 0x016F0230` (`mov eax,imm; ret`).
- `00408630 -> current dword stored at 0x016E2EFC` (`mov eax,[imm]; ret`), therefore this one is a value accessor, not an address-token accessor.
- `00408660 -> address token 0x016B24CC`.
- `004086C0 -> address token 0x016B2484`.
- `00408720 -> address token 0x016B24A8`.
- `00408760` is an exact one-byte `ret` no-op.

### Constructor/destructor pairs

`00408640` calls base constructor `0x00A67E10`, installs vtable `0x0137E440`, returns `this`. `00408670` installs the same vtable, calls base destructor `0x00A67E30`, and conditionally frees `this` through allocator-owner virtual slot `+0x18` when deletion-flag bit 0 is set.

`004086A0` / `004086D0` repeat that pattern with base ctor/dtor `0x00A712D0` / `0x00A71300` and vtable `0x0137E458`.

`00408700` / `00408730` repeat the same base pair with vtable `0x0137E470`.

No class names are assigned from vtable addresses alone.

## 0x00409420 helper cluster

### FUN_00409420 / FUN_00409430

- `00409420` is exact address-token accessor `0x01657DD0`.
- `00409430` returns the dword currently stored at global `0x016E2FD0`; it is deliberately not represented as a fixed address token.

### FUN_00409440

Returns `this` after writing vtable token `0x0137A97C` to `this+0`.

### FUN_00409450

Copies exactly 0x20 bytes from its stack pointer argument into `this+0x34` using four 8-byte moves; `ret 4`.

### State predicates at this+0x58

Recovered exactly in `region_00409420.cpp`:

- `00409480`: true for state 2 or 5.
- `004094A0`: true for state 3 or 4.
- `004094C0`: true for state 0, 3, or 4.
- `004094E0`: true for state 1, 2, or 5.

### FUN_00409500

Returns true iff dword `this+0x60 == 0` and byte `this+0x64 != 0`.

### FUN_00409520

Returns dword `this+0x08`.

### FUN_00409530

Reads dword `this+0x04`; returns true iff low three bits equal 2 and bit `0x400` is set.

### FUN_00409550

Reads dword `this+0x04`. Exact byte-register semantics are preserved: `CL` is masked to low three bits and must equal 2; `EAX` is shifted right by 10, then the resulting `AL` must share bit 1 with masked `CL` and must also have bit `0x10` set. The recovered C++ uses the masked low byte rather than the original unmasked low byte.

### FUN_00409570

Takes a pointer to three floats. It validates each component against two target constants at `0x0137E4E4` and `0x0137E4E0`; only when all three are strictly inside the accepted interval are they copied to `this+0x30`, `+0x34`, `+0x38`. Otherwise object state is unchanged. Constants are not named until their actual values/meaning are independently established.

### FUN_004095D0 / FUN_004095E0

- `004095D0` returns `this+0x30`.
- `004095E0` returns byte `this+0x21`.

These are included in the recovered compact-helper source.

## 0x004095F0..0x0040974D record constructors

A dense family initializes a common record layout spanning at least `0x28` bytes. Bodies write a leading dword, two 16-bit fields at `+4/+6`, dwords beginning at `+8`, and zero unused trailing fields through `+0x24`. Individual variants differ in constant type code (`0x0003`, `0x0018`, `0x001C`, etc.), ORed flag bits (`0x0A`, `0x02`, `0x80`) and number/order of stack parameters. They remain evidence-only until the common record type is reconstructed as a single layout rather than emitting duplicate guessed classes.

## FUN_00409750

Calls allocator/helper `0x0119F780`, copies a 0x28-byte record from the stack pointer argument into the returned node using five 8-byte moves, then clears link fields at `+0x20/+0x24`. If `this+4` already points to a node, links the new node after it through those fields; finally stores the new node into `this+4`. `ret 4`.

## FUN_004097B0

Copy/construct helper: installs vtable `0x014FA17C`, copies dwords from source offsets `+4..+0x18`, preserves the destination's high byte at dword `+0x1C` while replacing its low 24 bits from the source, then explicitly copies source byte `+0x1F` and dword `+0x20`; returns `this`, `ret 4`.

## Validation performed

The newly recovered `region_00407e60.cpp` and `region_00409420.cpp` were independently compiled with `g++ -std=c++20 -Wall -Wextra -Wpedantic`; both compile checks passed. Full repository CTest could not be executed in the analysis container because outbound GitHub cloning is unavailable there; CI remains the repository-level acceptance path.
