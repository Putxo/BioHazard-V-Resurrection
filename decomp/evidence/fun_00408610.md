# FUN_00408610 evidence

## Candidate membership and preflight

The task-supplied exact V6 row is:

~~~text
34320,4228624,1
~~~

This is exact row 136 of `function_candidates.csv`. Membership is
determined solely by the second decimal column:
`4228624 == 0x00408610`. The first decimal column is the RVA:
`34320 == 0x00008610`.

`FUN_00408610` is a real function entry. The canonical denominator therefore
remains **79,782** and no false-positive removal is created.

Remote preflight found no existing 8610 function row, claim, matching branch,
or PR. The exclusive reservation is `decomp/00408610-next`. Parallel
`FUN_00408550`, `FUN_004085D0`, `FUN_004085F0`, and all other work are
preserved.

## Exact target and boundary

- Product: Resident Evil 5 PC 1.1.0 Dev.
- Exact executable size: `19,977,216` bytes.
- Exact executable SHA-256:
  `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- PE32 x86 image base: `0x00400000`.
- VA: `0x00408610`; RVA: `0x00008610`; section: `.text`.
- Native body: `0x00408610..0x00408614`, exactly **5 bytes**.
- Native-body bytes:
  `e9 fb 6f d9 00`.
- Native-body SHA-256:
  `6094f34ba2820fa6e469cfa5b073da2b1a3c8a5078fec226a14ba931afe73a17`.
- `0x0040860B..0x0040860F` is INT3 padding before the root.
- `0x00408615..0x0040861F` is INT3 padding before the next aligned root
  `0x00408620`.
- The 16-byte root-plus-padding span `0x00408610..0x0040861F` has SHA-256
  `2352debf90d3f28ab4bcd3fba96b3028f03a28a2cae7ff63e72ee3d081dde026`.

GNU objdump and LLVM llvm-objdump independently agree:

~~~text
00408610  E9 FB 6F D9 00        jmp 0119F610h
~~~

This is a pure tail-jump thunk. There is no fall-through and no local
conditional branch or return instruction.

The private executable was rehashed and analyzed locally only. It is not
committed or uploaded.

## Tail target 0x0119F610

The tail target is a separate 66-byte native routine at
`0x0119F610..0x0119F651`. Its body SHA-256 is:

~~~text
f843bb702ecb388daab60ce285f080ebb24fd273f344577497b33a07b24f3693
~~~

Exact instructions:

~~~text
0119F610  A1 58 30 65 01        mov eax,[01653058h]
0119F615  8B 15 60 30 65 01     mov edx,[01653060h]
0119F61B  8B C8                 mov ecx,eax
0119F61D  C1 E1 0F              shl ecx,0Fh
0119F620  33 C8                 xor ecx,eax
0119F622  A1 5C 30 65 01        mov eax,[0165305Ch]
0119F627  A3 58 30 65 01        mov [01653058h],eax
0119F62C  89 15 5C 30 65 01     mov [0165305Ch],edx
0119F632  8B 15 64 30 65 01     mov edx,[01653064h]
0119F638  8B C2                 mov eax,edx
0119F63A  C1 E8 11              shr eax,11h
0119F63D  33 C1                 xor eax,ecx
0119F63F  C1 E8 04              shr eax,4
0119F642  33 C2                 xor eax,edx
0119F644  33 C1                 xor eax,ecx
0119F646  89 15 60 30 65 01     mov [01653060h],edx
0119F64C  A3 64 30 65 01        mov [01653064h],eax
0119F651  C3                    ret
~~~

Let the four state dwords at
`0x01653058, 0x0165305C, 0x01653060, 0x01653064` be
`s0,s1,s2,s3`. The exact state transition is:

~~~text
mixed = s0 ^ (s0 << 15)
next = ((s3 >> 17) ^ mixed)
next >>= 4
next ^= s3
next ^= mixed

s0 = s1
s1 = s2
s2 = s3
s3 = next
return next
~~~

All operations are 32-bit unsigned operations. There is no zero-state special
case or reseed branch.

## ABI and caller evidence

A complete decoded scan of the exact `.text` section finds exactly **3**
direct rel32 calls to `0x00408610`:

~~~text
0056DE58  call 00408610h
0056DE82  call 00408610h
0056FFA9  call 00408610h
~~~

The callers establish the observed no-stack-argument thiscall ABI by obtaining
the singleton-like object address through the neighboring getter
`0x00408620`, moving that result to ECX, and then calling 8610:

~~~text
0056DE51  call 00408620h
0056DE56  mov  ecx,eax
0056DE58  call 00408610h
0056DE5D  push eax
...
0056DE7B  call 00408620h
0056DE80  mov  ecx,eax
0056DE82  call 00408610h
0056DE87  push eax
~~~

A third caller consumes the result as random low bits:

~~~text
0056FFA2  call 00408620h
0056FFA7  mov  ecx,eax
0056FFA9  call 00408610h
0056FFAE  and  eax,7
0056FFB1  cmp  eax,3
~~~

The neighboring getter returns constant object address `0x016F0230`.
However, the tail target `0x0119F610` never reads ECX and instead operates
only on the four fixed state globals. The portable recovery therefore keeps
an explicit this-object parameter to preserve the observed ABI while injecting
the global state separately for testability.

The exact executable contains zero raw little-endian absolute
`0x00408610` pointer values.

## Tests and guards

`tests/unit/test_fun_00408610.cpp` covers:

- missing services host guard;
- present services with missing global-state pointer;
- exact first transition from state `{1,2,3,4}`, returning `0x00008805`;
- exact second transition and four-word state rotation, returning
  `0x00019807`;
- distinct this objects proving the native state is global rather than stored
  in the observed object;
- high-bit/unsigned wrap behavior with expected result `0x9ABCA225`;
- all-zero native state remaining all-zero.

The two missing-state exits are portable host-safety guards only. Native 8610
is a branchless jump thunk and native `0x0119F610` assumes its fixed globals
exist. There is no native early return to invent.

An isolated C++20 build/run passes with
`-Wall -Wextra -Wpedantic -Werror`.

## Scope

Only `FUN_00408610` is recovered here. Parallel 8550/85D0/85F0 and all other
work are preserved. The denominator remains 79,782.

No EXE, DLL, proprietary asset, `.obj`, or binary product is committed.
