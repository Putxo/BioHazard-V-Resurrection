# FUN_004066C0 evidence

## Target

- Evidence was taken only from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x004066C0`, RVA `0x000066C0`.
- Candidate body: 27 bytes (`0x1B`), through the `ret` at `0x004066DA`.
- Candidate byte SHA-256: `5c7a23b35c6df3671b2dda2e0105f92c0372c54f66a6b57566ec4efc484c221a`.
- `0x004066DB..0x004066DF` is `INT3` padding; the next aligned root begins at `0x004066E0`.
- GNU `objdump` and LLVM `llvm-objdump` independently agree on the instruction boundaries and control flow below.

## Instruction evidence

```text
004066C0  push 00h
004066C2  push 00h
004066C4  push 09h
004066C6  push 01567958h
004066CB  push 0137BFB4h
004066D0  push FFFFFFFFh
004066D2  call 004061F0h
004066D7  add esp,18h
004066DA  ret
```

The wrapper forwards six cdecl arguments to the separately recovered `FUN_004061F0`:

1. message index `-1` (`0xFFFFFFFF`);
2. archive literal at `0x0137BFB4`;
3. direct-table token `0x01567958`;
4. direct count `9`;
5. resolved-table token `0`;
6. resolved count `0`.

The archive literal at `0x0137BFB4` is:

```text
Image\\Archive\\VersusResource
```

The `add esp,18h` removes exactly six pushed dwords and the function returns with a plain `ret`, confirming a caller-cleaned cdecl no-argument wrapper.

## Table evidence

The direct table at `0x01567958` contains exactly nine 8-byte `(name pointer, resource token)` records, matching the pushed count `9`:

```text
0  id\\jpn\\versus\\versus_00    01692E3C
1  id\\jpn\\versus\\versus_01    01692E3C
2  id\\jpn\\versus\\versus_02    01692E3C
3  id\\jpn\\versus\\versus_03    01692E3C
4  id\\jpn\\versus\\versus_04    01692E3C
5  id\\jpn\\versus\\versus_05    01692E3C
6  id\\jpn\\menu\\top09          01692E3C
7  id\\jpn\\menu\\top15          01692E3C
8  id\\jpn\\versus\\versus_10    01692E3C
```

The raw pointer pairs are:

```text
01567958: 0137BFD4 01692E3C
01567960: 0137BFEC 01692E3C
01567968: 0137C004 01692E3C
01567970: 0137C01C 01692E3C
01567978: 0137C034 01692E3C
01567980: 0137C04C 01692E3C
01567988: 0137BDB4 01692E3C
01567990: 0137BE18 01692E3C
01567998: 0137C064 01692E3C
```

Nine records occupy `0x48` bytes, so the direct table ends exactly at `0x015679A0`, the next table boundary used by the adjacent wrapper at `0x004066E0`. The second table/count pair is explicitly `(0, 0)`, so this wrapper supplies no resolved-entry table. The reconstruction records the observed table address and count as opaque forwarding tokens and does not duplicate or alter `FUN_004061F0`.

## Caller evidence

A direct `CALL rel32` scan of the complete exact `.text` image finds exactly one direct call to `0x004066C0`, at `0x00509BDF`.

The caller first compares the current resource name against the same `VersusResource` literal and invokes the wrapper only on equality:

```text
00509BCD  mov edi,0137BFB4h
00509BD2  mov esi,ebp
00509BD4  mov ecx,1Dh
00509BD9  xor edx,edx
00509BDB  repz cmps byte ptr [esi],byte ptr [edi]
00509BDD  jne 00509BEEh
00509BDF  call 004066C0h
00509BE4  pop ebp
```

`0x1D` bytes is the full archive string including its terminating zero. No argument is pushed for the `call 004066C0h`, which agrees with the no-argument wrapper contract.

## Early exits and reconstruction choice

The original 27-byte body has no conditional branch and no early-return path: it always forwards the six constants to `FUN_004061F0`, cleans the stack, and returns.

The portable reconstruction adds only two harness guards: a null-services guard and a null-dispatch guard. Both are unit-tested explicitly as host-only early exits and are not claimed as original binary control flow. The normal-path test verifies every observed forwarded argument.

`FUN_00406600`, `FUN_00406620`, and `FUN_00406680` are active or parallel assignments and are not modified by this recovery. `FUN_004061F0` is treated as an existing opaque dispatch dependency; this candidate only reconstructs the fixed forwarding wrapper at `0x004066C0`.
