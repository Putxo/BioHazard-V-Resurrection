# FUN_00403AF0 evidence

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

- VA: `0x00403AF0`
- RVA: `0x00003AF0`
- Exact machine body: `0x00403AF0..0x00403F46` inclusive (`0x457` / 1111 bytes).
- The body is followed by nine `INT3` bytes at `0x00403F47..0x00403F4F`; the next aligned body begins at `0x00403F50`.
- Calling convention: `thiscall`; owner object arrives in `ECX` and the epilogue is `ret 0x04`, proving one callee-cleaned 32-bit stack argument.
- This is the next ordered function after `FUN_00403640`; no later candidate is promoted ahead of it.

## Entry and path validation

- The stack argument is dereferenced at `0x00403AF3..0x00403AF7`. A null inner pointer uses the empty-string fallback at `0x0137AA5C`; otherwise the character data begins at `inner+0x08`.
- The resulting path is passed to `0x0119B910` at `0x00403B12`. A false result exits through the common epilogue without entering the traversal.
- If global count `0x01657B8C` is zero, `FUN_00402B10(0x016E20DC)` is called before enumeration, matching the registry/table ensure behavior already observed in the preceding ordered region.

## Enumeration and recursion

- A search string is assembled from the input path plus literal `0x0137AE48`, which is exactly `"\\*"`.
- Enumeration is driven by `0x0119B790`; the loop continues from `0x00403F0D` back to `0x00403B84` while another entry exists.
- Each entry path is assembled by appending literal `0x0137AE44` (`"\\"`) and the current entry name.
- Literals `0x0137AB48` and `0x0137AB44` are exactly `"."` and `".."`; both are skipped at `0x00403BDC` and `0x00403BFC`.
- Entry flag bit 0 is tested at `0x00403C04`. When set, the function recursively calls itself at `0x00403C14` using the newly assembled child path and the same owner object.

## Non-directory path

- Non-directory entries are normalized through the existing string helpers and compared against the address-stable registry arrays rooted at `0x01656380` and `0x01657380`, bounded by count `0x01657B8C`.
- The loop performs an exact byte-string comparison between the current normalized key and each non-null registry name. Only entries with a corresponding non-null value in `0x01657380` continue into the binding/removal path.
- The code uses globals at `0x016E1D3C` offsets `+0x1028` and `+0x102C` while constructing derived paths/keys. Their semantic field names are not yet proven and are intentionally left unnamed.
- A derived path is validated again through `0x0119B910` at `0x00403E1A`. Failed validation continues to the next registry item.
- After the registry scan, `0x0119B710` is called on the assembled entry path. When it succeeds, the function emits the exact embedded format string `"Removed. %s\n"` (`0x0137AE4C`) through logger `0x01198F40` with context `0x01652E00`.

## Ownership / cleanup evidence

- Several temporaries use reference-counted objects: the body increments a dword reference count before retaining a value and decrements it on release. A zero count dispatches the release operation through global object `0x01652F28`, vtable slot `+0x18`.
- Enumeration and string temporaries are released on all observed exits, including recursive and skipped-entry paths.
- The final epilogue at `0x00403F3F` restores `ESI`/`EBX`, releases the `0x64`-byte local frame, and executes `ret 4`.

## Status

`FUN_00403AF0` is **DISASSEMBLED / evidence-complete for the sequential pass**, not yet `FAST_PASS` or `CONVERTED`. The traversal, recursion, registry matching, path validation and successful-removal log are machine-supported, but the concrete types/contracts of the string/enumerator objects and globals `0x016E1D3C`, `0x01652F28`, `0x01656380` and `0x01657380` are not sufficiently constrained for a non-opaque C++ reconstruction. Per the project acceptance rule, no guessed source implementation is introduced.
