# FUN_00403AF0 evidence

- VA: `0x00403AF0`
- RVA: `0x00003AF0`
- Size: `1120` bytes in the ordered candidate inventory, ending at the next baseline candidate `0x00403F50`.
- Calling convention: `thiscall`; `ecx` is preserved as context and the function returns with `ret 4`.

## Instruction evidence

- The first stack argument is dereferenced, then the string payload at pointer `+8` is used, with fallback literal `0x0137AA5C`.
- The candidate name is validated by the same external predicate used in nearby resource walkers; failure exits through the cleanup path.
- If global table count `0x01657B8C` is zero, `FUN_00402B10` is called with registry root `0x016E20DC` before scanning.
- The function constructs string temporaries, opens an iterator, and repeatedly probes it through the same external boolean iterator routine.
- Entry names are filtered against literals at `0x0137AB48` and `0x0137AB44`, matching the `"."` and `".."` skip behavior.
- Entries whose flag byte has bit 0 set recursively call `0x00403AF0`.
- Leaf entries are normalized and compared against the global name table rooted at `0x01656380`, bounded by `0x01657B8C`; associated records from the parallel table at `0x01657380` must be non-null before binding/reporting.

## Host reconstruction

The recovered host form exposes validation, registry ensure, scanning, recursive descent, table lookup, binding, and missing-record reporting through `FUN_00403AF0_Services`. It preserves the fallback-name behavior, registry-populate guard, dot-entry filtering, recursive call, extension trimming before lookup, and bind/missing side effects without embedding engine code or binary payloads.
