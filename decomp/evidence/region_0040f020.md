# Ordered decompilation evidence — 0x0040F020 through 0x0040F3B0

## FUN_0040F020
- `thiscall`, one byte/boolean stack arg, `ret 0x04`.
- Computes desired boolean from root flags: bit 17 of `+0x1E4F8`, flag `0x10000000`, caller flag, or `+0x1E550 & 0x100000` force true.
- When desired state differs from owner byte `+0x448`, optionally locates a named/resource object through `0x01199D40`, `0x00D9AA20`, `0x00D9A880` and toggles its flag `0x800`; stores inverse in owner byte `+0x449`.
- Iterates linked objects from global registry `0x016E1690+0xAC`; for objects whose descriptor chain matches global type token `0x016E3360`, calls `0x00D9FDD0(object,!desired)`.
- Stores desired to `this+0x448`; always calls `0x00475240(global 0x016E2044)`.
- Global visibility/enable propagation routine.

## FUN_0040F130 / FUN_0040F1A0 / FUN_0040F210
- Three sibling `stdcall` boolean toggles, each `ret 0x04`.
- True path calls `0x0041B150(global_root, mask, 1)` with masks respectively `0xFFE02000`, `0x001F9000`, `0x00008000`.
- False path synchronizes and clears the corresponding mask bits from root DWORD `+0x1E500`.
- Exact flag-group togglers.

## FUN_0040F280
- `stdcall`, one integer arg.
- Compares arg against current-user threshold/value `+0x273E0`; acts only when greater.
- If global byte `+0x56C` is set, dispatches `0x004A1BF0(global 0x016E03CC,9,arg,0)`; otherwise calls `0x0041BB30(arg,1)`.

## FUN_0040F2D0 / FUN_0040F2F0
- `thiscall`, no args; synchronized getters protected by owner lock `this+0x08`.
- Return owner DWORD `+0x420` and `+0x42C` respectively.

## FUN_0040F310
- `thiscall`, one player/index arg, `ret 0x04`.
- Validates index/object via `0x00523300`; invalid returns zero.
- Under owner lock, indexes table with `(arg+0x14)*0x2C` and returns the DWORD stored at that computed owner location.

## FUN_0040F360
- `thiscall`, one player/index arg, same validation/locking pattern.
- Returns owner table DWORD at `this+0x388 + arg*0x2C`.

## FUN_0040F3B0
- `thiscall`, two stack args, `ret 0x08`.
- Validates first player/index arg through `0x00523300`.
- Under owner lock, if table pointer `this+0x444` exists, obtains index via `FUN_0040F310`, multiplies second integer by float scale `table[index]`, converts to integer, and preserves minimum 1 when original value was positive but scaled result became zero.
- Clamps result to maximum `0x7FFF`.
- Per-player scaled/clamped numeric conversion.

Next sequential body at `0x0040F430`; next aligned post-padding candidate later is `0x0040F500`. No VERIFIED/MATCHED claim.