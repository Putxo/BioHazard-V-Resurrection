# Ordered decompilation evidence — 0x00417120 through 0x00417360

## FUN_00417120
- `thiscall`, `(slot,delta_float)`, `ret 0x08`; active only in global mode 1.
- Resolves/validates player; if global per-slot handle/state requires cleanup, calls player virtual operations, sets player byte `+0x12F4`, invokes `0x009B7150`, and in non-`0x00AC11D0` mode clears per-slot counters `+0x690/+0x69C/+0x6B4`.
- Otherwise, when synchronized global fields match, decrements owner per-slot float timer `+0x4C0[slot]`; on zero calls `FUN_0040BBC0(slot)` and player cleanup `FUN_00410810(slot)`, optionally dispatching `0x004A0BD0`.
- If timer was nonpositive but player virtual predicate true, resets it from constant `0x01569E14`.

## FUN_004172C0
- `thiscall`, three stack args, `ret 0x0C`.
- Searches 6-byte records at table `0x01569F48` for caller key; defaults companion WORDs from `0x01569F44/48`.
- Optional third arg doubles first companion value.
- Forwards `(arg1, valueA, valueB)` to `FUN_004111C0`.

## FUN_00417330
- Factory: calls allocator vtable slot `+0x14` on global `0x01641A0C` for size `0x510`, alignment `0x10`; non-null result tail-jumps to constructor `0x004121A0`, else returns null.

## FUN_00417360
- Begins large registration/reflection metadata routine.
- Builds repeated descriptor records on stack and registers them through `0x00409750` against caller-supplied registry/context.
- Observed literals/tokens `0x0137E828`, `0x0137E81C`, `0x0137E810`, `0x0137E800`, `0x0137AA5C`, `0x0137E7F0`, `0x0137E7E0`, `0x0137E7D4`, `0x0137E7C8`, `0x0137E7B8`, `0x0137E7A8`; callback pointers include `0x0041C0D0`, `0x0041C0E0`, `0x0041BFE0`, `0x0041C020`.
- Associates fields including owner `+0x138` and `+0x348` with metadata records.
- Strong semantics: class/property reflection/serialization registration. Body continues to candidate `0x00417A60`.

No VERIFIED/MATCHED claim.