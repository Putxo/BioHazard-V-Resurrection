# Ordered decompilation evidence — 0x0040D130 through 0x0040D370

## FUN_0040D130
- bool predicate/action.
- Reads current-user record via `0x0165BBB0`, stride `0x5940`, field `+0x273D8`.
- Values in `[0x258,0x2BC)` or exactly `0x323` bypass per-player rejection checks; otherwise rejects root flags `0x20000000` or `0x08000000`.
- Iterates four active-player bits from global state `+0x478`, resolves each player via `0x005231E0`, validates with `0x00523330`, and rejects if `0x009C7080(player)` is true.
- Success synchronously ORs root `+0x1E4F8` with `0x80000000` and returns true; rejection returns false.

## FUN_0040D220
- `thiscall`, one pointer/record stack arg, `ret 0x04`.
- Runs only once while byte `this+0x58 != 1` and `FUN_0040D130()` succeeds; then sets that byte.
- Reads first DWORD from supplied record as an ID. In global modes `3/4`, helper `0x00AC1890` may remap IDs `0x3F -> 0x40`, `0x42 -> 0x43`, `0x44 -> 0x45`.
- Stores selected ID to `this+0x5C` and copies 0x30 bytes from input record to `this+0x70`.
- Unless owner byte `+0x04 == 1`, when global `+0x574 == +0x578` calls `0x004A0650(global 0x016E03CC, record)`.
- Sets `this+0x6C = 3` on normal completion.

## FUN_0040D310
- bool lookup/transition helper.
- Scans signed-WORD triplets rooted at `0x015688C8` until negative sentinel, comparing first field against `this+0x64`.
- On match, extracts the next two WORDs, calls `FUN_0040D050(this, valueA, valueB, 1)` and then `FUN_0040D0C0(this)`, returning true.
- Returns false if no table entry matches.

## FUN_0040D370
- One-time initializes a global table block at `0x01657E30..0x01657EB8` guarded by bit in `0x01657EC0`.
- Initializes three 0x30-byte-style records with IDs `0x0E`, `0x21`, `0x32`, associated WORD values (`0x0DE8`, `0x1D74`, zero for third), flags/bytes, and two repeated 3-float vectors sourced from `0x017602C0/4/8`.
- Then scans WORD-pair table rooted `0x01568924` against `this+0x64`.
- On match stores companion WORD into `this+0x5C`, zeroes/copies 0x30 bytes into `this+0x70` from selected initialized record, and calls `0x00409DC0(this)`, returning true; no match returns false.
- Strong semantics: lazy initialization plus table-driven preset selection.

Next ordered entry: `0x0040D540`. No VERIFIED/MATCHED claim.