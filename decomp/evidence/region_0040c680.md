# Ordered decompilation evidence — 0x0040C680 through 0x0040CA90

## FUN_0040C680
- `thiscall`; large state machine controlled by byte `this+0x05`, with states 0..30 mapped through byte table `0x0040CA60` and jump table `0x0040CA30`.
- Initializes owner fields `+0x35C/+0x360`, examines global state `+0x2C`, and transitions outer state byte `this+0x04` when a sequence completes/fails.
- Coordinates global service `0x016E03CC` through helpers including `0x004A8490`, `0x0049E310`, `0x004A50C0`, `0x0049E2C0`, `0x004A1E60`, `0x004A1970`, `0x00495640`, `0x004A53A0`, `0x004A2270`.
- Uses player index/value derived from `0x0165BBB0` and per-player records with stride `0x5940`; manipulates global bitmask `+0x57C` and state `+0x2C`.
- Distinct paths test capability bits `0x02/0x04` in a per-user structure and issue corresponding service transitions.
- Contains timed wait using owner float `+0x30`; expiry invokes `0x004A2270` and moves substate to `0x1E`.
- Strong semantics: multi-step player/session transition state machine. Exact symbolic state names remain unresolved.

## FUN_0040CA90
- Next state machine begins at `0x0040CA90`, keyed by byte `this+0x05`, accepted states 0..9 through jump table `0x0040CDB0`.
- Initial state ensures several global conditions by calling `0x004093D0` with flag masks when root flags/mode/byte `+0x798` disagree; waits for root `+0x1E4F8 & 0x40000`.
- Then configures global `0x0165C148`, requests masks `0x10000000`, `0x200`, `0x400`, `0x40000000`, and advances state.
- Subsequent states coordinate `0x005092D0`, `0x004A1E60`, `0x004A1970`, `0x00495640`, and reset global timing through `0x0048C8A0`.
- Owns pointer `this+0x2C`: old object has flag `0x2000` cleared and virtual slot `+0x30` called; replacement acquired via `0x0084B370(0x016A1028)`, flag `0x2000` set, registered with `0x00D05520(global 0x016E1690,0x1A,obj)`, and mirrored into global `0x016E03CC+0x1940`.
- Calls a vtable operation on global `0x016922AC` with float constant `0x0137E50C` and selector `0x20`.
- Body continues beyond the excerpt; this function remains the active ordered target.

No VERIFIED/MATCHED claim.