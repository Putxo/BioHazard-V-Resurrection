# FUN_00405DF0 instruction evidence

Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Boundary and ABI

- VA: `0x00405DF0`
- RVA: `0x00005DF0`
- Size: **533 bytes** (`0x215`), through the `RET` at `0x00406004`.
- Function-byte SHA-256: `bd79211062308190f07b9aa81e435a20b675c81a83042420ad170d5c6335936c`.
- `0x00406005..0x0040600F` are `INT3`; the next aligned code root starts at `0x00406010`.
- The function reserves `0x20C` bytes and saves `EBP/ESI/EDI/EBX`. At `0x00405E2C`, `[esp+0x21C]` resolves to the caller's first stack argument (`[entry_esp+4]`). The function ends with a plain `RET`, establishing a caller-cleaned single-argument contract.
- Direct CALL xrefs observed in the exact target: `0x004064C4`, `0x00406E11`, `0x0050998C`. The first two callers pass an integer loop value from `0` through `10`; the third passes the current dword stored in the same selector field that this function temporarily replaces.

## Instruction-backed behavior

### Save and replace active selector

- `0x00405DF0`: `mov eax, [0x0165BBB0]`.
- `0x00405DF5..0x00405E09`: reads the active slot index at `+0x20`, multiplies it by `0x8C74`, and saves the dword at `base + index*0x8C74 + 0x70` into `EBP`.
- `0x00405E04..0x00405E28`: if byte `base+0x1C` is nonzero, or global byte `0x016E2090` is nonzero, the code calls the imported synchronization hook at `[0x01371178]` with `base+4`.
- `0x00405E29..0x00405E39`: the first stack argument is stored into `base + index*0x8C74 + 0x70`.
- `0x00405E3D..0x00405E54`: the matching conditional hook at `[0x013710E8]` is called with `base+4`.
- `0x00405FA1..0x00405FE8`: the same conditional hook pair surrounds restoration of the saved `EBP` value to the selector field.

### Global loading flag and path construction

- `0x00405E55..0x00405E67`: saves byte `[0x016E1D3C + 0x904D]` in `BL`, then forces that byte to `1`.
- `0x00405E68..0x00405E87`: calls `0x004E99F0` on global object `0x0165BBB0` with literal `0x0137B67C`, which is `Image\\Archive\\Msg2Resource`, then formats it with literal `0x0137B518`, `%s`, into the first local path buffer.
- `0x00405E88..0x00405EBC`: reads global-root pointer `[0x016E1D3C + 0x102C]`, falls back to literal `0x0137AA5C` (empty string) when null, and formats root plus the first local path using literal `0x0137AAD4`, `%s\\%s.arc`, into the second local path buffer.

### Optional special registration and early cleanup branch

- `0x00405EBD..0x00405ECF`: tests bit `0x00200000` in `[0x016E1660 + 0x1E550]`; when clear, control skips directly to the batch path at `0x00405F17`.
- `0x00405ED1..0x00405EDE`: passes the second local archive path to `0x0119B710`.
- `0x00405EE7..0x00405EF8`: invokes `0x00D14C10` with destination token `0x016E216C`, the first local path, and enabled value `1`.
- `0x00405EFA..0x00405F03`: a nonzero registration result is finalized through `0x00D25060`.
- `0x00405F03..0x00405F15`: calls `0x0119B910` on the second local archive path. If `AL != 0`, execution jumps to cleanup at `0x00405F95`; therefore the batch loop is skipped but the loading byte and selector are still restored.

### Nine-entry message-resource batch

- `0x00405F17..0x00405F28`: passes the first local path to `0x00A52D90`, then calls `0x00A54010` to enter the batch.
- `0x00405F29..0x00405F89`: iterates `ESI = 0, 8, ... 0x40`, stopping at `0x48`, proving **9** table records of 8 bytes starting at `0x01567548`.
- For each record, the first dword is passed through `0x004E99F0`; the second dword and resolved path are then passed to `0x00D14C10` with enabled value `1`.
- The exact nine path literals referenced by the first dwords are:
  1. `etc\\message\\mes_system`
  2. `etc\\message\\mes_item`
  3. `etc\\message\\mes_file`
  4. `etc\\message\\mes_event`
  5. `etc\\message\\mes_stage1`
  6. `etc\\message\\mes_stage2`
  7. `etc\\message\\mes_stage3`
  8. `etc\\message\\mes_stage5`
  9. `etc\\message\\mes_stage7`
- All nine second dwords in the exact table are `0x01692FC8`.
- `0x00405F59..0x00405F82`: when global suppressor bytes `0x016B1F39` and `0x016B1F3B` are both zero and the registration result is nonzero, `0x005477E0` is called through global context `0x016B1F3C` with the address of the local registration result.
- `0x00405F8B..0x00405F94`: closes the batch with `0x00A54120(0)`.

### Cleanup

- `0x00405F95..0x00405FA0`: restores the original loading byte saved in `BL` to `[0x016E1D3C + 0x904D]`.
- `0x00405FA1..0x00405FE8`: restores the prior selector dword saved in `EBP`, using the same conditional synchronization hooks as the entry mutation.
- `0x00405FE9..0x00405FFB`: calls `0x01198F40` with `0x01652E00` and literal `0x0137AF90` (`<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<\n`).
- `0x00405FFB..0x00406004`: restores registers and the `0x20C`-byte local frame and returns.

## Reconstruction choices

The recovered C++ keeps target-global addresses only as evidence-backed opaque destination tokens and exposes external engine operations through injectable services. It preserves the observable ordering that matters for the target routine: selector swap, loading-byte force, path construction, optional special registration/cancel branch, nine-entry batch, then restoration of the original loading byte and selector on both the normal and cancel paths. No neighboring candidate is implemented by this change.
