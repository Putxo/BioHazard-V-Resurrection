# FUN_004064E0 evidence

## Target

- Exact local reference executable only; no binary is committed.
- SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- PE32 x86 image base `0x00400000`.
- Function VA `0x004064E0`, RVA `0x000064E0`.
- Candidate body is 33 bytes (`0x21`), ending at the `ret` at `0x00406500`.
- SHA-256 of bytes `0x004064E0..0x00406500`: `5faa02528106f4d9bf29a5b8f6f58822dd2d291d9ecd081ea63f1698fbcd3e3a`.
- `0x00406501..0x0040650F` is `INT3` padding; the next aligned function starts at `0x00406510`.

## Instruction evidence

```text
004064E0  mov eax,[esp+04h]
004064E4  push 07h
004064E6  push 01567608h
004064EB  push 0Fh
004064ED  push 01567590h
004064F2  push 0137B698h
004064F7  push eax
004064F8  call 004061F0h
004064FD  add esp,18h
00406500  ret
```

The wrapper therefore accepts one caller-cleaned stack argument and forwards six arguments to `0x004061F0` in cdecl order:

1. incoming selector;
2. literal at `0x0137B698`;
3. table token `0x01567590`;
4. count `15`;
5. table token `0x01567608`;
6. count `7`.

The referenced literal at `0x0137B698` is `Image\\Archive\\GameResource`.

No branch or machine-code early-exit exists in the 33-byte candidate. The C++ host implementation adds only a null-services/null-dispatch safety guard so the portable unit harness can run without the original engine; tests label this explicitly as harness-only behavior.

## Caller evidence

A direct `CALL rel32` scan of the exact `.text` image finds one direct call to `0x004064E0`, at `0x005099F2`.

Immediately before that call, the caller loads the active slot from global `0x0165BBB0`, applies stride `0x8C74`, reads field `+0x70`, pushes that dword, calls `0x004064E0`, and then performs `add esp,4`. This confirms a one-dword cdecl argument and caller cleanup.

The nearby caller compares its current resource name against the same `Image\\Archive\\GameResource` literal before selecting this wrapper.

## Reconstruction choice

The original wrapper contains no local table iteration and no control flow beyond a single call and return. The reconstruction therefore keeps `0x004061F0` opaque and injectable rather than modifying or reimplementing that separately assigned function. It preserves exactly the observed selector, literal, table tokens, counts, call order, and void return behavior.
