# FUN_00408630 evidence

## Candidate membership and preflight

The task-supplied exact V6 row is:

~~~text
34352,4228656,1
~~~

This is exact row 138 of the supplied `function_candidates.csv` view.
Membership is determined solely by the exact second decimal column:
`4228656 == 0x00408630`. The first decimal column is the RVA:
`34352 == 0x00008630`.

The canonical local `work/analysis/function_candidates.csv` is intentionally
not tracked in Git. This literal row is preserved as task evidence. No
denominator correction is involved; the V6 denominator remains **79,782**.

Remote preflight before reservation found no `FUN_00408630` row in
`database/functions.csv` or `database/claims.csv` on either `main` or
`integration`, no matching remote branch, PR, or commit. The exclusive
reservation is `decomp/00408630-next`. Parallel `FUN_00408620` is not
modified.

## Exact target and boundary

- Product: Resident Evil 5 PC 1.1.0 Dev.
- Exact executable size: `19,977,216` bytes.
- Exact executable SHA-256:
  `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- PE32 x86 image base: `0x00400000`.
- VA: `0x00408630`; RVA: `0x00008630`; section: `.text`.
- Native body: `0x00408630..0x00408635`, exactly **6 bytes**.
- Native bytes: `a1 fc 2e 6e 01 c3`.
- Native-body SHA-256:
  `07a9a02e49b2ad789555765717171e143cb4b0973aa4dcb52f343bce0f8f3d36`.
- `0x00408636..0x0040863F` is INT3 padding before the next aligned root
  `0x00408640`.
- The 16-byte root-plus-padding span `0x00408630..0x0040863F` has SHA-256
  `cfedcb410df88aa555033f7abeec5b4cbd5e933c86f5e8ee3b03ccaa5c763a06`.

GNU objdump decodes the complete function as:

~~~text
00408630  A1 FC 2E 6E 01   mov eax,dword ptr [016E2EFCh]
00408635  C3               ret
~~~

There is no conditional branch, stack adjustment, ECX use, or local mutation.
The exact behavior is therefore a no-argument global pointer/value getter.

The private executable was rehashed and inspected locally only. It is not
committed or uploaded.

## Caller evidence

A complete rel32 scan of the exact target finds four direct calls:

~~~text
00D932D5  call 00408630h
00D932E2  call 00408630h
00D932FD  call 00408630h
00D93314  call 00408630h
~~~

The first result is tested directly for null:

~~~text
00D932D5  call 00408630h
00D932DA  test eax,eax
00D932DC  je   00D93323h
~~~

Other call sites immediately move the returned value to ECX and dispatch
methods through it. Pushes immediately before some getter calls remain on the
stack and are consumed by the subsequent callee, confirming that
`FUN_00408630` itself takes no stack arguments and performs no cleanup.

## Recovery and tests

The portable recovery injects the fixed global value through
`FUN_00408630_Services` and returns it unchanged. Tests cover:

- missing-services host guard;
- mapped global whose current value is null;
- exact pointer identity;
- repeated reads with no mutation;
- changing the modeled global between calls;
- transition back to null.

The missing-services check is a host-only safety guard. Native code has no
early return and assumes the fixed global address is mapped. The null global
value itself is native behavior because the instruction simply returns the
stored dword unchanged.

Only `FUN_00408630` is recovered here. `FUN_00408620` and all parallel
work remain untouched. No EXE, DLL, proprietary asset, `.obj`, or other
binary product is committed.
