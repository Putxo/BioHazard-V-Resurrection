# FUN_00402020 evidence

## Candidate

- RVA: `0x00002020`
- VA: `0x00402020`
- Size: `64` bytes through alignment before next candidate `0x00402060`.
- Candidate source: V6 ordered candidate after `FUN_00402000`.

## Disassembly body

```asm
00402020  push ebx
00402021  mov  ebx, dword ptr [esp + 8]
00402025  test ebx, ebx
00402027  push esi
00402028  mov  esi, ecx
0040202A  je   0x0040205B
0040202C  push edi
0040202D  mov  edi, dword ptr [esp + 0x14]
00402031  test edi, edi
00402033  je   0x0040205A
00402035  test byte ptr [esi + 4], 1
00402039  je   0x00402045
0040203B  push esi
0040203C  call dword ptr [0x013714EC]
00402045  push 0x44D
0040204A  push esi
0040204B  mov  dword ptr [esi + 0x0C], ebx
0040204E  mov  dword ptr [esi + 0x10], edi
00402051  call dword ptr [0x013714F0]
0040205A  pop  edi
0040205B  pop  esi
0040205C  pop  ebx
0040205D  ret  8
```

## Recovered behavior

`thiscall`-style callback-slot registration helper:

- `ECX` points to the callback slot.
- First stack argument is the owner object.
- Second stack argument is callback address.
- If either stack argument is null, it returns without registering.
- If slot flag byte `+4` already has bit `1`, it unregisters the existing callback through IAT `0x013714EC`.
- Writes owner to slot `+0x0C` and callback to slot `+0x10`.
- Registers the slot through IAT `0x013714F0` with callback id `0x44D`.
