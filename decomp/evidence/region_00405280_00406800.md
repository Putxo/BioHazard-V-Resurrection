# Sequential decomp evidence — 0x00405280..0x00406800

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

Evidence was decoded directly from the exact RE5DX9 1.1.0 Dev target. Names remain provisional where ownership is not established.

## Confirmed entry boundaries

INT3/alignment boundaries establish entries at:
`00405280`, `004052E0`, `00405390`, `004053F0`, `00405450`, `004054B0`, `00405510`, `00405570`, `004055D0`, `00405630`, `00405690`, `00405710`, `00405930`, `00405A60`, `00405BD0`, `00405DF0`, `00406010`, `004061F0`, `00406480`, `004064A0`, `004064C0`, `004064E0`, `00406510`, `00406540`, `00406570`, `004065A0`, `004065D0`, `00406600`, `00406620`, `00406640`, `00406660`, `00406680`, `004066A0`, `004066C0`, `004066E0`, `00406700`, `00406720`, `00406740`, `00406760`, `00406780`, `004067A0`, `004067C0`, `004067E0`.

## Factory family 00405280 / 00405390..00405630

These functions share a concrete allocation-and-construction shape:

- allocator owner is global `0x01641A0C`; virtual allocator slot is `+0x14`.
- requested alignment is `0x10`.
- `00405280` requests `0x1A0` bytes.
- `00405390` requests `0x1A8` bytes.
- `004053F0`, `00405450`, `004054B0`, `00405510`, `00405570`, `004055D0`, and `00405630` request `0x1A0` bytes.
- allocation failure returns null.
- successful allocation first calls `0x00D02A20` on the returned object.
- the embedded object at `self+0x158` is initialized by `0x0119C370` after temporarily installing vtable `0x0137AA9C`.
- common zero initialization includes dword `+0x04`, dword `+0x48`, byte `+0x198`, and dword `+0x19C`.
- `00405390` additionally clears dword `+0x1A0`, consistent with its larger `0x1A8` allocation.
- final vtable tokens observed sequentially are: `00405280 -> 0x0137ACB0`, `00405390 -> 0x0137ACDC`, `004053F0 -> 0x0137AD0C`, `00405450 -> 0x0137AD38`, `004054B0 -> 0x0137AD64`, `00405510 -> 0x0137AD90`, `00405570 -> 0x0137ADBC`, `004055D0 -> 0x0137ADE8`, `00405630 -> 0x0137AE14`.

The class names are not inferred from these addresses alone.

## FUN_004052E0

- `thiscall`; object is retained in `ESI`.
- state field is dword `this+0x28`.
- state 0: calls object/global path through `0x016E1FD0`, `0x00470600`, `0x0041C210`, and `0x0050B230`; on completion increments state and clears `this+0x2C`.
- state 1: waits on boolean result from `0x00404E60`; success increments state and clears `+0x2C`.
- state 2: waits on boolean result from `0x00402FC0`; success calls `0x0050B370(0)` through global `0x0165C148`, increments state, and clears `+0x2C`.
- states beyond 2 take a finalization path guarded by `0x005092D0`; when the guard is false it invokes `0x00470600`, `0x0041C5E0`, then virtual slot `+0x24` on global object `0x016E1FD0` with `0x01657F80`.

## FUN_00405690

- ends with `ret 8`, proving two callee-cleaned stack arguments.
- calls `0x00A52D90` with arg1, then `0x00A54010`.
- arg2 is treated as an array of 8-byte records and iterated until the pointer wraps to zero; each record contains a pointer/string-like field at `+0` and dword at `+4`.
- nonempty records are passed with constant `1` to `0x00D14C10` using global `0x016E1D3C`.
- when globals `0x016B1F39` and `0x016B1F3B` are both zero and the call result is non-null, its address is forwarded to `0x005477E0` using object/global `0x016B1F3C`.
- function finishes with `0x00A54120(0)`.

## FUN_00405710

- reserves `0x218` bytes of stack.
- takes a pointer argument and rejects entries whose byte at `+0x1F` has bit 0 set.
- formats local strings using the record dword at `+0x04` and literals around `0x0137AAE0`, `0x0137AAD4`, with fallback literal `0x0137AA5C`.
- tests feature bit `0x00200000` in global state rooted at `0x016E1660` (`+0x1E550`).
- obtains a virtual object from the input and resolves a related object through `0x00407C00` with token/global `0x016B1024`.
- virtual slots `+0xA0` and `+0xA4` expose an array pointer and count; records are 8 bytes each and are dispatched through the same `0x00D14C10` / `0x005477E0` path as `FUN_00405690`.
- temporarily forces byte `global(0x016E1D3C)+0x904D` to one and restores the original byte before returning.
- retry loop uses `0x00A52D90`, `0x00A54010`, `0x00A54120`, and a local counter initialized to 2.
- object cleanup is performed through virtual slot 0 with argument 1 on failure/exit paths.

## FUN_00405930

- `thiscall`, reserves `0x100` stack bytes.
- state field is `this+0x2C`; progress/index is `this+0x38`; count is `this+0x30`; allocated pointer array is `this+0x34`; current record pointer is stored at `this+0x4C`.
- state 2 calls `FUN_00405710(this+0x4C)`, increments progress, decrements state, and returns false.
- state 1 frees `this+0x34` through `0x0125145C` and returns true when progress reaches count; otherwise loads `this+0x34[progress]`, formats its `+4` field and passes the temporary to `0x00402770`, then increments state and returns false.
- state 0 begins the enumeration/setup path through `FUN_00402560` and subsequent formatting/allocation logic; semantic ownership remains unresolved.

## Remaining entries through 00406800

`00405A60`, `00405BD0`, `00405DF0`, `00406010`, `004061F0` are bounded larger routines and remain DISASSEMBLED pending full per-basic-block recovery. `00406480` and the dense aligned entries from `004064A0` through `004067E0` are retained as separate candidates; they are not collapsed into data because their boundaries are backed by executable instruction streams and alignment padding. Their semantic contracts require caller/XREF validation before source conversion.

## Acceptance rule

No entry in this evidence file is promoted to VERIFIED/MATCHED/LINKED solely from linear decoding. Factory-family source conversion is safe only after the shared allocator/constructor service contract is represented without inventing class names.