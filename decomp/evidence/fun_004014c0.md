# FUN_004014C0 evidence

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

- VA `0x004014C0`, RVA `0x000014C0`, exact size `0x105` (261 bytes); body ends at `0x004015C4` and is followed by `INT3` padding.
- RVA `0x14C0` is decimal `5312`; VA `0x004014C0` is decimal `4199616`.
- The exact target binary was rechecked against `config/target.json` and independently disassembled because the large local `work/analysis/function_candidates.csv` / `linear_disassembly.csv` artifacts are intentionally not tracked in Git. A direct `CALL 0x004014C0` is present at VA `0x00401E67`, supporting the entry boundary.
- No semantic subsystem or class name is inferred. Module/subsystem remain `UNKNOWN`.

## Primary polling block

- Preserves `EBX`, `ESI`, and `EDI`; `EDI` receives function pointer global `0x013714D8`, and `EBX` receives the sleep function pointer at `0x013711DC`.
- Calls `0x013714D8`. If it returns true in `AL`, the primary wait setup is skipped.
- If false, calls function pointer `0x013714D4` with exact token `0x0004F8A8`. A false result also skips the primary wait setup.
- When both conditions require waiting, calls helper `0x01198FE0` with `(0x01652E00, 0x0137A744)`, writes byte global `0x01655B57 = 1`, and queries `0x013714D8` again.
- If still false, the loop performs at most **five** sleeps of exactly `1000` ms. Readiness is checked once before the loop and once after every sleep, so this wait phase has at most six readiness checks after setting the flag; including the initial pre-gate query, the whole primary block can call the readiness function seven times.
- Every path reaching `0x0040151F` writes `0x01655B57 = 0`, even paths that never wrote it to one.

## Secondary polling block

- Calls function pointer global `0x013714D0`. If false, calls helper `0x01198F40` with `(0x01652E00, 0x0137A724)` and returns `false`.
- Otherwise, calls provider function pointer `0x013714CC`, then invokes the returned object's virtual method at vtable offset `+0x04`.
- If that readiness query is false, a second bounded loop performs at most **five** sleeps of exactly `1000` ms; after each sleep it reacquires the object through `0x013714CC` and calls virtual method `+0x04` again. This produces at most six readiness checks in the initial/poll stage.
- After the poll stage, it reacquires the object. If that result is null, it calls `0x01198F40` with `(0x01652E00, 0x0137A6E0)` and returns `true`.
- If non-null, it reacquires the object once more, calls virtual method `+0x04`, and if false logs `(0x01652E00, 0x0137A6FC)` through `0x01198F40`. It then returns `true`.
- The only explicit `false` return demonstrated by the machine body is the `0x013714D0` failure path; all later explicit returns set `AL = 1`.

## Recovery status

The C++ recovery preserves the exact branch topology relevant to observable calls, constants, bounded poll counts, wait-flag writes, log-message tokens, and boolean return split through injectable host services. The service layer is a reconstruction/testing boundary only; it does not assert semantic names for the original globals or virtual object.

Status: **FAST_PASS**. `FAST_PASS_VALIDATED`, `CONVERTED`, `REFINED`, `VERIFIED`, `MATCHED`, and `LINKED` remain false until their respective validation/promotion stages.
