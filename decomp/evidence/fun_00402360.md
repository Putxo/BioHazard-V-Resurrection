# FUN_00402360 evidence

- VA: `0x00402360`
- RVA: `0x00002360`
- Candidate extent: 128 bytes, ending before `0x004023E0`.
- Exact target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Recovered behavior

This function initializes a small reference-counted string holder.

- Null or empty input stores a null pointer in the destination.
- Non-empty input computes `strlen(input)`.
- It requests `length + 0x0C` bytes from the global allocator/service at `0x01652F28` through virtual slot `+0x14`, with alignment `0x10`.
- The allocated block stores reference count `1` at `+0x00`, string length at `+0x04`, and the bytes including the terminating NUL beginning at `+0x08`.
- The destination receives the allocated block pointer.

The host implementation exposes the allocator call through injectable services so tests do not depend on proprietary runtime state. The size/alignment and block fields are preserved exactly from the observed flow.
