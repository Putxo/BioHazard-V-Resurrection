# FUN_00401650 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x00401650`
- RVA: `0x00001650`
- Size: `0x15` / 21 bytes (`0x00401650` through `0x00401664`)
- Previous entry `FUN_00401640` ends at `0x00401645`; `0x00401646..0x0040164F` are `INT3` padding.
- `0x00401665..0x0040166F` are `INT3`; next clear entry begins at `0x00401670`.

## Exact observed control flow

1. `call dword ptr [0x013714C8]`
2. load the function's sole stack argument from `[esp+4]`
3. load vtable from the returned interface pointer
4. load virtual target at vtable offset `+0x54`
5. push the original argument unchanged
6. call the virtual method with the interface pointer as `ECX`
7. return with the virtual call's `EAX` unchanged

The PE import table identifies IAT slot `0x013714C8` exactly as `steam_api.dll!SteamUserStats`.

The exact Steamworks interface-version method name for vtable offset `+0x54` is not proven from the executable alone, so the recovered boundary intentionally retains the neutral name `invoke_slot_54`. Module/subsystem remain `UNKNOWN`.
