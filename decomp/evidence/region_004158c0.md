# Ordered decompilation evidence — 0x004158C0 through 0x00415BE0

## FUN_004158C0
- `thiscall`; six-state machine on owner byte `+0x05`.
- Requests root `0x800`, flag groups `0xFFE02000/0x8000`, calls `0x004A4EF0(0)`, advances.
- Waits for global byte `0x016E03CC+0x131C` clear, calls `0x004B6570`, seeds owner timer via `0x0040AC10`, advances.
- Waits for UI vtable readiness, resets owner wait byte `+0x07`; waits for `FUN_00412BF0`; then waits for asynchronous `FUN_0040C0D0` to return true; final tail-jumps to global reset `FUN_0040E410`.

## FUN_004159B0
- `thiscall`; state machine on owner byte `+0x05` up to `0x15`, with jump tables `0x00415BA4/0x00415BC4`.
- Initial state requests `0x800`, `0xFFE02000`, `0x8000`, zeros timer, UI vtable reset, calls `0x004E9990(1)`, advances, then tail-calls central state updater `FUN_0040E930` on most exits.
- Waits for global service fields `0x0165BBB0+0x39744/+0x39748`, forwards owner `+0x368` through `0x004E9920`, reacts to global byte `+0x131C`.
- Special owner code `+0x368==0x21` resets multiple global services/UI, clears root flag groups and owner state back to 2.
- Other path seeds timer, waits UI readiness, invokes `FUN_00412BF0`, and terminally sets owner state byte `+0x04=0x0B`.

## FUN_00415BE0
- `thiscall`; state machine on owner byte `+0x05` up to 8.
- Initial state calls UI `0x00471DE0`, requests root `0x800`, `0xFFE02000`, `0x8000`, flags `1/0x200`, resets timer and service/UI state, seeds owner timer via `0x0040AC10`, advances.
- Waits UI readiness and `FUN_00412BF0`; later resets global session helpers and owns object `this+0x500`, replacing it via factory `0x0082D970(0x016A0DDC)` with ownership flag `0x2000` and registration.
- Body continues to next candidate `0x00416110`.

No VERIFIED/MATCHED claim.