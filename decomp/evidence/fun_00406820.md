# FUN_00406820 evidence

## Target

- Exact local reference executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`; size 19,977,216 bytes; PE32 x86 image base `0x00400000`.
- Function VA `0x00406820`, RVA `0x00006820`.
- Native body: 27 bytes through `ret` at `0x0040683A`.
- Candidate byte SHA-256: `55029a6b9a8d816099ebcdca4651057efb7a39d33f757f470b250b78948f5a3c`.
- `0x0040683B..0x0040683F` is `INT3` padding. The next aligned root starts at `0x00406840` and is outside this recovery.

## Instruction evidence

```text
00406820  6A 00                   push 0
00406822  6A 00                   push 0
00406824  6A 01                   push 1
00406826  68 08 7C 56 01          push 0x01567C08
0040682B  68 18 C5 37 01          push 0x0137C518
00406830  6A FF                   push -1
00406832  E8 B9 F9 FF FF          call 0x004061F0
00406837  83 C4 18                add esp,0x18
0040683A  C3                      ret
0040683B..0040683F                  int3 padding
```

The native body has no conditional branch or early return. It forwards exactly:

`FUN_004061F0(-1, "Image\\Archive\\Credit2Resource", 0x01567C08, 1, 0, 0)`

and returns. `add esp,0x18` cleans the six pushed dwords; the plain `ret` and no incoming argument load establish a no-argument caller-cleaned `cdecl void(void)` wrapper.

## Referenced data

- `0x0137C518` = `Image\\Archive\\Credit2Resource`.
- Direct table `0x01567C08`, count `1`:

| # | path pointer | resource token | path |
|---:|---:|---:|---|
| 0 | `0x0137C538` | `0x01692E3C` | `id\\jpn\\credits\\credeits_dc` |

- Resolved table/count = `(0, 0)`.
- Message index = `-1`.

The spelling `credeits_dc` is preserved exactly from the reference binary; it is not corrected in reconstructed evidence.

## Caller evidence

A complete direct `CALL rel32` scan of the exact `.text` image finds exactly one direct call target equal to `0x00406820`, at `0x00509D29`.

```text
00509D17  BF 18 C5 37 01          mov edi,0x0137C518
00509D1C  8B F5                   mov esi,ebp
00509D1E  B9 1E 00 00 00          mov ecx,0x1E
00509D23  33 D2                   xor edx,edx
00509D25  F3 A6                   repz cmps byte ptr [esi],byte ptr [edi]
00509D27  75 0F                   jne 0x00509D38
00509D29  E8 F2 CA EF FF          call 0x00406820
00509D2E  5D                      pop ebp
```

The compare length `0x1E` equals the 29-byte archive name plus its terminating NUL, so this direct caller selects the wrapper only after an exact `Image\\Archive\\Credit2Resource` comparison.

## Host-only early exits and tests

The portable host implementation adds only two harness-safety exits: null services and null dispatch callback. Neither exists in the native 27-byte body. Unit tests explicitly cover both host-only exits and assert exact normal-path forwarding of message index, archive literal, direct table/count and resolved table/count.

`FUN_004067C0`, `FUN_004067E0`, and `FUN_00406800` are explicitly excluded parallel work and are not modified by this recovery. `FUN_004061F0` is used only through its already-recovered injectable dispatch contract and is not edited.
