# FUN_00406620 evidence

## Target

- Evidence was taken only from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x00406620`, RVA `0x00006620`.
- Candidate body: 27 bytes (`0x1B`), through the `ret` at `0x0040663A`.
- Candidate byte SHA-256: `75e5ba48283cb99d536f901eca4d238023fb552fe29400a109628f4b82fc299b`.
- `0x0040663B..0x0040663F` is `INT3` padding; the next aligned root begins at `0x00406640`.
- GNU `objdump` and LLVM `llvm-objdump` independently agree on the instruction boundaries and control flow below.

## Instruction evidence

```text
00406620  push 00h
00406622  push 00h
00406624  push 02h
00406626  push 015678B0h
0040662B  push 0137BE6Ch
00406630  push FFFFFFFFh
00406632  call 004061F0h
00406637  add esp,18h
0040663A  ret
```

The wrapper forwards six cdecl arguments to the already recovered `FUN_004061F0`:

1. message index `-1` (`0xFFFFFFFF`);
2. archive literal at `0x0137BE6C`;
3. direct-table token `0x015678B0`;
4. direct count `2`;
5. resolved-table token `0`;
6. resolved count `0`.

The archive literal at `0x0137BE6C` is:

```text
Image\\Archive\\Menu2Resource
```

The `add esp,18h` removes exactly six pushed dwords and the function returns with a plain `ret`, confirming a caller-cleaned cdecl no-argument wrapper.

## Table evidence

The direct table at `0x015678B0` contains exactly two 8-byte `(name pointer, resource token)` records, matching the pushed count `2`:

```text
0  id\\jpn\\menu\\top11a                 01692E3C
1  id\\jpn\\figures\\figure_viewer      01692E3C
```

The raw pointer pairs are:

```text
015678B0: 0137BE88 01692E3C
015678B8: 0137BE9C 01692E3C
```

The second table/count pair is explicitly `(0, 0)`, so this wrapper supplies no resolved-entry table. The reconstruction records the observed table address and count as opaque forwarding tokens and does not duplicate or alter `FUN_004061F0`.

## Caller evidence

A direct `CALL rel32` scan of the complete exact `.text` image finds exactly one direct call to `0x00406620`, at `0x00509B8B`.

The caller first compares the current resource name against the same `Menu2Resource` literal and invokes the wrapper only on equality:

```text
00509B79  mov edi,0137BE6Ch
00509B7E  mov esi,ebp
00509B80  mov ecx,1Ch
00509B85  xor eax,eax
00509B87  repz cmps byte ptr [esi],byte ptr [edi]
00509B89  jne 00509B9Ah
00509B8B  call 00406620h
00509B90  pop ebp
```

`0x1C` bytes is the full archive string including its terminating zero. No argument is pushed for the `call 00406620h`, which agrees with the no-argument wrapper contract.

## Early exits and reconstruction choice

The original 27-byte body has no conditional branch and no early-return path: it always forwards the six constants to `FUN_004061F0`, cleans the stack, and returns.

The portable reconstruction adds only two harness guards: a null-services guard and a null-dispatch guard. Both are unit-tested explicitly as host-only early exits and are not claimed as original binary control flow. The normal-path test verifies every observed forwarded argument.

`FUN_004065A0`, `FUN_004065D0`, and `FUN_00406600` are parallel assignments and are not modified by this recovery. `FUN_004061F0` is treated as an already recovered opaque dispatch dependency; this candidate only reconstructs the fixed forwarding wrapper at `0x00406620`.
