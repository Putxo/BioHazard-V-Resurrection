# FUN_00407B50 evidence

## Target

- Exact target executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- VA: `0x00407B50`.
- RVA: `0x00007B50`.
- Exact body size: `0x14` (20) bytes.
- Exact body SHA-256: `18c67b28b3cbd8c4c571a0d56b207d42c2f89909248c607e5f11baa529f78978`.
- The assignment's exact local V6 membership check records `31568,4225872,1` in `work/analysis/function_candidates.csv`; decimal `4225872` is `0x00407B50`. The large candidate CSV is intentionally not tracked in Git.

## Exact instruction stream

```text
00407B50  8B 81 28 10 00 00    mov eax,[ecx+1028h]
00407B56  85 C0                test eax,eax
00407B58  74 04                je 00407B5Eh
00407B5A  83 C0 08             add eax,8
00407B5D  C3                   ret
00407B5E  B8 5C AA 37 01       mov eax,0137AA5Ch
00407B63  C3                   ret
```

Exact body bytes:

```text
8B 81 28 10 00 00 85 C0 74 04 83 C0 08 C3 B8 5C AA 37 01 C3
```

## Entry and boundary evidence

The canonical region evidence records `0x00407B50` as an aligned executable entry. The supplemental analysis of `0x00407B60` independently confirms that the real function begins at `0x00407B50`, that the body above ends at `0x00407B63`, and that the next real helper begins at `0x00407B70`.

A complete direct relative control-flow scan of the exact target found many direct CALL references to `0x00407B50`. One representative caller is `0x00403923 -> CALL 0x00407B50`.

## Recovered contract

The function is a compact x86 `thiscall` read-only accessor: load the 32-bit value at `this+0x1028`; if zero return `0x0137AA5C`; otherwise return the value plus 8 with 32-bit EAX wrap semantics.

## Tests and status

Tests cover the native null early branch, normal +8 path and 32-bit ADD wraparound. Implementation PR #323 completed 5/5 CI and merged as `2d11460a43a57cfa570f68618e42417050d4bc53`; promotion PR #338 completed 5/5 CI and merged to integration as `edef5de7e6b369325d1b59ffd0bd18d980b212be`.

## Scope

- Only `FUN_00407B50` is reconstructed and published here.
- `FUN_00407A70`, `FUN_00407A90`, `FUN_00407B30`, and `FUN_00407B70` are preserved.
- This address is a real function, so no candidate exclusion is created and the V6 denominator remains `79,782`.
- No EXE, DLL, proprietary asset, `.obj`, or binary product is added.
