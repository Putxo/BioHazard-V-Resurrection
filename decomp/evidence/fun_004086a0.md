# FUN_004086A0 evidence

## Candidate membership and classification

The assigned canonical V6 row is literally:

~~~text
34464,4228768,1
~~~

This is exact row 140 of the supplied `function_candidates.csv` view.
Membership is determined solely from the exact second decimal column:
`4228768 == 0x004086A0`. The first decimal column is the RVA:
`34464 == 0x000086A0`.

`FUN_004086A0` is a real function entry. The canonical denominator remains
**79,782** and no false-positive removal is created.

## Exact target and boundaries

- Product: Resident Evil 5 PC 1.1.0 Dev.
- PE32 x86 image base: `0x00400000`.
- Exact executable size: `19,977,216` bytes.
- Exact executable SHA-256:
  `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- VA: `0x004086A0`; RVA: `0x000086A0`; section: `.text`.
- Native body: `0x004086A0..0x004086B1`, exactly **18 bytes**.
- Native-body SHA-256:
  `1433206bcafad37eaf62e0bd05e20d441cac498e33bde7fde97b5cfe143d0558`.
- `0x00408699..0x0040869F` is INT3 padding before the root.
- `0x004086B2..0x004086BF` is INT3 padding before the independent accessor
  root `0x004086C0`.
- Root-plus-padding span `0x004086A0..0x004086BF` is 32 bytes with SHA-256
  `ec5fa9e5d8dc3b62d0df9b72352705f14edb6726caaefc600b6acb298f640802`.

The private executable was materialized only into the local analysis
container, rehashed and disassembled. It is not committed or uploaded.

Exact body bytes:

~~~text
56 8b f1 e8 28 8c 66 00 c7 06 58 e4 37 01 8b c6 5e c3
~~~

## Exact instruction stream

GNU objdump and LLVM llvm-objdump independently agree:

~~~text
004086A0  56                    push esi
004086A1  8B F1                 mov esi,ecx
004086A3  E8 28 8C 66 00        call 00A712D0h
004086A8  C7 06 58 E4 37 01     mov dword ptr [esi],0137E458h
004086AE  8B C6                 mov eax,esi
004086B0  5E                    pop esi
004086B1  C3                    ret
~~~

The function has no stack arguments and ends with a plain `ret`, consistent
with a no-argument `thiscall` constructor. It preserves the incoming this
pointer in ESI, invokes base constructor `0x00A712D0`, overwrites the leading
vtable word with `0x0137E458`, returns the original this pointer in EAX, and
restores ESI.

## Exact base-constructor dependency

Because the direct dependency is present in the same exact executable, it was
also disassembled rather than modeled as an opaque host callback.

`0x00A712D0..0x00A712FD` is exactly 46 bytes, SHA-256
`11af98485b072c34d2d9e08c0509fde0ba4ec73cef34c0eeaf49b21b662f876f`:

~~~text
00A712D0  56                    push esi
00A712D1  8B F1                 mov esi,ecx
00A712D3  E8 38 6B FF FF        call 00A67E10h
00A712D8  F3 0F 10 05 FC 6B
          49 01                 movss xmm0,dword ptr [01496BFCh]
00A712E0  33 C0                 xor eax,eax
00A712E2  89 46 08              mov dword ptr [esi+08h],eax
00A712E5  89 46 0C              mov dword ptr [esi+0Ch],eax
00A712E8  C7 06 9C F6 49 01     mov dword ptr [esi],0149F69Ch
00A712EE  C7 46 10 FF FF FF FF  mov dword ptr [esi+10h],FFFFFFFFh
00A712F5  F3 0F 11 46 18        movss dword ptr [esi+18h],xmm0
00A712FA  8B C6                 mov eax,esi
00A712FC  5E                    pop esi
00A712FD  C3                    ret
~~~

Its first dependency, `0x00A67E10..0x00A67E26`, is exactly 23 bytes,
SHA-256 `c6ce0b1804803a922bc53b226b2a2c1c285e27204d827f74b18ffae88c5469a7`.
It installs vtable `0x0149F53C` and clears bytes `this+4..this+7`, i.e. the
dword at offset `+4`.

The literal dword at `0x01496BFC` is `0x447A0000`, IEEE-754
`1000.0f`. Therefore the complete observable object state after
`0x00A712D0` and before the final derived-vtable overwrite is:

- `+0x00 = 0x0149F69C`;
- `+0x04 = 0`;
- `+0x08 = 0`;
- `+0x0C = 0`;
- `+0x10 = 0xFFFFFFFF`;
- `+0x14` untouched;
- `+0x18 = 1000.0f`.

The caller allocates exactly `0x1C` bytes before invoking
`FUN_004086A0`, so the recovered portable object is exactly 0x1C bytes.
`FUN_004086A0` changes only the final `+0x00` value to `0x0137E458`.

## Direct-call evidence

A complete decoded disassembly search of the exact target finds one direct
call to `0x004086A0`:

~~~text
004088A1  push 1Ch
004088A3  call 004085D0
004088A8  add  esp,4
004088AB  test eax,eax
004088AD  je   00408897
004088AF  mov  ecx,eax
004088B1  call 004086A0
004088B6  mov  [esi+28h],eax
~~~

The caller allocates 0x1C bytes through the already-recovered allocator
wrapper, null-checks the allocation, places it in ECX, invokes this constructor
and stores the returned this pointer. A whole-file raw little-endian scan finds
zero embedded absolute data pointers to `0x004086A0`.

## Tests and guards

`tests/unit/test_fun_004086a0.cpp` covers:

- portable null-`this` guard (native code assumes valid ECX);
- exact 0x1C object layout;
- all base-constructor writes visible through `FUN_004086A0`;
- exact final vtable `0x0137E458`;
- preservation of the untouched dword at offset `+0x14`;
- exact `1000.0f` scalar at `+0x18`;
- exact this-pointer return;
- repeated construction proving there is no native conditional early exit.

The native function contains no conditional branch. The null guard is
host-only and does not claim a native early-exit path.

## Scope

Only `FUN_004086A0` is recovered here. Parallel `FUN_00408620`,
`FUN_00408630`, `FUN_00408640`, the accessor at `0x004086C0`, the
destructor `FUN_004086D0`, and all other work remain untouched.

No EXE, DLL, proprietary asset, `.obj`, or binary product is committed.
