# FUN_00405710 evidence

- VA: `0x00405710`
- RVA: `0x00005710`
- Size: `544` bytes in the ordered V6 candidate inventory, ending at next candidate `0x00405930`.
- Calling convention: stack-argument entry; the request pointer is loaded from `[esp + 0x220]` after reserving `0x218` bytes of local storage and pushing `esi`.
- The body returns with plain `ret`.

## Instruction evidence

- `0x00405710..0x00405717` reserves a `0x218`-byte local frame and loads the request pointer.
- `0x0040571E..0x00405722` checks request byte `+0x1F`, bit `0x01`; if set, it exits at `0x0040591C`.
- `0x00405728..0x00405736` formats a local path from request field `+0x04` and literal marker `0x0137AAE0`.
- `0x0040573B..0x0040576B` loads global text from `0x016E1D3C + 0x102C`, falling back to empty literal `0x0137AA5C`, and formats a second local path with marker `0x0137AAD4`.
- `0x00405770..0x004057C8` conditionally registers the first local path through `0x00D14C10` using key/base `0x016E216C`, finalizes non-null results through `0x00D25060`, then calls `0x0119B910` and exits early if that check succeeds.
- `0x004057CE..0x004057DF` calls the request object's vtable slot `+0x04`; null result exits.
- `0x004057E5..0x00405812` queries child object `0x00407C00` with global key `0x016B1024`; missing or not-ready children are released through child vtable slot `+0x00` with argument `1`.
- `0x0040581B..0x00405841` obtains an entry count from child vtable slot `+0xA4`; zero count releases the child and exits.
- `0x00405842..0x00405858` saves global byte `0x016E1D3C + 0x904D`, forces it to `1`, and initializes a two-pass local batch counter.
- `0x00405861..0x004058D6` begins a batch (`0x00A52D90` / `0x00A54010`), obtains the child entry table from vtable slot `+0xA0`, and walks `entry_count` 8-byte records.
- Each non-empty record is registered through `0x00D14C10`; when global suppressor bytes `0x016B1F39` and `0x016B1F3B` are both clear and the registration result is non-null, it binds the result through global `0x016B1F3C` call `0x005477E0`.
- `0x004058D8..0x004058F8` ends the batch with `0x00A54120`, checks the local batch path through `0x0119B910`, and repeats while the two-pass counter remains non-negative.
- `0x004058FE..0x00405915` releases the request child and restores the saved global byte.

## Host reconstruction

The recovered host function models the routine as a guarded request/child registration pass. It preserves the request skip flag, two formatted path constructions, special global registration with cancel check, child acquisition/readiness checks, two-pass batch processing, per-entry registration/bind behavior, child release, and global loading-flag save/restore behavior through explicit service callbacks.
