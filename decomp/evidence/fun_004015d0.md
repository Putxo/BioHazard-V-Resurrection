# FUN_004015D0 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x004015D0`
- RVA: `0x000015D0`
- Size: `0x68` / 104 bytes (`0x004015D0` through `0x00401637`)
- Sequential boundary evidence: previous function returns at `0x004015C4`; `0x004015C5..0x004015CF` are `INT3`; next function starts at `0x00401640` after `INT3` padding.
- Direct-call evidence: `0x00401EFC -> 0x004015D0`.

## Imported interface evidence

The indirect call at `0x004015D2` uses IAT slot `0x013714DC`. The PE import table identifies that slot exactly as `steam_api.dll!SteamUtils`.

The returned interface is invoked through vtable slot `+0x10`. Its returned pointer is compared against two-letter country strings in the static table beginning at `0x01567008`. The table contains `AU`, `AT`, `BH`, `BE`, ... `ES`, ... `US`, `UK`, and `XX`.

## Recovered behavior

- obtains the country-code string once from the SteamUtils interface;
- searches the country table beginning at table index 1;
- returns the corresponding integer value on an exact string match;
- `GB` maps to 13 and the additional `UK` alias also maps to 13;
- `US` maps to 47;
- `XX` maps to 48;
- after the `XX` sentinel, or when no preceding code matches, returns `0x30` (48).

The host-side adapter exposes the observed country string directly so the recovered mapping can be unit-tested without linking proprietary Steam/game binaries. Module/subsystem remain `UNKNOWN` because no stronger game-level ownership is demonstrated by this function alone.
