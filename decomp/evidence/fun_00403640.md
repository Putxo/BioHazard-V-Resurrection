# FUN_00403640 evidence

- VA: `0x00403640`
- RVA: `0x00003640`
- Size: `1200` bytes in the ordered candidate inventory, ending at the next baseline candidate `0x00403AF0`.
- Calling convention: `thiscall`; `ecx` is preserved as the owner object and the function returns with `ret 0x0C`.

## Instruction evidence

- `0x00403648` passes the first stack argument to an external validation/probe routine; failure exits at `0x00403AE7`.
- `0x0040365D` through `0x00403671` open a root resource from another stack argument and table/global `0x01652EF0`; null exits.
- The opened object is queried through its vtable and checked against registry/global `0x016E20DC`; if the global table count at `0x01657B8C` is zero, `FUN_00402B10` is invoked to populate it.
- The loop constructs string temporaries, reads names with fallback literal `0x0137AA5C`, and filters entries matching literals at `0x0137AB48` and `0x0137AB44`, corresponding to `"."` and `".."`.
- If an entry flag byte has bit 0 set, the function recursively calls `0x00403640`.
- Otherwise it normalizes a leaf name, checks it through `FUN_00402640`, obtains an associated value through `FUN_004026C0`, follows the value object, and binds/activates the leaf path when all checks succeed.

## Host reconstruction

The recovered host form exposes handle validation, root opening/closing, registry population, iteration, leaf-known lookup, value lookup, and final binding as `FUN_00403640_Services`. It preserves the early-exit checks, registry ensure step, dot-entry filtering, recursive descent decision, extension stripping before table lookup, and leaf bind side effect.
