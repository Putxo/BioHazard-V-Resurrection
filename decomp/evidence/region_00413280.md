# Ordered decompilation evidence — 0x00413280 through 0x00413890

## FUN_00413280
- `thiscall`; three-state submachine on byte `+0x06`.
- State 0 either advances immediately when root flag `+0x1E550 & 0x80000`, or checks service count `0x0050B910`; positive count resets global timer, otherwise requests flags `0x800` and `0xFFE02000`, optionally prepares resource from global `+0x46C`, calls `0x0050B870(this+0x60)`, chooses timer constant by `this+0x60` lookup, advances.
- State 1 waits for `0x005092D0` false and `0x004897F0` true, clears/requests root flag groups `0x800`, `0x02000000`, `0x00400000`, `0x01000000`, `0x00800000`, calls `0x00509800`, advances.
- State 2 advances outer byte `+0x05` and resets substate.

## FUN_00413490
- `thiscall`; large state machine on byte `+0x06` (0..0x14), with jump tables at `0x00413844/0x0041386C`.
- Requests root `0x10000000`, branches on global readiness predicate `0x004094E0`, configures service `0x0165BA28` from owner `+0x60`, and waits on service byte/state.
- Uses global flags `0x001F9000`, global state `+0x18`, owner `+0x68`, and ID-specific check for `this+0x60==0x11` with service value >=`0x82`.
- Coordinates session teardown/setup helpers `0x004231D0`, `0x00AC1150`, `0x00AC11A0`, `0x00473100`, `0x00475240`, timer reset, global vtable slot `+0x34`, `0x00509800`, and global steps `0x004A1E60/0x004A1970/0x00495640` using selector 7.
- Later states update UI/global object `0x0165C1A0`, clear/request `0x10000000`, handle special owner ID `0x2F`, and eventually reset owner/global state to state 2.
- Strong semantics: long service/session load-transition state machine.

Next ordered aligned candidate `0x00413890`. No VERIFIED/MATCHED claim.