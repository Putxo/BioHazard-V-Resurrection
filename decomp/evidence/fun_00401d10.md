# FUN_00401D10 evidence

## Candidate

- RVA: `0x00001D10`
- VA: `0x00401D10`
- Size: `64` bytes through alignment before next candidate `0x00401D50`.
- Candidate source: V6 ordered candidate after `FUN_00401B50`.

## Disassembly body

```asm
00401D10  push esi
00401D11  push edi
00401D12  push 0
00401D14  mov  esi, ecx
00401D16  push 0x46
00401D18  push esi
00401D19  call 0x004012F0
00401D1E  mov  edi, eax
00401D20  call dword ptr [0x013714C8]
00401D26  mov  edx, dword ptr [eax]
00401D28  mov  ecx, eax
00401D2A  mov  eax, dword ptr [edx + 0x54]
00401D2D  push 1
00401D2F  call eax
00401D31  mov  ecx, dword ptr [esi]
00401D33  push edi
00401D34  push ecx
00401D35  mov  ecx, esi
00401D37  call 0x00401B50
00401D3C  mov  eax, edi
00401D3E  pop  edi
00401D3F  pop  esi
00401D40  ret
```

## Call evidence

The direct caller loads `ECX` from global `0x01655B58` and calls this helper without stack arguments, so it is recorded as a `thiscall`-style wrapper returning the enumeration count in `EAX`.

```asm
0086D8F4  call 0x00401E30
0086D8F9  mov  ecx, dword ptr [0x01655B58]
0086D8FF  call 0x00401D10
```

## Recovered behavior

- Calls the already recovered achievement enumerator path with requested count `0x46` and unused argument `0`.
- Stores the returned count in a preserved register.
- Calls the SteamUserStats provider (`0x013714C8`) and invokes virtual slot `+0x54` with argument `1`.
- Passes the enumerated records/count into `FUN_00401B50` for logging and Steam-save counting.
- Returns the enumerated count, not the saved count.
