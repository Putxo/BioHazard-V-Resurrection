# Ordered decompilation evidence — 0x0040D540 through 0x0040D880

## FUN_0040D540
- `thiscall`; table/availability driven selector.
- Uses `this+0x64` as current ID. Special ID `0x31` initially maps to `0x32`.
- If root flag byte at `+0x1E558` has bit `0x20`, a table/jump map for IDs roughly `0x04..0x37` produces fixed mapped values including `5,6,0x1E,0x11,0x1B,0x3C`; otherwise IDs below `0x47` may map to `id+1`.
- When no direct mapping is selected, scans availability bits associated with current user (`0x0165BBB0`) starting from global cursor `+0x46C`, wrapping/terminating around `0x35`; on hit updates cursor and calls `0x004E6BD0`.
- Stores selected value in `this+0x5C` and tail-jumps to `0x00409DC0(this)`.
- Exhaustion path changes owner byte state `+0x04` to `0x0C`.

## FUN_0040D730
- `thiscall`, three stack args, `ret 0x0C`; first argument is player index byte 0..3, another is desired enable byte.
- Resolves/validates player via `0x005231E0/0x00523330`; skips if desired state already equals byte in owner allocation `this+0x348[player]`.
- For current-user record ID `0x2BC`, enabling may trigger global event `0x0048B290` with per-player code `0x78/0x79`.
- Synchronizes through object `this+0x08` using imported lock/unlock functions.
- Enable path sets owner per-player byte, calls `0x009B6D50(player,1)`, computes a mask (default `0x0F`, optionally from `0x004731D0`), calls player virtual slot `+0xCC`, and clears flag `0x800` on player child `+0x2740`.
- Disable path clears byte, calls `0x009B6D50(player,0)`, virtual slot `+0xCC` with `0x0F`, and sets child flag `0x800`.
- Strong semantics: synchronized per-player enable/visibility/participation toggle.

## FUN_0040D880
- `thiscall`; large multi-state routine keyed by owner DWORD `this+0xD8` (0..5) and excluded entirely for global modes `1/2/5` in early branches.
- Initial state waits for root flag mask `0x10000600` clear, global object `0x016922AC` vtable `+0x38` to return zero, owner `+0x368 == 0`, and command `this+0xD4 != 0`; then dispatches `0x0041B100` with `0x80` or `0x40`, increments state, clears command.
- Next stage builds owner bitmask `+0xE4` from global slot entries `+0x490/+0x494` and service `0x0052FE80` when root flag `0x80` is set.
- Iterates slot/player-specific resources starting at owner `+0xEC`, using gate `0x00AC0F30`, releasing/replacing registered objects with the recurring `0x2000` ownership flag protocol.
- Body continues beyond this evidence slice; function remains active until its post-padding boundary near `0x0040DBE0`.

No VERIFIED/MATCHED claim.