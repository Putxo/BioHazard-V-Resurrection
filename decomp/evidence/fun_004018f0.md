# FUN_004018F0 evidence

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

## Inventory

- VA `0x004018F0`, RVA `0x000018F0`, exact size `0x1A` (26 bytes).
- Candidate row: `6384,4200688,1,direct CALL target (linear sweep)`.
- Direct caller row: `4643510,8837814,5,e8353eb9ff,call,0x4018f0,6384`, i.e. caller VA `0x0086DAB6`.
- Caller context pushes one zero argument for `FUN_004018F0` and later performs caller cleanup, so this helper is recorded as `cdecl` with one byte/bool argument.

## Machine behavior

```asm
004018F0  cmp  byte ptr [esp + 4], 0
004018F5  mov  ecx, dword ptr [0x016E15C8]
004018FB  jne  0x00401902
004018FD  jmp  0x0068E280
00401902  push 1
00401904  call 0x00CF8370
00401909  ret
```

The helper reads global object pointer `0x016E15C8` into `ECX` for both branches. When the incoming flag byte is zero, control tail-jumps to `0x0068E280`; when it is nonzero, it calls `0x00CF8370` with stack argument `1` and then returns to the caller.

Recovered source models the two unknown branch targets through an injectable services table. Tests preserve branch selection, object forwarding, the true-path constant argument `1`, and tolerate missing host bindings without guessing object/class names.
