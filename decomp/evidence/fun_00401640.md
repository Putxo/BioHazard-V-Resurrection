# FUN_00401640 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x00401640`
- RVA: `0x00001640`
- Size: 6 bytes (`0x00401640` through `0x00401645`)
- Previous sequential function `FUN_004015D0` returns at `0x00401637`; `0x00401638..0x0040163F` are compiler `INT3` padding.
- `0x00401646..0x0040164F` are `INT3`; the next clear function entry begins at `0x00401650`.

## Exact body

The complete body is a single indirect tail jump:

`FF 25 E0 14 37 01` -> `jmp dword ptr [0x013714E0]`

The PE import table identifies IAT slot `0x013714E0` exactly as `steam_api.dll!SteamAPI_Shutdown`.

## Recovery

The recovered function therefore performs exactly one Steam shutdown import call through the host runtime adapter and returns no value. The adapter is intentionally inert on non-Windows CI where `steam_api.dll` is unavailable. No broader game module/subsystem ownership is inferred, so both remain `UNKNOWN`.
