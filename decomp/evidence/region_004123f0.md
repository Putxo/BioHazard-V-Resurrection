# Ordered decompilation evidence — 0x004123F0 through 0x00412920

## FUN_004123F0
- `thiscall`; three-state submachine on owner byte `+0x05`.
- State 0 resets global timing through `0x0048C8A0(...,1,float[0x0137E4D4])`, calls global vtable slot `+0x34`, advances.
- State 1 looks up token/string `0x0137E6AC` through `0x00489970`, optionally calls `0x00407250`, invokes `0x0050D640(global 0x0165C148)`, advances.
- State 2 waits for `0x005092D0` false, calls resource wrapper `FUN_0040CDE0`, advances outer state byte `+0x04`, resets substate.

## FUN_00412480
- `thiscall`; guarded against root flags `0x500`, owner byte `+0x364`, and root service state.
- Requests flags `0x800` and `0xFFE02000`, calls sibling `0x0040AE70`; if global predicate `0x004094E0` true calls reset `FUN_0040B210`.
- Dispatches by global state `+0x24` (0..5): one path forwards current-user ID through `0x0040AAE0` and updates UI; others manipulate global bytes, load token `0x0137E6C4`, call `0x0041BAE0/0x0041BE80`, and request root flag `0x100000`.
- Session/UI transition dispatcher.

## FUN_004125D0 (entry immediately after embedded jump table)
- `thiscall`; countdown/substate routine.
- Decrements owner byte `+0x06`; on zero either runs readiness `FUN_0040ED70`, clears flag groups through `FUN_0040F130/FUN_0040F1A0`, and changes owner state, or selects owner state 6 for global modes `1/2/5`.
- Alternate path requests/clears root flag `0x800`, advances owner `+0x05`, seeds countdown 8, and may call `0x00409ED0` when global state `+0x18==2`.

## FUN_004126F0
- `thiscall`; if owner selected value `+0x5C >=0`, marks `+0x50=1`, synchronously clears root flag group `0x001F9000`, then tail-jumps to `0x00409DC0`.
- If selected value negative but root flag `0x400` set, performs same flag clear and changes owner state to 5.
- Otherwise countdown `+0x06`; on expiry calls `0x004A1E60(...,5)`, resets global timer via `0x0048C8A0`, and advances substate.

## FUN_004127F0
- `thiscall`; waits for `0x004A1970(global 0x016E03CC,5)`.
- On success calls `0x00495640(...,5)`, zeros global timer, invokes global vtable slot `+0x28(0,0x20)`, `0x004744C0`, and synchronously clears root flags `0x800`, `0xFFE02000`, `0x001F9000`.
- Advances owner substate and seeds byte `+0x06=4`.

Next ordered candidate `0x00412920`. No VERIFIED/MATCHED claim.