# Ordered decompilation evidence — 0x0040E070 through 0x0040E410

## FUN_0040E070
- `thiscall`, two stack args, `ret 0x08` when already active.
- If byte `this+0x104` is clear, sets it, stores args to `this+0x44/+0x48`, and tail-jumps to `0x0041B150(global_root,0x400000,1)`.
- One-shot request setup.

## FUN_0040E0B0
- `thiscall`, bool return; global transition/predicate routine.
- For non-mode-1 paths, inspects root flag `0x20000000` and iterates up to four player objects through `0x005231E0/0x00523300`, testing `0x009C7080` or `0x009C7030` depending on branch.
- Can set/clear root flags `0x20000000`, `0x400000`, `0x08000000` under synchronization and zero owner float `+0x12C`.
- One branch calls `0x00A60AB0` with global address derived from `0x016E201C+0x167850` and token `0x0137E548`.
- If root `0x08000000` is active, changes owner state byte `+0x04` to 9 (unless already 9/10), calls `0x00495640` with selectors 6 and 10, then sends `0x004A0100(global 0x016E03CC,5,0)` and returns true; otherwise false.
- Global/player readiness transition predicate.

## FUN_0040E340
- `thiscall`; synchronously clears root flag `0x40000`.
- Modes `3/4` simply clear owner byte `+0x128` and return.
- Otherwise, when owner byte `+0x128` is clear, examines current-user record ID `+0x273D8` (notably `0x64,0x6A,0x6D,0x6F,0x73,0x262`) and companion `+0x273DC`; for selected conditions calls `0x0041B100(global_root,0x40000)`.
- Conditional re-request based on current title/session ID.

## FUN_0040E410
- Begins broad reset/teardown sequence.
- Clears global byte `+0x56C`, global DWORD `+0x2C`, and invokes per-user structure reset `0x00B7A310`.
- Branches on root flag `+0x1E4F4 & 1`; initializes global state fields `+0x18=0,+0x04=2,+0x08=-1` and calls one of several `0x0165B6E0` service routines.
- Continues with helpers `0x00AC1260`, `0x004A5020`, `0x00AC1150`, `0x00AC11A0`, and multiple global-service reset calls. Body extends beyond this evidence slice.

Next work remains within `FUN_0040E410`, followed by aligned candidate `0x0040E650`. No VERIFIED/MATCHED claim.