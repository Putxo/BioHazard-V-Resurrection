# FUN_00402FC0 evidence

- VA: `0x00402FC0`
- RVA: `0x00002FC0`
- Size: `400` bytes in the ordered candidate inventory, ending before `0x00403150`.
- Calling convention: `thiscall`; `ecx` is the state object.

## Instruction evidence

- The state word at `this + 0x2C` is bounded to values `0..3` and dispatches through the jump table at `0x0040313C`.
- State `0` formats `this + 0x54` from the global `0x016E1D3C + 0x1028` active name, probes the transition object at `this + 0x158`, returns true if the probe fails, and otherwise advances to state `1`.
- State `1` reads the pending name pointer at `this + 0x168`; null falls back to the empty literal. Names beginning with `'.'` or objects whose byte at `this + 0x170` has bit 0 set move directly to state `3`.
- The normal state `1` path formats `this + 0x54`, builds a stack-local label with literal `0x0137AB0C`, submits it through `FUN_00402770`, advances state, and returns false.
- State `2` reports the active `0x102C` name plus `this + 0x54`, resolves/creates the registry entry at `0x016E216C`, optionally activates the result, then advances state.
- State `3` probes the transition object at `this + 0x158` with argument `0`; a non-ready result returns true, while ready resets the state to `1` and returns false.

## Host reconstruction

The recovered host form preserves the four-state control flow, state transitions, boolean return contract, pending-name filtering, label submission, registry resolution, and activation side effect. External engine objects are exposed as `FUN_00402FC0_Services` hooks so no engine implementation or proprietary binary payload is embedded.
