# FUN_00402B10 evidence

- VA: `0x00402B10`
- RVA: `0x00002B10`
- Candidate extent used by the frozen ordered inventory: 736 bytes, ending immediately before `0x00402DF0`.
- Exact target: Resident Evil 5 PC 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Recovered behavior

The function walks a sibling/child node tree. It first compares each node key at offset `+0x04` with a constructed reset string based on literal `0x0137AAC8`; matching that key clears the global registration count at `0x01657B8C`. Non-matching paths stop when that count reaches `0x200`.

The function recurses through child offset `+0x0C`, skips nodes whose flag byte at `+0x1F` has bit 0 set, checks a resolved payload object, and then fills the parallel global tables rooted at `0x01657380`, `0x01656B80`, `0x01656380`, and `0x01655B80`. Accepted entries increment the global count and traversal continues through sibling offset `+0x08`.

The host recovery models the observed reset, capacity, recursive traversal, skip flag, and table-registration behavior through an injected resolver/record service.
