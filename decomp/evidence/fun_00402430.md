# FUN_00402430 evidence

- VA: `0x00402430`
- RVA: `0x00002430`
- Exact extent: 80 bytes through `0x0040247F`; executable body ends with `ret 4` at `0x0040247C`, followed by one `INT3`; next entry starts at `0x00402480`.
- Exact target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Recovered behavior

This is a replacement/copy helper for the reference-counted string holder used by `FUN_00402360`.

1. If the destination currently owns a block, decrement its reference count.
2. If the count becomes zero, release that block through global allocator `0x01652F28`, virtual slot `+0x18`.
3. Read the source holder passed as the sole stack argument.
4. If the source block is non-null, use its character data at block offset `+0x08`; otherwise use fixed fallback string `0x0137AA5C`.
5. Invoke `FUN_00402360` on the destination with that C string, reconstructing a fresh block.
6. Return the destination (`this`) pointer.

The implementation composes the already recovered `FUN_004023E0`, `FUN_00402420`, and `FUN_00402360` contracts. The fallback string itself remains injected host-side and is not copied from proprietary target data.
