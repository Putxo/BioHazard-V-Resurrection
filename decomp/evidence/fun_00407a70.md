# FUN_00407A70 evidence

## Target and candidate membership

- Exact target identity inherited from the canonical inventory freeze: SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`, size 19,977,216 bytes, PE32 x86 image base `0x00400000`.
- Function VA `0x00407A70`, RVA `0x00007A70`.
- The canonical local V6 candidate-membership check for this task is the literal `function_candidates.csv` row `31344,4225648,1`. Its second-column decimal value `4225648` is exactly `0x00407A70`, so this address is a member of the 79,782-row V6 universe.
- Existing exact-target regional evidence in commit `a638bd04612355ec37817c3f4850b31c2536d238` records `0x00407A70` among the confirmed aligned entries and identifies it as a compact thiscall size/count helper.
- Independently, `decomp/evidence/fun_00407a30.md` proves that the preceding real function ends with `ret` at `0x00407A68`, followed by INT3 padding through `0x00407A6F`, and names `0x00407A70` as the next independently aligned root.
- The next confirmed aligned root is `0x00407A90`, giving this candidate a canonical 32-byte span `0x00407A70..0x00407A8F`. No behavior of `FUN_00407A90` is reconstructed or modified here.

The repository evidence currently preserves the exact decoded semantics and entry-boundary evidence for this helper, but not a function-specific raw byte dump/body hash. This recovery therefore does not fabricate an instruction-byte string or hash.

## Recovered contract

The binary-derived regional evidence establishes the following exact behavior:

- read dword `this+0x04` as the begin value;
- if begin is zero, return `0` immediately;
- otherwise subtract begin from dword `this+0x08`;
- arithmetic-shift the signed 32-bit delta right by two and return it.

This is structurally a count of four-byte elements. No concrete STL/container or game class name is inferred.

The portable representation stores the observed fields as `std::uint32_t` rather than host pointers. That preserves target-width subtraction, including 32-bit wraparound, independently of whether the test host is 32-bit or 64-bit. The explicit arithmetic-shift helper preserves x86 `SAR` semantics without relying on implementation-defined right shift of a negative C++ signed integer.

## Tests

Unit tests cover:

- the native null-begin early return, including an arbitrary nonzero end value;
- a normal positive three-element range;
- a non-null empty range;
- negative aligned and non-multiple deltas, proving arithmetic-shift behavior;
- 32-bit subtraction wraparound;
- the fact that dword `this+0x00` is not observed.

No host-only callback/service layer is required because this helper has no external dependency.

## Inventory accounting

`FUN_00407A70` is a real function entry and is already a verified member of the canonical raw V6 candidate universe. Its recovery therefore **does not change the denominator**, which remains `79,782`.

## Scope

This change is exclusive to `FUN_00407A70`. It does not implement, edit, claim, or classify `FUN_00407A90`, `FUN_00407B30`, or `FUN_00407B50`.

No EXE, DLL, proprietary asset, or `.obj` is committed.
