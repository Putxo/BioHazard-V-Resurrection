# Ordered decompilation evidence — 0x00413890 through 0x00413D40

## FUN_00413890
- `thiscall`; five-state submachine on owner byte `+0x06`.
- Initial state requests root flags `0x40000000/0x20000000`, clears `0x80000/0x10000`, resolves preferred player from current-user `+0x273C4` or scans 0..3 for a valid player with WORD `+0x13F0 <=0`.
- Stores owner status `+0x120/+0x124` from selected player; calls global service `0x00473320(1)` and dispatches event `0x97`.
- Iterates active player mask `+0x478`, calling mode-dependent global operations (`0x004E83D0` or `0x0041BC60`).
- Replaces owned object `this+0x11C` via factory `0x00847BA0(0x016A0FF8)`, applies ownership flag `0x2000`, configures object fields `+0x184/+0x188`, registers type 1 through `0x00D05520`.
- Later states wait for object bytes `+0x190/+0x191`, interact with service `0x0165BA28`, global vtable/UI, dispatch code `0x0C`, then restore root flag groups and force owner state `+0x04=9,+0x05=10`.

## FUN_00413C60
- `thiscall`, bool return.
- If root low flags `&3` and owner ID `+0x64==0x10`, forces owner state 9 and global selectors 6/10, returning true.
- Otherwise searches triplet WORD table `0x01568940` for owner ID. On match extracts two companion WORDs; if one-shot byte `+0x104` clear, stores them to owner `+0x44/+0x48`, sets one-shot flag and requests root `0x400000`.
- Then clears one-shot flag, ensures owner state byte `+0x04=7`, returns true; no table match returns false.

## FUN_00413D40
- `thiscall`, one boolean/byte arg; very large global feature toggle.
- Compares desired value with owner byte `+0x138`; under owner lock, enable path requests root flag `0x400`, enables UI/system services (`0x0051B5F0`, `0x0052E160`, `0x0047DD00`, `0x0047AA80`), sets global byte `0x0165BC60+0x90=1`.
- Iterates global collection `0x0165BC60`, casts entries through type token `0x016AF0A4`, filters object state and descriptor code `0x10A00`, and calls `0x009FC430(obj,1)` for eligible objects.
- Disable counterpart and remaining cleanup extend beyond this slice to a later boundary.

Next ordered candidate after completing this body is beyond `0x00414400`. No VERIFIED/MATCHED claim.