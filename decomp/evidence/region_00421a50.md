# Ordered decompilation evidence — 0x00421A50 through 0x00421CC0

- `0x00421A50`: synchronized reset for owner object `+0x288`: clears selected entry via `0x00D9B1A0(...,0)`, calls `0x00D97B60(...,1)`, re-enables byte `+0x07` on cached registry object `+0x28C`, unlocks.
- `0x00421AA0`: iterates child objects of owner `+0x288`. For ordinary children writes caller byte to child `+0x07`. For descriptor type `0x016E3E3C`, further distinguishes subtype `0x016E3E18`; otherwise builds a five-slot/root update mask and synchronously writes root per-player fields `0x016E2044 + 0x34 + slot*0x5E0` according to caller bitmask. This is a propagated visibility/enable toggle with subtype-specific global updates.
- `0x00421BD0`: command/string-to-handle mapper. Strips prefix from caller string, compares against two accepted literals (`0x0137EAD8`, `0x0137F2BC`); unknown string logs `0x0137F278`. For recognized kind, calls global handle allocator/service `0x00E264A0`, stores returned handle into owner `+0x2E8[kind]`, then invokes `FUN_0041E9A0(kind,1)`.
- `0x00421CC0`: very large parser/loader with 0x514-byte stack buffer. Builds strings from caller record, computes/normalizes path/token through `0x011BEC40`/`0x0068E280`, tests markers (`0x0137F2E8`, `0x0137F2E0`, etc.) using `0x00B8C8D0`, manages ref-counted strings and allocates/loads resources according to recognized format. Body continues beyond `0x00422200`.

Ordered decompilation has now advanced from the previous `0x0040B000` point through the start of `0x00421CC0`, with evidence committed for every analyzed region and no false VERIFIED/MATCHED promotion.