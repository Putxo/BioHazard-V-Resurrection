# Ordered decompilation evidence — 0x0040B970 through 0x0040BE40

## FUN_0040B970
- `thiscall`, no stack args, bool return.
- Rejects global modes `0/3/4`.
- Requires object from `0x016E1FD0 -> 0x004706A0` to have byte `+0x20 != 0` and byte `+0x04 == 2`.
- If global `+0x574 == +0x578`, returns whether `this+0x4D4 == 0`; otherwise returns whether global `+0x678 == 1`.
- Predicate for whether the surrounding timed/state sequence is currently eligible.

## FUN_0040B9E0
- `thiscall`, one stack boolean/byte argument, `ret 0x04`.
- Rejects global modes `0/3/4`.
- Reads global timer `+0x67C` and owner phase `this+0x4D0`.
- Phase 0 transitions to phase 1 when timer reaches threshold `0x01569F38`, invokes `FUN_0040B4E0`, then dispatches code `0x6C`.
- Phase 1 transitions either back to 0 when timer rises above threshold or to phase 2 when caller flag is nonzero; both dispatch code `0x70`.
- Dispatch uses global `0x0165A1F0`, a value from `0x016E1FD0 -> +0x3830 -> +0x574`, selected code, and self through `0x004B6B60`.
- Small phase machine coupled to the global timer.

## FUN_0040BAA0
- `stdcall`, slot index, `ret 0x04`.
- Uses `slot << 6` into global per-slot state.
- Promotes current `+0x698` into maximum/history `+0x69C` when larger, then zeros `+0x698`.
- Accumulates `+0x6B0` into `+0x6B4`, then zeros `+0x6B0`.
- Clears byte `+0x694`.
- Per-slot accounting/rollover routine.

## FUN_0040BB30
- `stdcall`, `(slot, delta_float)`, `ret 0x08`.
- If per-slot active byte `+0x6A4` is set, decrements timer `+0x6A8` by delta and clamps to zero.
- On expiry, when global `+0x574 == +0x578`, clears active byte and dispatches `0x1F` with the slot through `0x004A0100(global 0x016E03CC, ...)`.
- Per-slot countdown/expiry event.

## FUN_0040BBC0
- One stack slot argument; `ret 0x04`; complex allocation/selection routine.
- Gate `0x00AC1210(global_state)` must pass before querying slot/player object through `0x005231E0` and validating it with `0x00523330`.
- Uses object byte `+0x2ECC` to access global per-player data rooted at `+0x658`.
- Stores per-slot selection data in the `+0x6C4` family.
- Final selection uses random source `0x0119F610`, scans an 8-bit occupancy mask at global `+0x650`, chooses a free bit/index, sets that bit, and stores the chosen index into a slot entry (`(slot+0x1B)<<6`).
- Conservative interpretation: allocate/assign an unused 0..7 channel/index to a slot, with player validation and associated resource bookkeeping.

## FUN_0040BD30
- `stdcall`, slot argument.
- Requires `0x00AC1210` gate, obtains and validates the slot/player object through `0x005231E0`/`0x00523330`.
- Reads previously assigned per-slot value `+0x6C4`; if nonnegative, combines it with player-indexed global value `+0x658` and calls `0x004EEE40(global 0x0165BC60, slot, assigned, player_value)`.
- Consumer/commit path for assignment created by `FUN_0040BBC0`.

## FUN_0040BDB0
- `stdcall`, one player/slot argument.
- Obtains and validates object through `0x005231E0`/`0x00523330`.
- Iterates nine entries rooted at object `+0x22A0`, fetched through `0x00ACA7A0`.
- For each non-null entry, compares entry DWORD `+0x04` against 21 WORD identifiers in table `0x01568872`.
- When matched, either global gate `0x00AC1210` is true or companion WORD at `0x01568870` must be nonzero; then copies entry `+0x0C` into `+0x08`.
- Conservative interpretation: synchronize/activate selected values for a whitelist of entry IDs.

## FUN_0040BE40
- No stack args; operates on global state and then calls `FUN_0040B9E0(1)`.
- Rejects modes `0/3/4`.
- For each of four bits in global byte `+0x478` (`1,2,4,8`), if corresponding object pointer/value `+0x630..+0x63C` is nonzero and corresponding state `+0x620..+0x62C == 2`, changes that state to `1`.
- Then advances the owner/global phase machine via `FUN_0040B9E0(true)`.

Next ordered candidate after this body is `0x0040BF20`. No VERIFIED/MATCHED claim.