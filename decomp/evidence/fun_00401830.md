# FUN_00401830 evidence

Target: RE5DX9 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Boundary

- entry VA: `0x00401830`
- RVA: `0x00001830`
- exact body ends at `0x0040185E` (`ret`)
- exact size: `0x2F` = 47 bytes
- previous function `FUN_004017B0` ends at `0x0040182F`
- `0x0040185F` is `INT3`
- next clear entry starts at `0x00401860`

## Stack contract

After saving ESI and EDI, the function reads:

- `[esp+0x0C]` -> original first stack argument
- `[esp+0x10]` -> original second stack argument
- `[esp+0x14]` -> original third stack argument

The third argument controls the byte loop count. No direct CALL/JMP reference to `0x00401830` was found in the exact binary, so no higher-level caller semantics are asserted.

## Recovered behavior

If `length == 0`, AL is set to 1 and the function returns immediately. Otherwise it compares the two input byte ranges in order. It returns false on the first mismatching pair; if all `length` bytes match, it returns true.

This is represented as a generic three-argument byte-range equality helper. `module` and `subsystem` remain `UNKNOWN` because the binary evidence does not justify a more specific classification.
