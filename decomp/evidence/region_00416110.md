# Ordered decompilation evidence — 0x00416110 through 0x004165B0

## FUN_00416110 / FUN_00416160
- `thiscall`, one integer arg, `ret 0x04`; disabled for global modes `1/2/5`.
- Clamp owner values `+0x420` and synchronized `+0x42C` respectively into `[0, max(+0x424/+0x430)]`.

## FUN_004161D0
- `thiscall`, `(index,value)`, `ret 0x08`; disabled modes `1/2`.
- Indexes owner record `+0x36C + index*0x2C`, writes/clamps field `+0x04` into `[0,+0x08]`.

## FUN_00416220
- `thiscall`, `(index,value)`, `ret 0x08`; disabled modes `1/2`, index 0..3.
- Normalizes value around cap `0x1770` depending on predicates `0x00AC1240/0x004094A0`.
- Under owner lock updates two per-index helper objects at `+0x384` and `+0x378` through `0x0041BF30`; second receives an integer ratio derived from value.

## FUN_004162E0
- `thiscall`, three stack args, `ret 0x0C`; disabled modes `1/2/5` and selected current-user IDs.
- Under owner lock adjusts per-index owner record `+0x384 + index*0x2C` by signed delta, clamps `[0,max]`, and accumulates same delta into owner `+0x434`.

## FUN_00416390
- `thiscall`, two args, `ret 0x08`; disabled modes `1/2/5` and selected IDs.
- Validates player index, computes table index from current-user `+0x273C8`, per-player owner state and external float table `0x015684C0`; chooses coefficient table `0x01568688` or `0x01568738`, performs float transformation and integer conversion.
- Updates per-index helper at `+0x384` via `0x0041BF60`, accumulates result into owner `+0x434`.

## FUN_004164C0 / FUN_00416530
- Two wrapper routines, each two stack args, disabled modes `1/2/5`.
- Resolve an event/object hierarchy to a player object/slot using type tokens `0x016B1024` and `0x016AF0F4`; then forward `(resolved_slot, caller_value)` to `FUN_00416390`.
- `FUN_004164C0` starts from an object with `+0xFFC` state; `FUN_00416530` starts through `0x00AC8310`.

## FUN_004165B0
- Begins another player-index update path; continuation handled next.

No VERIFIED/MATCHED claim.