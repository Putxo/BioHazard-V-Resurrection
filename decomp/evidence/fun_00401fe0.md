# FUN_00401FE0 evidence

## Candidate

- RVA: `0x00001FE0`
- VA: `0x00401FE0`
- Size: `32` bytes through alignment before next candidate `0x00402000`.
- Candidate source: V6 ordered candidate after `FUN_00401E60`.

## Disassembly body

```asm
00401FE0  mov ecx, dword ptr [esp + 8]
00401FE4  mov edx, dword ptr [esp + 4]
00401FE8  lea eax, [esp + 0x0C]
00401FEC  push eax
00401FED  push ecx
00401FEE  push 0x800
00401FF3  push edx
00401FF4  call 0x012517C4
00401FF9  add  esp, 0x10
00401FFC  ret
```

## Recovered behavior

This is a cdecl variadic formatting wrapper:

- argument 1: destination buffer;
- argument 2: format string;
- remaining stack bytes: variadic argument list;
- fixed buffer limit: `0x800`;
- return value: return value of the underlying formatter.

`FUN_00401B50` uses this helper to build the header and per-award log lines before passing them to the log sink.
