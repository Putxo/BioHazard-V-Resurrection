# FUN_00401730 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x00401730`
- RVA: `0x00001730`
- Size: `0x75` / 117 bytes (`0x00401730` through `0x004017A4`)
- Boundary evidence: previous function returns at `0x0040172B`; `0x0040172C..0x0040172F` are `INT3`; this function returns at `0x004017A4`; `0x004017A5..0x004017AF` are `INT3` before the inverse routine at `0x004017B0`.
- Direct caller evidence: `0x005091F4 -> 0x00401730`.

## Four-argument contract

The caller at `0x005091F4` pushes, in cdecl right-to-left order: source length, source pointer, the same length as destination capacity, and destination pointer. Inside the function these resolve as:

1. `uint8_t* destination`
2. `uint32_t destination_size`
3. `const uint8_t* source`
4. `uint32_t length`

The routine returns the count processed in EAX. The caller tests that value against zero immediately after return.

## Seed and transformation

After rejecting `destination_size < length`, the function copies the two DWORD globals at `0x01655B48` and `0x01655B4C` into an 8-byte local feedback state. The target image initializes these bytes to zero, but they are writable `.data` globals and therefore are represented through an injectable host seed provider rather than assumed constant.

For each source byte, cycling lanes 0..7, the exact transformation is:

`feedback[lane] = feedback[lane] XOR source[i]`

`destination[i] = feedback[lane]`

Thus each produced ciphertext byte becomes the feedback byte reused when that lane is visited again eight bytes later. A zero-length input still performs the observed 8-byte global seed read and then returns zero. If destination capacity is smaller than length, the function returns zero before reading the seed.

The contiguous function at `0x004017B0` performs the complementary feedback operation, which independently supports interpreting `FUN_00401730` as the encode/encrypt direction. No stronger algorithm family/name is asserted.

Module/subsystem remain `UNKNOWN`.
