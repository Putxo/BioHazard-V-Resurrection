# FUN_00401880 evidence

Target: RE5DX9 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Boundary

- entry: `0x00401880`
- end: `0x004018BF` (`ret`)
- exact size: 64 bytes
- `0x00401872..0x0040187F` are compiler `INT3` padding
- next clear entry: `0x004018C0`

## Control flow

The function calls IAT `0x013714E4`, verified as `steam_api.dll!SteamFriends`, then calls vtable slot `+0x00` on the returned interface. The slot result is treated as a `const char*`.

Two stack arguments are recovered from the body and direct callers:

1. `char* destination`
2. `uint32_t capacity`

The function writes `destination[0] = 0`, copies at most `capacity - 1` bytes from the returned string through `0x01251550`, then always writes `destination[capacity - 1] = 0`. If the slot returns null, it still writes both observed terminators and returns with `EAX == 0`.

`0x01251550` has the observed `strncpy` contract: destination/source/count arguments, NUL-aware bounded copying/zero fill, and returns the destination pointer in `EAX`. Consequently the wrapper returns `destination` on the non-null-string path and null on the null-string path.

The original has no capacity-zero guard; all confirmed callers pass a positive capacity. The recovered host implementation adds a safety guard for invalid host-side service/buffer inputs without claiming that guard exists in the target binary.

## Direct callers

- `0x004E6EB9`: passes a 32-byte local buffer and capacity `0x20`; return value is ignored.
- `0x0052F847`: passes a local buffer and capacity `0x20`; return value is ignored.

The exact historical Steamworks method name for `ISteamFriends` slot `+0x00` is deliberately not required for the fast-pass claim. Module/subsystem remain `UNKNOWN`.
