# Ordered decompilation evidence — 0x00415330 through 0x00415720

## FUN_00415330
- `thiscall`; four-state submachine.
- Requests root flags `0x200`, `0x800`, `0xFFE02000`, `0x8000`; sets UI vtable value, dispatches global event code 0, advances.
- Waits for UI readiness, calls `0x0050F2E0`, resets owner wait byte `+0x07`; next waits for `FUN_00412BF0()`.
- Final state sends service selectors `3,6,4,5` through `0x0050B230`, advances outer byte `+0x05`, resets substate.

## FUN_00415460 (entry after jump table)
- `thiscall`; three-state transition.
- State 0 resets global session helpers, enables `FUN_0040F020(true)`, resets UI vtable, releases/replaces owner object `+0x4F8` via factory `0x00867FB0(0x016A11B4)`, flags/registers type 1, calls `0x004E5180` and `0x00509800`.
- Dispatches mode-specific event codes: `0xA3` mode 1, `0xA1` mode 2, `0xD2` mode 5.
- State 1 waits for owned object readiness byte `+0x38`, sets UI vtable value and dispatches code 0.
- State 2 waits for global UI readiness, releases object, sends service selector 1, advances outer state and resets substate.

## FUN_00415640
- Exact tail jump to `FUN_0040E410`.

## FUN_00415650
- `thiscall`; unless owner state already 10, synchronously sets root flags `0x4000` and `0x800`, requests flag groups `0xFFE02000` and `0x8000`, then forces owner state byte `+0x04=0x0A`.

## FUN_00415720
- `thiscall`; larger state machine on owner byte `+0x05` up to `0x0C`.
- Initial branch examines global mode and may call `0x004A6950`; scans four global handles `+0x490..+0x49C` through `0x0052FE80` to determine readiness, with special handling for modes `1/2/5` and global state `+0x678`.
- Body continues to next candidate `0x004158C0`.

No VERIFIED/MATCHED claim.