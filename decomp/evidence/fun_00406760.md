# FUN_00406760 evidence

## Target

- Evidence was taken only from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x00406760`, RVA `0x00006760`.
- Candidate body: 27 bytes (`0x1B`), through the `ret` at `0x0040677A`.
- Candidate byte SHA-256: `589234c2779990b5edaeecaab53c820820228530258386b018b9706a111c8cbd`.
- `0x0040677B..0x0040677F` is `INT3` padding; the next aligned root begins at `0x00406780`.

## Instruction evidence

```text
00406760  push 00h
00406762  push 00h
00406764  push 02h
00406766  push 01567B20h
0040676B  push 0137C324h
00406770  push FFFFFFFFh
00406772  call 004061F0h
00406777  add esp,18h
0040677A  ret
```

The wrapper forwards six cdecl arguments to the separately recovered `FUN_004061F0`:

1. message index `-1` (`0xFFFFFFFF`);
2. archive literal at `0x0137C324`;
3. direct-table token `0x01567B20`;
4. direct count `2`;
5. resolved-table token `0`;
6. resolved count `0`.

The archive literal at `0x0137C324` is `Image\Archive\Extra2End`.

The `add esp,18h` removes exactly six pushed dwords and the function returns with a plain `ret`, confirming a caller-cleaned cdecl no-argument wrapper.

## Table evidence

The direct table at `0x01567B20` contains exactly two 8-byte `(name pointer, resource token)` records, matching the pushed count `2`:

```text
0  id\jpn\chapter\cap_end01  016E2758
1  id\jpn\extra\result02     01692E3C
```

The raw pointer pairs are:

```text
01567B20: 0137C2F0 016E2758
01567B28: 0137C33C 01692E3C
```

Two records occupy `0x10` bytes, so the table ends exactly at `0x01567B30`, the direct-table address pushed by the adjacent aligned wrapper at `0x00406780`. The second table/count pair is explicitly `(0, 0)`, so this wrapper supplies no resolved-entry table. The reconstruction preserves both resource tokens as opaque original addresses and does not interpret or modify the downstream `FUN_004061F0` dependency.

## Caller evidence

A direct `CALL rel32` scan of the complete exact `.text` image finds exactly one direct call to `0x00406760`, at `0x00509CA5`.

The caller compares the current resource name against the same `Extra2End` literal and invokes the wrapper only on equality:

```text
00509C93  mov edi,0137C324h
00509C98  mov esi,ebp
00509C9A  mov ecx,18h
00509C9F  xor edx,edx
00509CA1  repz cmps byte ptr [esi],byte ptr [edi]
00509CA3  jne 00509CB4h
00509CA5  call 00406760h
00509CAA  pop ebp
```

`0x18` bytes is the full archive string including its terminating zero. No argument is pushed for `call 00406760h`, agreeing with the no-argument wrapper contract.

## Early exits and reconstruction choice

The original 27-byte body has no conditional branch and no early-return path: it always forwards the six constants to `FUN_004061F0`, cleans the stack, and returns.

The portable reconstruction adds only two harness guards: a null-services guard and a null-dispatch guard. Both are unit-tested explicitly as host-only early exits and are not claimed as original binary control flow. The normal-path test verifies every observed forwarded argument.

This recovery is limited to `FUN_00406760`; previously recovered neighboring wrappers are not modified. `FUN_004061F0` is treated only as an existing opaque dispatch dependency.
