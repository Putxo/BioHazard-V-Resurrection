# Ordered decompilation evidence — 0x0040B210 through 0x0040B4A0

## FUN_0040B210

- `thiscall`, no stack arguments, plain `ret` at `0x0040B3C7`.
- Resets a large state block reached through global root `0x016E1660 -> +0x1E4EC`.
- Clears/reset fields `+0x678`, `+0x67C` (copied from `+0x680`), `+0x684`, `+0x688`, `+0x68C`.
- Four iterations are performed with byte displacement stride `0x40`. Each iteration clears the per-slot fields rooted at `+0x690`, `+0x694`, `+0x698`, `+0x69C`, `+0x6A0`, `+0x6A4`, `+0x6A8`, `+0x6B0`, `+0x6B4`, `+0x6C8`, `+0x6CC` and calls `0x00AC1110` with `(slot_index, 0)`.
- In parallel it clears four DWORDs at `this+0x4C0`.
- Final owner fields: `this+0x4D0 = 0`, `this+0x4BC = [0x01657FA8]`, `this+0x4DC = global_state+0x680`, `this+0x4D4 = 0`, `this+0x4E0 = 0.0f`.
- Conservative interpretation: resets a four-slot runtime/player state aggregate and synchronizes timing/default fields.

## FUN_0040B3D0

- `thiscall`; preserves ECX argument/object in ESI.
- Reads mode/state from `0x016E1660 -> +0x1E4EC -> +0x58`.
- State `1`: calls `0x00AC11F0`; dispatches code `0x78` when true or `0x79` when false through `0x004B6EC0(global 0x0165A1F0, code, self)`.
- State `2`: dispatches code `0x77`.
- State `5`: dispatches code `0xD7`.
- Other states return without dispatch.
- This is a mode-dependent event/message selector.

## FUN_0040B440

- `thiscall`, no stack args.
- If `this+0x4F4` is non-null, clears flag `0x2000` in object DWORD `+0x04`, invokes virtual slot `+0x30`, then nulls `this+0x4F4`.
- Acquires/replaces the object via `0x00864090` using fixed context/token `0x016A1190`.
- If acquisition succeeds, sets flag `0x2000` and registers/submits it through `0x00D05520(global 0x016E1690, 0x1B, object)`.
- Conservative interpretation: replace/re-register owned runtime object/resource.

## FUN_0040B4A0

- `stdcall`, one stack argument, `ret 0x04`.
- Loads float constant at `0x0137E510` and passes it together with `-1`, `0`, caller argument and fixed values into `0x005122D0` on global service `0x0165C170`.
- Selects value `0x0E` when global state `+0x58 == 5`, otherwise `0x0D`; also passes fixed selector `0x0C`.
- Conservative interpretation: mode-dependent request/effect dispatch with one caller-supplied parameter.

Next ordered aligned post-padding entry: `0x0040B4E0`. No VERIFIED/MATCHED claim is made.