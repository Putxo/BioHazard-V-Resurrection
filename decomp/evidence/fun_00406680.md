# FUN_00406680 evidence

## Target

- Evidence was decoded directly from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x00406680`, RVA `0x00006680`.
- Candidate body: 27 bytes (`0x1B`), through the `ret` at `0x0040669A`.
- Candidate byte SHA-256: `a9b43b576ff7623be9cc3c4b58af8b1338c21fae78095ee02e1fcd96d59a7b28`.
- `0x0040669B..0x0040669F` is `INT3` padding. The next separately distinct candidate begins at `0x004066A0` and is not part of this recovery.

## Instruction evidence

```text
00406680  6A 00                   push 0
00406682  6A 00                   push 0
00406684  6A 03                   push 3
00406686  68 28 79 56 01          push 0x01567928
0040668B  68 30 BF 37 01          push 0x0137BF30
00406690  6A FF                   push -1
00406692  E8 59 FB FF FF          call 0x004061F0
00406697  83 C4 18                add esp,0x18
0040669A  C3                      ret
0040669B  CC                      int3
0040669C  CC                      int3
0040669D  CC                      int3
0040669E  CC                      int3
0040669F  CC                      int3
```

The wrapper pushes exactly six cdecl arguments, calls the separately recovered `FUN_004061F0`, then removes exactly `0x18` bytes from the stack and returns. There are no conditional branches and no native early returns.

## Referenced data

- `0x0137BF30` = `Image\\Archive\\ShopResource`.
- `0x01567928` is the direct-table token and the pushed direct count is exactly `3`.
- The resolved-table pointer/count pair is exactly `(0, 0)`.
- The message index is exactly `-1` (`0xFFFFFFFF`).

The three native direct-table records at `0x01567928` are:

| # | path pointer | resource token | path |
|---:|---:|---:|---|
| 0 | `0x0137BF4C` | `0x01692E3C` | `id\\jpn\\itembox\\itembox_360` |
| 1 | `0x0137BF68` | `0x016E289C` | `id\\jpn\\cockpit\\XfSSBox` |
| 2 | `0x0137BF80` | `0x016E289C` | `id\\jpn\\cockpit\\XfSSBox2` |

These records document the observed native forwarding contract only. The portable wrapper keeps the native table address opaque and does not reinterpret or duplicate the separately recovered `FUN_004061F0` implementation.

## Caller evidence

A direct `CALL rel32` scan of the exact `.text` image finds one direct call to `0x00406680`, at `0x00509BBE`.

The caller accepts either the primary `ShopResource` archive name or the adjacent `Shop2Resource` alias before invoking this same no-argument wrapper:

```text
00509B9A  BF 30 BF 37 01          mov edi,0x0137BF30
00509B9F  8B F5                   mov esi,ebp
00509BA1  B9 1B 00 00 00          mov ecx,0x1B
00509BA6  33 D2                   xor edx,edx
00509BA8  F3 A6                   repz cmps byte ptr [esi],byte ptr [edi]
00509BAA  74 12                   je 0x00509BBE
00509BAC  BF 98 BF 37 01          mov edi,0x0137BF98
00509BB1  8B F5                   mov esi,ebp
00509BB3  B9 1C 00 00 00          mov ecx,0x1C
00509BB8  33 C0                   xor eax,eax
00509BBA  F3 A6                   repz cmps byte ptr [esi],byte ptr [edi]
00509BBC  75 0F                   jne 0x00509BCD
00509BBE  E8 BD CA EF FF          call 0x00406680
00509BC3  5D                      pop ebp
```

`0x0137BF98` is `Image\\Archive\\Shop2Resource`. No argument is pushed for the call and the callee uses a plain `ret`, confirming a `cdecl void(void)` wrapper contract. The alias check belongs to the caller; `FUN_00406680` itself always forwards the primary `ShopResource` literal embedded at `0x0137BF30`.

## Reconstructed behavior and early exits

The native body has no conditional branch. It always performs exactly:

1. `FUN_004061F0(-1, "Image\\Archive\\ShopResource", 0x01567928, 3, 0, 0)`;
2. return.

For portable host testing, the reconstruction adds a null-services guard and a null-dispatch guard so unit tests can execute without dereferencing or calling native absolute addresses. Both are harness-only safety paths and are not claimed as native control flow.

`FUN_00406600`, `FUN_00406620`, and `FUN_004066C0` are parallel assignments and are not modified by this recovery.

## Tests

`tests/unit/test_fun_00406680.cpp` verifies:

- portable null-services early exit;
- portable missing-dispatch early exit;
- one dispatch when the service is present;
- exact message index `-1`;
- exact `Image\\Archive\\ShopResource` literal;
- direct table token `0x01567928` and count `3`;
- exact null/zero resolved-table pair.
