# FUN_00408700: reservation and preflight checkpoint

Date: 2026-09-25.

**Incomplete: blocked before exact local binary analysis.** This document is a
coordination checkpoint, not instruction-level evidence, an implementation,
a validation PASS, or a FAST_PASS_VALIDATED promotion.

## Exclusive scope

- Repository: `Putxo/BioHazard-V-Resurrection`.
- Candidate: `FUN_00408700`.
- VA: `0x00408700` = decimal `4228864`.
- RVA: `0x00008700` = decimal `34560`.
- Image-base arithmetic: `4228864 - 34560 = 4194304` (`0x00400000`).
- Exclusive reserved branch: `decomp/00408700-next`.
- Reservation base: `3eadb787bb165b706cee86eef0809e48563f8109` (`integration`).
- Do not implement, modify, or take ownership of `FUN_00408620`,
  `FUN_00408640`, `FUN_004086A0`, or any other parallel candidate.

## Remote preflight actually performed

The following immutable snapshots were read through the authorized GitHub
connection before the reservation was created:

| Reference | Commit |
| --- | --- |
| main | `184a9d7ea3a858e1fdf364de8a2d1f6ddbc71737` |
| integration | `3eadb787bb165b706cee86eef0809e48563f8109` |

At each of these commits, both `database/functions.csv` and
`database/claims.csv` were retrieved. No occurrence of `00408700` was found in
any of those four complete file responses. The integration functions file
has blob SHA `79d5badd746f5626708b34b200438e3fac4dee11`; its claims file has
blob SHA `bc8667a3e96a0b219bc6316963d0b7b2647fb8c1`.

The exact-address remote branch search returned no matching branch. The open
PR collection was inspected; it contained the unrelated PR #124. An all-state
PR search for `00408700` returned `total_count: 0` and
`incomplete_results: false`. The integration ref was re-read and still pointed
to the base above. The exclusive branch was then created and its ref read back
successfully at that same SHA.

These observations are snapshots, not a lock on future remote changes. Repeat
main/integration, inventories, branches and PR checks before subsequent work.
The existing `decomp/00408700-next` is this task's reservation, not a reason to
start another branch or candidate. Never force-update it over new work.

## Candidate membership: source availability distinction

The user supplied exact row number **141**, RVA **34560**, and VA **4228864**
for the canonical `function_candidates.csv`. Their numeric conversion is
consistent. However, the CSV itself is not mounted in this session and its
filename was not present in the retrieved integration tree. Therefore:

- Literal row 141 and membership by the second decimal column have **not**
  been independently re-read in this session.
- No third-column value, CSV hash, native boundary, byte count, or function
  classification is inferred from the supplied coordinates.
- An unavailable CSV is not evidence that the candidate is absent or false.
- The canonical denominator remains **79,782**. No removal is justified.

## Existing regional evidence, not newly generated evidence

The previously committed file
`decomp/evidence/region_00408000_00409800.md`, read at main commit
`184a9d7ea3a858e1fdf364de8a2d1f6ddbc71737` (blob
`43d1bff884b420b9296d04d8f8c8913a082e5aaf`), describes this constructor as:

1. Call the base constructor at `0x00A712D0`.
2. Install vtable token `0x0137E470`.
3. Return the original `this` pointer.

This is a useful starting point, not a substitute for the requested local EXE
analysis. It does not by itself establish the complete base-constructor
writes, object size, native instruction bytes, full CFG, or all caller/guard
behavior. No class name or additional initialization is inferred.

## Missing private inputs

`config/target.json` at the integration snapshot specifies:

- Resident Evil 5 PC **1.1.0 Dev**, x86.
- File size: **19,977,216 bytes**.
- SHA-256:
  `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

The current `/mnt/data` and shared working directory initially contained no
user files. Searches of the available working roots did not locate the
reference EXE, a file of that exact size, or `function_candidates.csv`.
Earlier-session filenames and hashes identify the expected input but do not
provide access to its bytes in this session. No EXE was rehashed, disassembled,
or otherwise claimed to have been analyzed here.

To unblock exact recovery, make the privately held executable matching the
above hash and the original canonical CSV accessible to the analysis session.
Keep them local and outside version control. Do not upload them to GitHub as a
workaround for the session boundary.

## Acceptance gates still pending

- Literal CSV membership verification.
- Exact local EXE identity, entry/boundary proof, instruction/CFG/XREF analysis,
  ABI and base-constructor effects.
- Original C++ implementation and relevant guards/early-exit tests.
- Local compilation, tests, metadata/report regeneration and validation.
- Recovery PR to integration, observed CI 5/5, and merge.
- Separate FAST_PASS_VALIDATED promotion, observed CI 5/5, and merge.
- Incremental main publication, observed CI 5/5, merge and public verification.

None of these gates is marked passed by this checkpoint. Do not treat CI on a
documentation-only change as validation of FUN_00408700.

## Preservation

This checkpoint adds only this documentation file on the exclusive branch.
It does not change `functions.csv`, `claims.csv`, inventory denominators,
README/progress counters, reports, build configuration, or parallel sources.
No EXE, DLL, proprietary asset, `.obj`, generated binary, or binary payload is
included. No recovery/promotion/publication PR is created for this incomplete
checkpoint.

The observed main README remains at **154 / 79,782 functions**,
**24,602 / 15,568,343 code bytes**, and **153 / 79,782 FAST PASS**; those are
pre-existing published values, not progress credited to this task.
