# Ordered decompilation evidence — 0x004190D0 through 0x00419510

## FUN_004190D0
- `thiscall`; per-frame update disabled by global byte `+0x798` or root flag `0x800`.
- Sends frame delta to global service `0x004E5B10/0x004E5AD0`, iterates four player objects.
- Maintains owner per-player activity bytes `+0x450`, timers `+0x474`, secondary activity bytes `+0x484`, timers `+0x488`, and one-shot bytes `+0x4A8` based on player predicates `0x009C7030`, player flags `+0x2EAC`, WORD `+0x13F0` and virtual state.
- Starts/stops related global/player updates through `FUN_0040F9A0`, `FUN_00416D20`; modes 3/4 reconcile WORD state with owner history and `0x004E83D0`.

## FUN_004192E0
- `thiscall`, three args; applies compact event record generated by earlier event classifiers.
- Increments global counter `+0x58C` on one path; modes 2/5 or predicate `0x00AC11F0` map slot via `0x00AC13B0`, maintain owner bitmask `+0x4F0`, and invoke `FUN_004172C0` with record bytes.
- Record byte fields trigger timer addition `FUN_004110A0(5)`, score delta `FUN_00416390(...,0x0C)`, global per-slot event `0x0041BCC0`, operations through `FUN_004106C0`/`FUN_00410550`.
- Record flag byte `+0x06` dispatches global event codes `0x27,0x28,0x29,0x2A,0x2B,0x2F,0x24`; mode 4 bit `0x80` also updates user state or dispatches code `0x45`; per-user value `0x96` triggers code `0x44`.
- Strong semantics: consume compact gameplay event and fan out scoring/stat/achievement-style side effects.

Next ordered candidate `0x00419510`. No VERIFIED/MATCHED claim.