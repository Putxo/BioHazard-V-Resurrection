# FUN_004067A0 evidence

## Target

- Exact local reference executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`; size 19,977,216 bytes; PE32 x86 image base `0x00400000`.
- Function VA `0x004067A0`, RVA `0x000067A0`.
- Native body: 27 bytes through `ret` at `0x004067BA`.
- Candidate byte SHA-256: `714e81034611162ca97b9b818a9a6515dfa9e0133523fdd24ed0c2aa3305a648`.
- `0x004067BB..0x004067BF` is `INT3` padding. The next aligned root starts at `0x004067C0` and is excluded.

## Instruction evidence

```text
004067A0  6A 00                   push 0
004067A2  6A 00                   push 0
004067A4  6A 01                   push 1
004067A6  68 98 7B 56 01          push 0x01567B98
004067AB  68 C8 C3 37 01          push 0x0137C3C8
004067B0  6A FF                   push -1
004067B2  E8 39 FA FF FF          call 0x004061F0
004067B7  83 C4 18                add esp,0x18
004067BA  C3                      ret
004067BB..004067BF                  int3 padding
```

The native body has no conditional branch or early return. It forwards exactly:

`FUN_004061F0(-1, "Image\\Archive\\RecordResource", 0x01567B98, 1, 0, 0)`

and returns.

## Referenced data

- `0x0137C3C8` = `Image\\Archive\\RecordResource`.
- Direct table `0x01567B98`, count `1`:

| # | path pointer | resource token | path |
|---:|---:|---:|---|
| 0 | `0x0137C3E8` | `0x01692E3C` | `id\\jpn\\menu\\top14` |

- Resolved table/count = `(0, 0)`.
- Message index = `-1`.

The portable reconstruction preserves native absolute table/resource tokens as opaque values and delegates to the separately recovered resource-group dispatcher contract.

## Caller evidence

A complete direct `CALL rel32` scan of the exact `.text` image finds no direct call target equal to `0x004067A0`. This means there is no directly encoded relative call to this wrapper in `.text`; it may be reached through indirect dispatch/data-driven selection. This absence is recorded rather than inventing a direct caller.

The plain `ret` and lack of incoming stack arguments in the wrapper body establish a no-argument caller-cleaned `cdecl void(void)` contract consistent with the adjacent recovered resource wrappers.

## Host-only early exits and tests

The portable host implementation adds only two harness-safety exits: null services and null dispatch callback. Neither exists in the native 27-byte body. Unit tests explicitly cover both host-only exits and assert exact normal-path forwarding of message index, archive literal, direct table/count and resolved table/count.

`FUN_00406780` is already validated/published and is preserved. The next aligned root `0x004067C0` is outside this recovery.
