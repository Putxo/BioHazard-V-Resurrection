# FUN_00405A60 evidence

- VA: `0x00405A60`
- RVA: `0x00005A60`
- Size: `368` bytes in the ordered V6 candidate inventory, ending at next candidate `0x00405BD0`.
- Calling convention: no stack arguments are read; plain `ret` at `0x00405BC9`, so the host reconstruction uses `cdecl void`.
- Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- GNU `objdump` and LLVM `llvm-objdump` independently agree on the decoded instruction boundaries and control flow over `0x00405A60..0x00405BC9`.

## Instruction evidence

- `0x00405A60..0x00405A70` reserves `0x20C` bytes of local storage and emits begin marker `0x0137AFEC` through `0x01198F40` with global token `0x01652E00`.
- `0x00405A75..0x00405A84` formats the first local path at frame `+0x04` using marker `0x0137B518` (`%s`) and literal `0x0137B51C` (`Image\\Archive\\Core2Resource`) through `0x01251809`.
- `0x00405A89..0x00405AB9` loads the pointer at `0x016E1D3C + 0x102C`, uses pointer `+0x08` when non-null or empty literal `0x0137AA5C`, and formats the second local at frame `+0x108` with `0x0137AAD4` (`%s\\%s.arc`) plus the first local path.
- `0x00405ABE..0x00405AD1` tests bit `0x00200000` in `[0x016E1660 + 0x1E550]`; when clear, the optional special-registration block is skipped.
- `0x00405AD3..0x00405B17` prepares the second local through `0x0119B710`, registers the first path through `0x00D14C10` with key token `0x016E216C` and enabled value `1`, finalizes a non-null result through `0x00D25060`, then checks the second local through `0x0119B910`. A non-zero `AL` branches directly to the common epilogue at `0x00405BC3` before the loading flag or batch is touched.
- `0x00405B1D..0x00405B3E` saves byte `[0x016E1D3C + 0x904D]` in `BL`, forces it to `1`, begins a batch through `0x00A52D90` using the first local path, and calls `0x00A54010`.
- `0x00405B43..0x00405B9B` iterates `ESI` from `0` to `0x28` in 8-byte steps over exactly six `(path,key)` records beginning at `0x015674D0`. Every record is submitted to `0x00D14C10` with enabled value `1`.
- `0x00405B6B..0x00405B90` binds a non-null registration through `0x005477E0` using context `0x016B1F3C` only when suppressor bytes `0x016B1F39` and `0x016B1F3B` are both zero.
- `0x00405B9D..0x00405BC2` ends the batch through `0x00A54120(0)`, restores the exact saved loading byte, emits end marker `0x0137AF90` through `0x01198F40`, and restores `ESI`/`EBX`.
- `0x00405BC3..0x00405BC9` is the common epilogue and plain `ret`. Bytes `0x00405BCA..0x00405BCF` are `INT3` padding before the next V6 candidate `0x00405BD0` and are not part of this reconstruction.

## Static table evidence

| Table VA | Path pointer | Path text | Key token |
| --- | --- | --- | --- |
| `0x015674D0` | `0x0137B538` | `id\\jpn\\menu` | `0x0165BB68` |
| `0x015674D8` | `0x0137B544` | `etc\\message\\mes_tex` | `0x016E21B4` |
| `0x015674E0` | `0x0137B544` | `etc\\message\\mes_tex` | `0x01692E3C` |
| `0x015674E8` | `0x0137B558` | `etc\\message\\cursor_ps3_dc` | `0x016E21B4` |
| `0x015674F0` | `0x0137B558` | `etc\\message\\cursor_ps3_dc` | `0x01692E3C` |
| `0x015674F8` | `0x0137B574` | `etc\\vibration_dc` | `0x016E22B0` |

## Early-exit and side-effect tests

- The native early branch is exercised with the special gate enabled and the `0x0119B910`-equivalent callback returning true. The test requires special registration/finalization to occur, then verifies there is no loading-byte access, batch start/end, static-table registration, bind, or end marker after the branch.
- A reconstruction-only null-service guard is tested to keep the host model safe without claiming an additional binary branch.
- The normal path checks all six table entries and key tokens in order, exact preservation/restoration of a non-boolean loading byte, one batch, and one bind per successful registration.
- The suppressor path confirms all six registrations still occur while bind callbacks are skipped when a suppressor is active.

## Host reconstruction

The recovered host routine models the observed fixed resource-registration pass only: it constructs the Core2Resource and archive paths, performs the optional special registration/cancel gate, preserves the global loading byte around one six-entry registration batch, applies the two suppressor checks before binding, and preserves the begin/end trace behavior through explicit service callbacks.
