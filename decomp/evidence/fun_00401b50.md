# FUN_00401B50 evidence

## Candidate

- RVA: `0x00001B50`
- VA: `0x00401B50`
- Size: `352` bytes through alignment before next candidate `0x00401CB0`.
- Candidate source: V6 ordered candidate after `FUN_00401950`.

## Calling convention

The function uses `ECX` as an object/state pointer and returns with `ret 8`, so it is recorded as a `thiscall`-style helper with two stack arguments. The recovered portable interface names those two inputs as an award-record array/count pair.

Representative caller:

```asm
00401D43  push edi
00401D44  push dword ptr [esi]
00401D46  mov  ecx, esi
00401D48  call 0x00401B50
```

## Disassembly summary

The function allocates a `0x2000` scratch buffer, clears counters at object offsets `+4` and `+8`, logs a header, iterates `0x24`-byte records, emits a line for each awarded record, calls `FUN_004016C0` with the achievement id, and logs a final saved/awarded summary.

Key body facts:

```asm
00401B72  mov dword ptr [edi + 4], esi ; awarded count = 0
00401B75  mov dword ptr [edi + 8], esi ; saved count = 0
...
00401BD0  mov  eax, dword ptr [esi]    ; flags at record +0x20
00401BD2  test eax, 0x30000
00401BD7  je   0x00401C5D
00401BDD  test eax, 0x20000
00401BE2  je   0x00401BEB
00401BE4  mov  eax, 0x0137A8A4         ; "Offline"
...
00401BEB  test eax, 0x10000
00401BF0  mov  eax, 0x0137A894         ; "Online"
00401BF9  mov  eax, 0x0137A890         ; ""
...
00401C4C  call 0x004016C0              ; store/set achievement id
00401C56  test al, al
00401C58  je   0x00401C5D
00401C5A  add  dword ptr [edi + 8], ebx
...
00401C6A  mov  edx, dword ptr [edi + 4]
00401C6D  mov  eax, dword ptr [edi + 8]
...
00401C8D  mov  eax, dword ptr [edi + 8]
00401C9F  ret  8
```

## String evidence

`.rdata` string references used by this function:

```text
0x0137A8B4 -> List of awarded achievements for %s:
0x0137A8A4 -> Offline
0x0137A894 -> Online
0x0137A890 -> ""
0x0137A868 -> Award %d %s: '%s'
0x0137A864 -> ""
0x0137A83C -> Achievements saved to Steam: (%d/%d)
```

## Recovered behavior

- Clears the per-object awarded and saved counters.
- Logs the awarded-achievements header.
- For each record with flags `0x10000` or `0x20000` set:
  - chooses status text `Offline`, `Online`, or empty according to the original branch order;
  - logs `Award %d %s: '%s'` with id/status/label;
  - increments the awarded counter;
  - invokes the recovered Steam achievement setter path represented by `FUN_004016C0`;
  - increments the saved counter only if that store succeeds.
- Logs `Achievements saved to Steam: (%d/%d)`.
- Returns the saved counter.
