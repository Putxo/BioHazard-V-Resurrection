# Ordered evidence: 0x00423920 - 0x00424E9F

Decoded directly from the verified target binary. This region contains one very large owner operation followed by a dense cluster of state/update helpers.

## FUN_00423920
- Begins with a 0xA0-byte local frame and continues through `0x004247EC` (`ret 4`), followed by short internal/tail helpers through `0x00424885`.
- The function is an owner method with one stack argument and extensive interaction with the same session/global managers initialized by `0x00423280`.
- Control flow contains numerous guarded state transitions, owner/global field updates, and calls into previously observed manager families. Because this body spans ~3.8 KiB and contains shared tails, it is recorded as a single large candidate pending semantic subdivision from caller/type evidence rather than inventing high-level names.

## FUN_00424890 and tails
- Global-state query/update helper rooted at global `0x016E1660`, with multiple return tails through `0x004248E1`.

## FUN_004248F0
- Calls `0x00424890` first, then performs additional state checks/updates; has several early return tails through `0x00424997`.

## FUN_004249A0
- Owner method with one stack argument (`ret 4` paths at `0x004249E6` and `0x00424A0F`).
- Maintains explicit early-return branches; kept separate from next padded candidate.

## FUN_00424A20
- Owner state helper ending `0x00424A5D`.

## FUN_00424A60
- Larger owner state/update helper with main return at `0x00424B5F` and secondary tail at `0x00424B70`.

## FUN_00424B80
- Tiny predicate beginning with `cmp byte [ecx+0x28],0`; returns by `0x00424B8D`.

## FUN_00424B90
- Owner helper with early/main returns through `0x00424BC3/0x00424BDB/0x00424C84`.

## FUN_00424C90
- Global/session state routine rooted at `0x016E1660`; continues until INT3 padding preceding `0x00424EA0`.

All entries above are boundary/control-flow evidence only where semantics are not yet proven. No VERIFIED/MATCHED status is implied.
