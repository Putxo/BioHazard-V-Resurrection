# Ordered decompilation evidence — 0x0040E650 through 0x0040E860

## FUN_0040E650
- `thiscall`; ensures owner byte state `+0x04 == 0x0C` using zero-DWORD then byte-write pattern.

## FUN_0040E670
- `thiscall`; tiny substate machine on `this+0x06/+0x07`.
- Initial state advances `+0x06` and seeds countdown byte `+0x07=1`; when countdown reaches zero calls `0x0041B100(global_root,0x10)`.
- Validates current player through `0x0052FCC0`; failure stores owner `+0x350` into global service `0x0165B6E0+0x74`, sets outer state `+0x05=5`, resets substate.
- Success calls `0x0052FAF0(global 0x0165B6E0, this+0x350)`, advances outer state and resets substate.

## FUN_0040E700
- `thiscall`; four-state substate machine on byte `this+0x06`.
- State 0 waits for global `0x0165BBB0+0x39744 == 0`, synchronizes owner lock `this+0x08`, forwards owner `+0x350/+0x354` through services `0x004E4FA0`, `0x004E6E90`, `0x00AC0E70`, `0x004E5030`, `0x004A5840`, then requests code `0x20` via `0x004E9920` and advances.
- State 1 waits for global byte `0x016E03CC+0x27928` clear, calls `0x004BA340`, then selects request code `0x25` or `0x20` based on `0x0165C170+0x2F658`, advances.
- State 2 waits until service state `0x0165B6E0+0x134 != 1`, then calls `0x004BAA20` and `0x004BA1A0`, advances.
- State 3 waits for same service condition, then sets outer state `this+0x05=5` and resets substate.
- Strong semantics: staged load/session/service handoff.

## FUN_0040E860
- `thiscall`; teardown/reset transition.
- Sends `(0,1)` and `0` through global `0x0165BBB0` service calls `0x004E9920/0x004E9990`, resets `0x0165B6E0` with `0x0052FAF0(-1)`, and calls `0x004E6E90`.
- Under synchronization clears root flags `0x10` and `0x80000000` from `+0x1E4F8`.
- Resets owner DWORD/byte state at `+0x04` to state 2.

Next ordered entry `0x0040E930`. No VERIFIED/MATCHED claim.