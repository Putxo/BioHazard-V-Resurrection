# FUN_00407C00 evidence

## Target identity

- Exact target: RE5DX9 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- VA `0x00407C00`, RVA `0x00007C00`, section `.text`.
- Exact native body size: `0x38` = 56 bytes, ending after the `ret 4` at `0x00407C35`.
- Native body SHA-256: `27c489f18c6d6d6d7a349e773fbc8ffbb876ca2cb43f44bea8fa591c0159329e`.
- `0x00407BF1..0x00407BFF` is INT3 padding before the entry; `0x00407C38..0x00407C3F` is INT3 padding before the next aligned entry at `0x00407C40`.

This independently confirms that `0x00407C00` is an instruction-aligned callable entry, not an embedded byte or data candidate.

## Exact instruction flow

```text
00407C00  56                    push esi
00407C01  8B F1                 mov esi,ecx
00407C03  8B 06                 mov eax,[esi]
00407C05  8B 50 10              mov edx,[eax+10h]
00407C08  FF D2                 call edx
00407C0A  8B 4C 24 08           mov ecx,[esp+8]
00407C0E  8B 49 04              mov ecx,[ecx+4]
00407C11  39 48 04              cmp [eax+4],ecx
00407C14  74 1C                 je 00407C32
00407C16  8B 40 10              mov eax,[eax+10h]
00407C19  85 C0                 test eax,eax
00407C1B  74 0F                 je 00407C2C
00407C1D  8D 49 00              lea ecx,[ecx]
00407C20  39 48 04              cmp [eax+4],ecx
00407C23  74 0D                 je 00407C32
00407C25  8B 40 10              mov eax,[eax+10h]
00407C28  85 C0                 test eax,eax
00407C2A  75 F4                 jne 00407C20
00407C2C  33 C0                 xor eax,eax
00407C2E  5E                    pop esi
00407C2F  C2 04 00              ret 4
00407C32  8B C6                 mov eax,esi
00407C34  5E                    pop esi
00407C35  C2 04 00              ret 4
```

The function is a one-argument callee-cleaned `thiscall`:

1. preserve `this` in ESI;
2. invoke virtual slot `+0x10` on `this` to obtain the first node;
3. load the supplied key object's dword at `+0x04`;
4. compare it with each node's dword at `+0x04`;
5. advance through the node link at `+0x10`;
6. return `this` on the first match, or null when the chain ends.

The sibling entry `FUN_00407BC0` walks the same `+0x04/+0x10` chain but returns boolean membership instead. No semantic class/type name is assigned here because the binary evidence establishes the structural contract but not an authoritative source name.

## Direct caller evidence

The exact executable contains 974 decoded direct transfers to `0x00407C00` in the full objdump pass. Representative independently decoded callers include:

- `0x00402D1C: E8 DF 4E 00 00  call 0x00407C00`
- `0x004057ED: E8 0E 24 00 00  call 0x00407C00`
- `0x004113A1: E8 5A 68 FF FF  call 0x00407C00`
- `0x004113C5: E8 36 68 FF FF  call 0x00407C00`

At `0x004057E6` and `0x00402D15`, callers push key object `0x016B1024` before the call. At `0x004113BE`, a later call pushes a second key object `0x016AF0F4`. Callers test EAX for null and then use the returned object, matching the reconstructed return contract.

## Portable reconstruction

The host-side reconstruction keeps the observed field offsets explicit:

- key value: `key+0x04`;
- node value: `node+0x04`;
- next node: `node+0x10`.

Only the virtual `this+vslot 0x10` operation is injected through a service callback so the logic can be unit-tested without a proprietary game vtable. Host-only guards for missing services/callbacks and a null harness head return null before dereference; these guards are not claimed to exist in the native body.

Unit tests cover:

- missing services guard;
- missing callback guard;
- null host head guard;
- first-node match / immediate `this` return;
- later-node match through the `+0x10` chain;
- chain exhaustion / native null return.

No EXE, DLL, proprietary assets, `.obj`, or binary output is versioned.
