# FUN_00408AF0 — exclusive reservation and preflight

Date: 2026-09-25

Status: **BLOCKED_MISSING_LOCAL_INPUTS — NOT IMPLEMENTED — NOT FAST_PASS_VALIDATED**.

This document records remote coordination checks and the exact point at which this session was blocked. It is not disassembly evidence, a function-boundary classification, a completed implementation, or a PASS report.

## Scope and supplied candidate identity

Repository: `Putxo/BioHazard-V-Resurrection`.
Exclusive target: `FUN_00408AF0`.
Reserved branch: `decomp/00408af0-next`.

The user supplied candidate row **142**, RVA **35568**, VA **4229872** in `function_candidates.csv`. Local arithmetic checks verified:

- `4229872 == 0x00408AF0`.
- `35568 == 0x00008AF0`.
- `4229872 - 35568 == 0x00400000`.

**The canonical CSV itself was not available in this session. Row ordinal and literal membership remain user-supplied, not independently verified here. No third-column value is assumed.** Correct address arithmetic is not proof of CSV membership or a real function entry point.

## Remote preflight observations

The following refs and files were read through the connected GitHub API before the branch was created:

| Check | Observed result |
| --- | --- |
| `main` | `184a9d7ea3a858e1fdf364de8a2d1f6ddbc71737` |
| `integration` | `3eadb787bb165b706cee86eef0809e48563f8109` |
| `database/functions.csv`, both branches | No `FUN_00408AF0` match |
| `database/claims.csv`, both branches | No `FUN_00408AF0` match |
| Branch search for `00408af0` immediately before reservation | No existing matching branch |
| Repository-wide PR search for `00408AF0`, all states | Zero matches; search reported complete results |
| Open PR collection | Existing unrelated PR #124 was read and left untouched |
| Recursive `integration` tree | No `function_candidates.csv` path and no `00408af0` path found |
| `integration` ref re-read before reservation | Still `3eadb787bb165b706cee86eef0809e48563f8109` |

The branch was successfully created from the exact integration commit above. This is a branch-level reservation only: no row was inserted into the shared `database/claims.csv` or `database/functions.csv`.

## Required local inputs and actual availability

`config/inventory_freeze.json` identifies the expected reference executable as:

- Size: **19,977,216 bytes**.
- SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Image base: `0x00400000`.

These are expected values read from repository configuration, **not a hash computed from an executable in this session**.

The configuration describes the canonical candidate inventory as the local artifact `work/analysis/function_candidates.csv`.

Actual local checks found `/mnt/data` empty. `/mnt/data/reference` and `/mnt/data/work/analysis/function_candidates.csv` do not exist. The inspected input locations under `/mnt`, `/tmp`, and `/home/oai/share` yielded no reference executable or candidate CSV. No recoverable reference download or target instruction listing was available from the retrieved prior-session context.

Consequently, this session has not read the original bytes at `0x00408AF0`, verified its boundaries, decoded its instructions, established calling convention or return behavior, or derived guards and early exits.

## Validation and publication status

| Stage | Status |
| --- | --- |
| Supplied VA/RVA/image-base arithmetic | Verified locally |
| Remote collision checks and branch creation | Performed |
| Independent literal membership in canonical CSV | BLOCKED: local CSV unavailable |
| Local EXE size and SHA-256 verification | BLOCKED: local EXE unavailable |
| Exact instruction and boundary analysis | NOT RUN |
| Recovered C++ implementation | NOT CREATED |
| Target tests, including guards and early exits | NOT CREATED / NOT RUN |
| Local build and repository validators for a target implementation | NOT RUN |
| Implementation PR to integration, target CI 5/5, merge | NOT CREATED / NOT RUN |
| FAST_PASS_VALIDATED promotion, target CI 5/5, merge | NOT CREATED / NOT RUN |
| Incremental main publication, target CI 5/5, merge | NOT CREATED / NOT RUN |

Any automatic CI that may run for this documentation-only branch must not be presented as semantic validation of `FUN_00408AF0`.

## Preservation and resumption contract

No executable, DLL, proprietary asset, object file, recovered source, test, progress increment, or denominator correction is part of this checkpoint. The configured denominator remains **79,782** and `classified_false_positive_removals` remains **0**. The missing local CSV is not evidence that this candidate is a false positive.

Preserve `FUN_00408620`, `FUN_004086A0`, `FUN_00408700`, all other parallel work, and PR #124. Do not merge all of integration into main to publish this target.

Resume this same branch rather than reserving another candidate. First repeat the live main/integration, inventory, claims, branches, and PR checks. Make the canonical executable and original candidate CSV available locally without committing the executable; verify the exact file size/SHA-256 and literal decimal-VA membership before reconstruction. Only after exact analysis, implementation, and actual tests may target metadata advance. Require actual successful checks on each relevant PR head before integration, FAST_PASS_VALIDATED promotion, and incremental main publication. Do not label this checkpoint PASS or completed.
