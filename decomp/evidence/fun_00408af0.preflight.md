# FUN_00408AF0 — exclusive reservation and preflight

Date: 2026-09-25

Status: **BLOCKED_REFERENCE_BYTES_UNAVAILABLE — NOT IMPLEMENTED — NOT FAST_PASS_VALIDATED**.

This document records remote coordination checks, the supplied canonical candidate row, and the actual reference-file retrieval result. It is not disassembly evidence, a function-boundary classification, a completed implementation, or a PASS report.

## Scope and confirmed candidate identity

Repository: `Putxo/BioHazard-V-Resurrection`.
Exclusive target: `FUN_00408AF0`.
Reserved branch: `decomp/00408af0-next`.

In the continuation request, the user explicitly confirmed canonical row **142**, literally:

```csv
35568,4229872,1,direct CALL target (linear sweep)
```

Local arithmetic was rechecked in the continuation:

- `4229872 == 0x00408AF0`.
- `35568 == 0x00008AF0`.
- `4229872 - 35568 == 0x00400000`.

**The literal row is now supplied evidence, including its third and fourth fields.** The complete canonical CSV has not been reopened independently in this execution. Do not keep requesting that CSV as a prerequisite merely to continue byte analysis: the outstanding input is the exact executable's bytes. The supplied row does not itself establish instruction boundaries, function size, calling convention, guards, or behavior. In particular, the third field is not being interpreted as the function's byte length.

## Original remote preflight observations

The following historical snapshot was recorded before initial branch creation:

| Check | Observed result |
| --- | --- |
| `main` | `184a9d7ea3a858e1fdf364de8a2d1f6ddbc71737` |
| `integration` | `3eadb787bb165b706cee86eef0809e48563f8109` |
| `database/functions.csv`, both branches | No `FUN_00408AF0` match |
| `database/claims.csv`, both branches | No `FUN_00408AF0` match |
| Branch search for `00408af0` immediately before reservation | No existing matching branch |
| Repository-wide PR search for `00408AF0`, all states | Zero matches; search reported complete results |
| Open PR collection | Existing unrelated PR #124 was read and left untouched |
| Recursive `integration` tree | No `function_candidates.csv` path and no `00408af0` path found |

The exclusive branch was created from that integration commit. Initial documentation commit: `0be22c22c74aff7be8d75bfd8d60e6e9e3f62cb7`. This remains a branch-level reservation; no target row has been inserted into the shared `database/claims.csv` or `database/functions.csv`.

## Continuation preflight and synchronization

The existing branch and its file were fetched again rather than recreated. At the start of the continuation, `main` still resolved to `184a9d7ea3a858e1fdf364de8a2d1f6ddbc71737`; its previously checked immutable inventory snapshot therefore remained applicable.

`integration` had advanced to `53466552e4f87ccaec42d485e5cfaac90210ffde`. Its `database/functions.csv` and `database/claims.csv` were fetched and searched again: no `00408AF0` match. The repeated all-state PR search returned zero matches. The branch search returned the existing `decomp/00408af0-next` reservation.

The comparison against the updated integration snapshot reported the original branch one commit ahead and eighteen behind, with only `decomp/evidence/fun_00408af0.preflight.md` in its own delta. There was no recovered source or test to resume. The latest integration commit and its promotion delta for the parallel `FUN_004086A0` were inspected but not edited.

The synchronization uses the complete integration tree `90de9162018bb68756449184fc2d1656177de68a` and adds only this target's updated preflight document. The merge preserves both the original branch commit and the integration snapshot as parents; no force update, history rewrite, shared-branch write, or parallel-file replacement is required. Any later integration advancement must be checked again before implementation writes.

## Expected reference and actual retrieval result

Expected executable name: `9) RE5DX9_1.1.0 Dev (Release 15 des. 2017).exe`.

The user identified its original directory as `C:/Program Files (x86)/Steam/steamapps/common/Resident Evil 5 retail/.EXEs historial/` and its location in the account's Library/context.

`config/inventory_freeze.json`, re-read at the continuation integration snapshot, specifies:

- Size: **19,977,216 bytes**.
- SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Image base: `0x00400000`.
- Candidate denominator: **79,782**.

These remain expected values from configuration and the user, **not a hash computed from an executable in this execution**.

Three targeted `personal_context` retrievals were made: exact filename/hash recovery, the mechanism behind earlier claimed Library recovery, and a request for the actual Library file object rather than conversational mentions. The returned material contained prior messages naming the file, reporting its hash, and giving a previous-session `sandbox:/mnt/data/...exe` link. It did not supply binary bytes, a mounted attachment, or an actionable download reference. Discovery of a Library-file capability likewise did not return an applicable attachment-recovery action.

Actual filesystem checks in this continuation found `/mnt/data` empty and no matching RE5 executable or `function_candidates.csv` under `/mnt`, `/tmp`, or `/home/oai/share`. The previous-session sandbox path is not a file present in this execution. These results establish an access limitation here, **not that the user's Library lacks the file**. Earlier assistant assertions of recovery are not substituted for current access to the bytes.

Consequently, no original instruction at `0x00408AF0` has been read in this execution. Function boundaries, semantics, ABI, guards, early exits, and dependencies remain unanalysed. No substitute executable was downloaded or analysed, and no executable was uploaded to GitHub.

## Target validation and publication status

| Stage | Status |
| --- | --- |
| Canonical candidate row | Literal row explicitly confirmed by user; recorded above |
| Supplied VA/RVA/image-base arithmetic | Rechecked locally |
| Existing branch and remote delta | Re-read; documentation-only target delta |
| Local EXE size and SHA-256 verification | BLOCKED: reference bytes not available to this execution |
| Exact instruction and boundary analysis | NOT RUN |
| Recovered C++ implementation | NOT CREATED |
| Target tests, including guards and early exits | NOT CREATED / NOT RUN |
| Local build and repository validators for a target implementation | NOT RUN |
| Implementation PR to integration, target CI 5/5, merge | NOT CREATED / NOT RUN |
| FAST_PASS_VALIDATED promotion, target CI 5/5, merge | NOT CREATED / NOT RUN |
| Incremental main publication, target CI 5/5, merge | NOT CREATED / NOT RUN |

Any CI for a documentation-only synchronization is not semantic validation of `FUN_00408AF0` and must not be reported as the requested target CI 5/5.

## Preservation and resumption contract

Relative to the synchronized integration snapshot, the only target change is this document. No EXE, DLL, proprietary asset, object file, recovered source, target test, progress increment, or denominator correction is included. The frozen inventory file is preserved unchanged; **79,782** remains the denominator. Missing access to a reference file is not false-positive evidence.

Continue exclusively on `decomp/00408af0-next`; preserve `FUN_00408620`, `FUN_004086A0`, `FUN_00408700`, all other parallel work, and unrelated PRs. Do not merge all of integration into main to publish this target.

Resume from the existing branch after repeating live coordination checks. Obtain access to the exact saved EXE as an actual attachment or local file; verify its full SHA-256 and size before reading code. Use the confirmed literal candidate row above without relitigating its supplied identity. Reconstruct only from observed instructions, then implement and run target tests before changing target metadata. Require actual successful checks on each relevant PR head for integration, FAST_PASS_VALIDATED promotion, and incremental main publication. This checkpoint is not completion and must not be labelled PASS.
