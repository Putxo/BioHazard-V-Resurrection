# FUN_00401670 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x00401670`
- RVA: `0x00001670`
- Size: `0x4B` / 75 bytes (`0x00401670` through `0x004016BA`)
- Boundary evidence: preceding function returns at `0x00401664`; `0x00401665..0x0040166F` are `INT3`; this function returns at `0x004016BA`; `0x004016BB..0x004016BF` are `INT3` before the next function at `0x004016C0`.

## Imports and virtual calls

The function first calls IAT slot `0x013714CC`, identified by the PE import table as `steam_api.dll!SteamUser`; its return value is intentionally ignored. It then calls IAT slot `0x013714C8`, identified exactly as `steam_api.dll!SteamUserStats`.

If `SteamUserStats()` returns null, the function returns false. Otherwise it performs the following virtual calls on the returned interface:

1. slot `+0x3C` with the sole function argument (achievement index); the return value is ignored;
2. slot `+0x20` with the pointer loaded from `0x015671B8 + index*4`;
3. if slot `+0x20` succeeds, tail-calls slot `+0x28` and returns that result unchanged.

The ISteamUserStats method ordering identifies these slots as `GetAchievementName(uint32)`, `ClearAchievement(const char*)`, and `StoreStats()` respectively. The observed calling sequence therefore probes the indexed Steam achievement name, clears the game's hard-coded achievement API name for the same index, and stores the modified stats.

## Achievement table evidence

The pointer table beginning at VA `0x015671B8` contains 71 entries. Entry 0 is the literal `Nothing. GFWL starts counting at 1`; entries 1..70 are RE5 achievement API names, beginning with `ACH_CHAPTER11_CLEAR` and ending with `ACH_HEAD_HUNTING`.

The recovered source reproduces all 71 observed strings exactly. It adds a host-side bounds guard for testability; the original machine code assumes its caller supplies an in-range index and directly indexes the global table.

Module/subsystem remain `UNKNOWN`; the Steam achievement behavior is proven, but no stronger game-level ownership is established by this routine alone.
