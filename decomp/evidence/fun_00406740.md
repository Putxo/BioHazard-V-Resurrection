# FUN_00406740 evidence

## Target

- Evidence was taken only from the exact local reference executable; no binary is committed.
- Reference SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.
- Reference size: 19,977,216 bytes.
- PE32 x86 image base: `0x00400000`.
- Function VA `0x00406740`, RVA `0x00006740`.
- Candidate body: 27 bytes (`0x1B`), through the `ret` at `0x0040675A`.
- Candidate byte SHA-256: `e4f7c67e54640dff10aaa53de91d5eb043d7fbbc420097c2bb3de9e1122bef64`.
- `0x0040675B..0x0040675F` is `INT3` padding; the next aligned root begins at `0x00406760`.

## Instruction evidence

```text
00406740  push 00h
00406742  push 00h
00406744  push 02h
00406746  push 01567B10h
0040674B  push 0137C2D8h
00406750  push FFFFFFFFh
00406752  call 004061F0h
00406757  add esp,18h
0040675A  ret
```

The wrapper forwards six cdecl arguments to the separately recovered `FUN_004061F0`:

1. message index `-1` (`0xFFFFFFFF`);
2. archive literal at `0x0137C2D8`;
3. direct-table token `0x01567B10`;
4. direct count `2`;
5. resolved-table token `0`;
6. resolved count `0`.

The archive literal at `0x0137C2D8` is `Image\Archive\Extra1End`.

The `add esp,18h` removes exactly six pushed dwords and the function returns with a plain `ret`, confirming a caller-cleaned cdecl no-argument wrapper.

## Table evidence

The direct table at `0x01567B10` contains exactly two 8-byte `(name pointer, resource token)` records, matching the pushed count `2`:

```text
0  id\jpn\chapter\cap_end01  016E2758
1  id\jpn\extra\result01     01692E3C
```

The raw pointer pairs are:

```text
01567B10: 0137C2F0 016E2758
01567B18: 0137C30C 01692E3C
```

Two records occupy `0x10` bytes, so the table ends exactly at `0x01567B20`, the direct-table address pushed by the adjacent aligned wrapper at `0x00406760`. The second table/count pair is explicitly `(0, 0)`, so this wrapper supplies no resolved-entry table. The reconstruction preserves both resource tokens as opaque original addresses and does not interpret or modify the downstream `FUN_004061F0` dependency.

## Caller evidence

A direct `CALL rel32` scan of the complete exact `.text` image finds exactly one direct call to `0x00406740`, at `0x00509C84`.

The caller compares the current resource name against the same `Extra1End` literal and invokes the wrapper only on equality:

```text
00509C72  mov edi,0137C2D8h
00509C77  mov esi,ebp
00509C79  mov ecx,18h
00509C7E  xor eax,eax
00509C80  repz cmps byte ptr [esi],byte ptr [edi]
00509C82  jne 00509C93h
00509C84  call 00406740h
00509C89  pop ebp
```

`0x18` bytes is the full archive string including its terminating zero. No argument is pushed for `call 00406740h`, agreeing with the no-argument wrapper contract.

## Early exits and reconstruction choice

The original 27-byte body has no conditional branch and no early-return path: it always forwards the six constants to `FUN_004061F0`, cleans the stack, and returns.

The portable reconstruction adds only two harness guards: a null-services guard and a null-dispatch guard. Both are unit-tested explicitly as host-only early exits and are not claimed as original binary control flow. The normal-path test verifies every observed forwarded argument.

`FUN_004066E0`, `FUN_00406700`, and `FUN_00406720` are active or parallel assignments and are not modified by this recovery. `FUN_004061F0` is treated only as an existing opaque dispatch dependency; this candidate reconstructs the fixed forwarding wrapper at `0x00406740`.
