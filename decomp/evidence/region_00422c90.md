# Ordered evidence: 0x00422C90 - 0x00423161

Decoded from the verified target executable.

## FUN_00422C90
- Owner in ECX, one stack argument, `ret 4`.
- Iterates owner collection count `+0x5F8` with pointer table `+0x604`.
- For each non-null element, walks runtime type chain via vtable `+0x10` and requires type token/global `0x016B51FC`.
- Matching objects receive the caller argument through `0x00AD3000`.

## FUN_00422D10
- Owner plus one object pointer; `ret 4`.
- Replaces owner pointer `+0x608`. Existing pointer is released through `0x00D25060`.
- Incoming object is accepted only when its runtime type chain contains token/global `0x01692C00`; accepted object is retained through `0x00D25050`.
- Null input clears the slot.

## FUN_00422D80
- Owner plus one index; `ret 4`.
- Queries an owner-provided pair table via virtual methods at slots `+0x4C/+0x50`; bounds-checks the index.
- Requires type token/global `0x01657F60` on the selected first pointer.
- Obtains a child/interface via virtual slot `+0x04`, initializes it through its virtual `+0x14` with owner and the pair's second value, writes owner to child `+0x24`, and returns child; failure returns null.

## FUN_00422E00
- Owner plus one pointer; `ret 4`.
- Non-null input is inserted/assigned into container rooted at owner `+0x60C` through `0x00B93D20`.
- Resets owner fields `+0x614,+0x618,+0x61C,+0x620` to zero.

## FUN_00422E40
- Large diagnostic/dump routine using logger `0x01198F40` and context `0x01652E00`.
- Logs owner scalar `+0x2C`, timing delta `(+0x834 - +0x830)`, and floats `+0x840/+0x844`.
- Iterates global collection `0x016EF020` count `0x016F0024`, logging element fields including `+0x14` and a scaled numeric value from `+0x08`.
- Iterates owner collection count `+0x62C`, table beginning `+0x630`; follows child interfaces, filters runtime type token/global `0x016A0CE4`, logs qualifying IDs/values, invokes a virtual release/operation with argument 1, and accumulates qualifying count for summary output.
- Additional second traversal continues the diagnostic accounting before epilogue at `0x0042313E`; a short tail/helper follows through `0x00423161`.
- No semantic logger labels are assigned without decoding the referenced literal strings.
