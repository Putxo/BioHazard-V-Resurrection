# FUN_00402E70 evidence

- VA: `0x00402E70`
- RVA: `0x00002E70`
- Size: `336` bytes in the ordered candidate inventory, ending before `0x00402FC0`.
- Calling convention: `thiscall`; `ecx` is the object and `[esp+0x118]` is the integer filter argument after stack allocation.

## Instruction evidence

- The state word at `this + 0x2C` selects the main path. State `2` releases the selected value at `this + 0x50`, advances the linked node at `this + 0x4C` to its `+0x08` next pointer, decrements state, and returns false.
- State `0` seeds `this + 0x4C` from global list head `0x01657F20`. With a negative filter it skips list entries while byte two of the entry name is not greater than ASCII `'0'`, then sets state to `1`.
- State `1` returns true when the current node pointer is null.
- For non-negative filters, byte two of the node name must match `filter + '0'`; otherwise the current node advances to next and the function returns false.
- The accepted node name is copied to a stack buffer; bytes two, three, and four are decoded as a three-digit decimal value and stored at `this + 0x50`.
- The selected value is formatted with literal `0x0137AAF4`, passed to `FUN_00402770`, state increments, and the function returns false.

## Host reconstruction

The recovered host form exposes the global list, release hooks, and label submission through `FUN_00402E70_Services`. It preserves the state transitions, linked-list advancement, digit parsing, selected-value storage, and boolean return contract while leaving the external engine routines injectable.
