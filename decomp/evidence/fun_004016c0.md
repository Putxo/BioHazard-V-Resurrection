# FUN_004016C0 evidence

- Target: RE5DX9 1.1.0 Dev Release, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- VA: `0x004016C0`
- RVA: `0x000016C0`
- Size: `0x6C` / 108 bytes (`0x004016C0` through `0x0040172B`)
- Boundary evidence: previous function returns at `0x004016BA`; `0x004016BB..0x004016BF` are `INT3`; this function returns at `0x0040172B`; `0x0040172C..0x0040172F` are `INT3` before the next function at `0x00401730`.

## Bounds and failure path

The sole argument is loaded from the original stack slot and compared against `0x47` (71). Values `>= 71` call helper `0x01198FE0` with context `0x01652E00`, message token `0x0137A768`, the supplied index and limit `0x47`, then return false.

## Steam interface path

For an in-range index the function calls IAT slot `0x013714CC` (`steam_api.dll!SteamUser`) and intentionally ignores its return value, then calls IAT slot `0x013714C8` (`steam_api.dll!SteamUserStats`). A null stats pointer returns false.

The non-null path performs three calls on ISteamUserStats:

1. vtable slot `+0x3C` with the achievement index; return value ignored (`GetAchievementName(uint32)`);
2. vtable slot `+0x1C` with the API-name pointer loaded from `0x015671B8 + index*4` (`SetAchievement(const char*)`);
3. when the set succeeds, vtable slot `+0x28` with no arguments (`StoreStats()`), whose boolean result becomes the function result.

The achievement-name pointer table is the same 71-entry table recovered for `FUN_00401670`, so the host reconstruction reuses that exact table instead of duplicating it.

Module/subsystem remain `UNKNOWN`; Steam achievement behavior is proven but stronger game-level ownership is not.
