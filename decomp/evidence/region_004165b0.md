# Ordered decompilation evidence — 0x004165B0 through 0x00416BC0

## FUN_004165B0
- `thiscall`, two args, validates player object and requires player state `+0x2ED8==0`.
- Classifies second arg through sibling `0x0040AF40`; classification 1 triggers global per-slot `0x0041BD20`.
- Maps second arg through `0x004E5F00`; nonnegative result forwarded with player slot to `FUN_00410550(...,1,slot,mapped)` and returned.

## FUN_00416630
- `thiscall`, two args; event/object classification wrapper.
- Resolves player-compatible object through descriptor chain token `0x016AF0F8`, validates state and slot.
- For event descriptor high byte `0x07`, extracts 16-bit ID, requires `0x0040AF40(id)` and `FUN_0040AFC0(other)`; then calls global per-slot `0x0041BD80` and sibling `FUN_00410450` using event field `+0x19C4-1`.
- Additional type-token combinations on source/other object dispatch global codes `0x25` or `0x26` through `0x004E9D00`.

## FUN_004167A0 (entry inside next large block)
- Large event scoring/flag-construction routine.
- One-shot guards source byte `+0x19CC`; derives classification through `0x0040F7C0`, global mapping `0x004E84F0`, and source flags/types.
- Resolves target player slot and classification via `FUN_0040B100`; applies many object-type tests (`0x01696564`, `0x01695E08`, `0x01696184`, `0x016A7EB4`, `0x016B02A4`, etc.) and source counters `+0x19C8` to build a compact bitfield (bits observed `0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80`).
- Uses descriptor tables/jump maps and special current-user IDs to refine the record.
- Final compact record is dispatched through global service `0x016E03CC` (`0x004A08B0` on synchronized path; alternate path follows) based on global `+0x574/+0x578` equality.
- Body reaches next aligned candidate `0x00416BC0`.

No VERIFIED/MATCHED claim.