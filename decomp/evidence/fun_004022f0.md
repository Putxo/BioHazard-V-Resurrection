# FUN_004022F0 evidence

- VA: `0x004022F0`
- RVA: `0x000022F0`
- Candidate extent used by the frozen ordered inventory: 96 bytes, ending immediately before `0x00402350`.
- Exact target: Resident Evil 5 PC 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Recovered behavior

The function compares the caller-supplied string against one of two sources:

- when the object pointer is non-null, the compared string is the string field observed at object offset `+0x08` in the x86 target;
- when the object pointer is null, the function compares only one input byte against the literal byte addressed at `0x0137AA5C`.

The return follows the observed strcmp-like equality/sign contract: zero for equality and signed nonzero ordering for mismatch. The null-object branch is intentionally modeled as a one-byte comparison rather than a full string comparison.

The concrete character stored at `0x0137AA5C` is not promoted to a semantic name in source; host tests inject that byte through services. Module/subsystem remain conservative.
