# Ordered decompilation evidence — 0x00419990 through 0x00419FA0

## FUN_00419990
- `thiscall`; central per-frame coordinator.
- Calls `0x00411990`, `FUN_0040F500`, readiness `FUN_0040ED70`; handles root flags `0x10000/0x4000`, global transition predicate `FUN_0040E0B0`, mode-specific sibling dispatchers and pending owner request fields.
- Runs `FUN_004190D0`; if global predicate `0x004094E0` true runs `FUN_00419510`; then owner recompute `0x00415E90`, central updater `FUN_0040E930`, sibling `0x0040A670`, and `FUN_0040F020(false)`.

## FUN_00419AC0
- `thiscall`; large state machine on owner byte `+0x06` up to `0x0D`, stack-aligned.
- Initial state finalizes UI/global services, promotes owner `+0x60` to `+0x64`, resets selection, and if global state `+0x18==2` delegates `FUN_0040D540`; otherwise broadcasts owner ID through `FUN_0040FBD0` when enabled.
- Requests root `0x80000000`, then tries table-driven transitions `FUN_00413C60`, `FUN_0040D310`, `FUN_0040D370`.
- Depending on owner metadata bytes `+0xA8..+0xAB`, applies transforms to up to two players using geometry/data services (`0x0056A310`, `0x0056A090`) or owner vectors `+0xB0/+0xC0`, then invokes player vtable slot `+0x160`.
- Uses owner `+0x50` to select `FUN_00418540`, special ID `+0x64==0x19` clears flag groups and advances; otherwise jumps substate to 10.
- Later states react to root `0x400`, drive global selector 9 (`0x004A1E60/0x004A1970/0x00495640`), reset UI/timer/flag groups, and continue session transition through boundary near `0x00419F5C`.

Next ordered candidate `0x00419FA0`. No VERIFIED/MATCHED claim.