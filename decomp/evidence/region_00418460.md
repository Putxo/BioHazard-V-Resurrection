# Ordered decompilation evidence — 0x00418460 through 0x00418A90

## FUN_00418460
- `thiscall`; four-state dispatcher on owner byte `+0x05`.
- Delegates states to `FUN_00414BF0`, `FUN_00414D60`, `FUN_00414E60`, `FUN_0040DFF0`; after each delegates/tail-calls central updater `FUN_0040E930`.

## FUN_004184C0
- `thiscall`; five-state dispatcher on owner byte `+0x05`.
- Exact tail jumps to `0x00415130`, `FUN_00415330`, `0x0040A3A0`, `0x00415460`, `FUN_0040E410`; out-of-range returns.

## FUN_00418500 (entry after jump table)
- Checks global byte `+0x56C` and synchronization fields; unless owner already state 10, calls `FUN_00415650` and dispatches global `0x004A0100(...,5,3)`.

## FUN_00418540
- `thiscall`, one arg; broad current-user/session setup routine with debug logging strings `0x0137E86C/0x0137E840`.
- Disabled for global modes `1/2/5`, global state 2, byte `+0x798`, and most low-root-flag cases.
- Normalizes caller mode, ensures global user service ready, lazily allocates owner buffer `+0x130` of size `0x5940` via `0x01199490` and constructor `0x0041C0F0`, copies current-user structure into it through `0x00ABF820`.
- For global mode 0, reconciles current-user progression fields and calls `0x0041B260/0x0041B2B0`; refreshes global service `0x004E5180`.
- Depending on caller arg and player predicates, iterates two global handles, opening each, calls `0x004098D0`, finalizes; otherwise may invoke `0x0040A080`.

## FUN_00418790
- `thiscall`, one operation arg; large commit/save/statistics routine disabled modes `1/2/5`.
- Requires owner buffer `+0x130`; copies many current-user values/floats into temporary state and invokes `0x00ABF820`.
- For operation 1 reads synchronized owner value `+0x42C`, forwards through `0x0041B9A0`, iterates player/stat blocks, then writes float/stat values back into current-user structures under synchronization.
- Additional operation paths continue to boundary `0x00418A80`.

## FUN_00418A80
- Tiny `ret` no-op.

## FUN_00418A90
- Next large routine begins; continuation next.

No VERIFIED/MATCHED claim.