# FUN_00402560 evidence

- VA: `0x00402560`
- RVA: `0x00002560`
- Candidate extent used by the frozen ordered inventory: 112 bytes, ending immediately before `0x004025D0`.
- Exact target: Resident Evil 5 PC 1.1.0 Dev, SHA-256 `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`.

## Recovered behavior

The function walks a node graph. It recurses through the child pointer at offset `+0x0C`, skips nodes whose flag byte at `+0x1F` has bit 0 set, and otherwise increments a count while dispatching an external formatting/logging call in the original target.

Sibling traversal through offset `+0x08` is active only when the incoming depth argument is non-zero, matching the observed `test ebp, ebp` guard.
