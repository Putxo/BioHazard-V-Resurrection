# Ordered decompilation evidence — 0x00417A60 through 0x00417E50

## FUN_00417A60
- `thiscall`, bool return; high-level dispatcher for owner states 0..4.
- Rejects/returns complete when root flags `0x400/0x100` active or owner state out of range.
- Dispatches owner state to previously recovered routines: `FUN_0040C330`, `FUN_0040C4E0`, `FUN_0040C680`, `FUN_0040CA90`, `FUN_004123F0`.
- If owner byte `+0x364` becomes set, calls `0x004098B0`, forces owner state `0x0C`, returns true; otherwise calls debug HUD `FUN_00411AB0` with literal `0x0137E838`, returns false.

## FUN_00417B30
- `thiscall`; decrements owner byte `+0x06`; when it reaches zero clears owner `+0x50`, sets UI vtable value, forces owner state 2, tail-jumps to `FUN_00412920`.

## FUN_00417B70
- `thiscall`; state machine on owner byte `+0x05` up to `0x0B`.
- Initial state requests root `0x800`, `0xFFE02000`, `0x8000`, resets service/player manager; if root flag `0x40` set zeros owner float `+0xE0`; clears owner command `+0xD4`.
- Branches on owner command/type `+0x4C`, choosing UI service/timer setup via `0x004B6570/0x004B6590`, `0x0040AC10/0x0040AC80`; waits UI readiness, resets vtable and global service, sends owner `+0x44` through `0x0041BE80`.
- Uses central updater `FUN_0040E930` and `FUN_00412BF0`; later clears root flags `0x4000`, `0x08000000`, `1`, `0x40000`, conditionally re-requests `0x40000`, sends multiple service selectors, updates global state `+0x24`, forwards owner `+0x44/+0x48` through `0x0041BA90/0x0041BAE0`, and invokes global vtable callback token `0x01657DFC`.

## FUN_00417E50
- Next large state machine begins; continuation handled next.

No VERIFIED/MATCHED claim.