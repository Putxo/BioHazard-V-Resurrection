# FUN_00406010 evidence

- VA: `0x00406010`
- RVA: `0x00006010`
- Executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`
- Recovered body size: `467` bytes (`0x00406010..0x004061E2` inclusive). `0x004061E3..0x004061EF` are `INT3` padding; the next assigned baseline candidate starts at `0x004061F0`.
- Calling convention: `cdecl`; the body consumes three stack arguments and returns with a plain `ret`.
- GNU `objdump` and LLVM `llvm-objdump` independently agree on the instruction boundaries and branch targets below.

## Argument and caller evidence

The three stack arguments are used as a relative/base directory, a resource-type key, and an extension token.

- At `0x00406016`, the second stack argument is loaded and passed with table/global `0x01652EF0` to `0x01199D40`; a null return is tested at `0x0040602B` and jumps directly to the epilogue at `0x004061DC`.
- After the saved `EBX`, `0x0040605A` loads the first stack argument. It is combined with the active global root name through format literal `0x0137AB7C = "%s\\%s\\*"`.
- The third stack argument is propagated by the recursive call at `0x00406159` and is also passed with each leaf path to helper `0x012519D0`.
- Direct caller sites `0x00406A6A`, `0x00406A81`, and `0x00406A98` pass the same formatted stage directory with the observable pairs `rLayout`/`.lot`, `rScenario`/`.sce`, and `rAIFSM`/`.fsm` respectively. These literals are at `0x0137CCC8`, `0x0137CCD0`, `0x0137CCB4`, `0x0137CCC0`, `0x0137CCA4`, and `0x0137CCAC`.

## Directory traversal evidence

- `0x00406036` constructs the local scan object. The first `0x0119B790` call at `0x004060BB` receives the formatted wildcard; the loop repeats through `0x004061BF` and exits when the boolean return is clear.
- Entry names come from the local scan record at `0x004060D0`; null names fall back to `0x0137AA5C`, the empty string.
- `0x004060E0..0x00406106` compares entry names with `0x0137AB48 = "."` and `0x0137AB44 = ".."`, skipping both pseudo-directories.
- `0x0040610C..0x00406122` formats child paths with `0x0137AB3C = "%s\\%s"`. Negative formatting results enter the path-error reporting sequence using `0x0137AB4C = "PATH ERROR in aConvertBase::convertDir(%s, %s)"`.
- `0x0040613D` tests bit 0 of the scan-entry flag. When set, `0x0040614C..0x00406159` recursively calls `FUN_00406010(child_path, key, extension)`.

## Leaf conversion and registration evidence

- For non-directory entries, `0x00406163..0x00406168` calls `0x012519D0(child_path, extension)`.
- Independent disassembly of `0x012519D0` shows a two-string substring search: it reads the first byte(s) of the second argument and scans the first argument, returning a pointer into the first string or null. The caller tests the result at `0x00406170`; when non-null, `0x00406178` writes `0` to the returned address. This truncates the path at the first occurrence of the requested extension token.
- `0x0040617B..0x00406189` passes the opened type root, the stripped path, and literal `1` to the global engine object at `0x016E1D3C`; the returned object is saved at `0x00406195`.
- Registration is suppressed independently by bytes `0x016B1F39` and `0x016B1F3B` (`0x0040618E..0x004061A2`). Only when both are zero and the resolved object is non-null does `0x004061A8..0x004061B3` forward its address through global `0x016B1F3C` to `0x005477E0`.
- `0x004061D2..0x004061D6` destroys the scan object before the shared epilogue.

## Host reconstruction

The host form exposes the resource-type opener, recursive directory scanner, path-error callback, leaf resolver, and final registration sink through `FUN_00406010_Services`. It preserves the observable null-open early return, wildcard and child-path construction, `.`/`..` filtering, bit-0 directory recursion, extension-substring truncation, resolve/create call, two suppression gates, and non-null registration behavior. The tests cover the null-open early exit, normal recursive traversal, extension filtering/truncation, and suppressed-registration path.
