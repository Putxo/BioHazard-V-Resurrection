# Sequential decomp evidence — 0x00401000 region

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

Source evidence: full `.text` linear-disassembly data supplied for the exact RE5DX9 1.1.0 Dev target, cross-checked against PE imports and raw target bytes. This file records only claims supported by those bytes; semantic names remain provisional/blank.

## FUN_00401000

- VA `0x00401000`, RVA `0x00001000`
- body bytes: `E9 57 04 E5 00`
- instruction: `jmp 0x0125145C`
- recovered as aligned-release wrapper.

## FUN_00401010

- VA `0x00401010`, RVA `0x00001010`, size `0xD5`.
- wide format: `L"%S <Build:%S> [ %S ] %S %S"`.
- embedded fragments: `3170`, `RELEASE`, `Thu Apr 16, 2015 06:51:32 PM`, `(CHEATS)`.
- optional path calls `FindWindowW(L"MTFramework", nullptr)` then `SetWindowTextW`.
- deterministic formatting covered by unit test.

## FUN_004010F0

- VA `0x004010F0`, RVA `0x000010F0`, size `0xB2`, `ret 0x10`.
- four callee-cleaned stack arguments are proven by the epilogue.
- allocates/probes a `0x2790`-byte stack frame via helper `0x012514A0`.
- constructs/initializes a local object at stack+`0x210` through `0x00CF50F0`.
- calls `FUN_00401010` with a local `0x104`-WCHAR destination, literal/input at `0x0137A078`, and false for the optional window-title side effect.
- reads the four incoming arguments back from stack offsets after the large frame is established and combines them with globals `0x01567000` and `0x0165A090` before calling `0x00CF59B0`.
- branch at `0x00401153` depends on the boolean return from `0x00CF59B0`.
- false branch: destroys local state with `0x00CF57A0`, returns `1`.
- true branch: uses global `0x0165B9B0`, calls `0x004D4BC0`, then `0x00CF2D10`, then `0x00CF57A0`, and returns the value preserved from `0x00CF2D10`.
- exact semantic types and the contract of `0x00CF59B0`/`0x00CF2D10` are not yet established. It remains **DISASSEMBLED** rather than introducing opaque guessed source.

## FUN_004011B0

- VA `0x004011B0`, size `0x32`, `ret 0x10`.
- four arguments map to first pointer, stride, count, callback.
- invokes callback with the current element in ECX and advances by stride.

## FUN_004011F0

- VA `0x004011F0`, size `6`.
- bytes `B8 00 2E 65 01 C3` -> returns `0x01652E00`.

## FUN_00401200

- VA `0x00401200`, size `0x10`.
- 16-byte aligned allocation wrapper.

## FUN_00401210

- VA `0x00401210`, size `0x0C`.
- forwards second argument to `KERNEL32!OutputDebugStringA`.

## FUN_00401220

- VA `0x00401220`, size `3`: `xor al,al; ret`.

## FUN_00401230 / FUN_00401240

- both are `ret 4` no-op callbacks.

## FUN_00401250 / FUN_00401260

- each body is exactly `FF 25 FC 14 37 01`.
- this is an indirect tail jump through IAT slot `0x013714FC`.
- PE import mapping resolves that slot to `steam_api!SteamAPI_RunCallbacks`.
- both are recovered as source wrappers around the platform import adapter.
- on Win32 the adapter calls the real imported Steam API symbol; non-Windows CI cannot load the target DLL and uses an inert host adapter only for build/test portability.
- status after CI acceptance may be promoted to **FAST_PASS**; this does not imply VERIFIED or MATCHED.

## FUN_00401270

- VA `0x00401270`, size `0x79`.
- uses global `0x0165B6E0`, stack object `0x258` bytes and a `0x46`-iteration initialization loop calling `0x0127174E` and `0x004E8340`.
- semantics unresolved; next sequential target after the Steam thunks.

## Acceptance rule

Only machine bodies with sufficiently constrained behavior receive source. Ambiguous functions remain DISASSEMBLED with evidence instead of being replaced by guessed implementations.
