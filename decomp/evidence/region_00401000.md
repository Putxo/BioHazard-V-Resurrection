# Sequential decomp evidence — 0x00401000 region

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

Source evidence: full `.text` linear-disassembly data supplied for the exact RE5DX9 1.1.0 Dev target, cross-checked against PE imports and raw target bytes. This file records only claims supported by those bytes; semantic names remain provisional/blank.

## FUN_00401000

- VA `0x00401000`, RVA `0x00001000`
- body bytes: `E9 57 04 E5 00`
- instruction: `jmp 0x0125145C`
- `0x0125145C` checks/null-normalizes an aligned pointer, loads the stored base pointer at `[aligned_base-4]` and calls the target CRT free routine.
- recovered as an aligned-release wrapper.

## FUN_00401010

- VA `0x00401010`, body ends at `0x004010E4` (`ret`), size `0xD5`.
- Builds local string fragments from `.rdata`, calls `0x01251484`, and conditionally calls IAT entries:
  - `0x01371384` = `USER32!FindWindowW`
  - `0x01371380` = `USER32!SetWindowTextW`
- The exact high-level parameter/formatting contract is not yet proven, so this function is **DISASSEMBLED**, not FAST_PASS.

## FUN_004010F0

- VA `0x004010F0`, body ends at `0x004011A1`, size `0xB2`.
- Large stack frame (`0x2790`), calls `FUN_00401010`, `0x00CF50F0`, `0x00CF59B0`, `0x00CF57A0`, `0x004D4BC0`, `0x00CF2D10`.
- `ret 0x10` proves four callee-cleaned stack arguments.
- Exact object/argument semantics remain unresolved; kept **DISASSEMBLED**.

## FUN_004011B0

- VA `0x004011B0`, size `0x32`.
- `ret 0x10`: four callee-cleaned stack arguments.
- Argument mapping from stack/register flow:
  1. first element pointer -> `ESI`
  2. stride -> `EBP`
  3. count -> `EDI`
  4. callback -> `EBX`
- loop sets `ECX=ESI`, calls callback, adds stride, decrements count.
- recovered with provisional portable callback type; original ECX-callback ABI remains documented.

## FUN_004011F0

- VA `0x004011F0`, size `6`.
- bytes: `B8 00 2E 65 01 C3`
- exact machine behavior: return `0x01652E00` in `EAX`.

## FUN_00401200

- VA `0x00401200`, size `0x10`.
- forwards `(size, 0x10)` to `0x01251471`.
- `0x01251471` forwards `(size, alignment, 0)` to target aligned-allocation code `0x012513C3`.
- recovered as 16-byte aligned allocation.

## FUN_00401210

- VA `0x00401210`, size `0x0C`.
- loads second stack argument and calls IAT `0x01371120`.
- IAT mapping: `KERNEL32!OutputDebugStringA`.
- first stack argument is untouched by this machine body.

## FUN_00401220

- VA `0x00401220`, size `3`.
- bytes: `32 C0 C3` -> `AL=0`, return false.

## FUN_00401230 / FUN_00401240

- VAs `0x00401230`, `0x00401240`.
- each is `C2 04 00` -> no-op, callee pops one 4-byte argument.

## FUN_00401250 / FUN_00401260

- each is `FF 25 FC 14 37 01` -> tail jump through IAT `0x013714FC`.
- IAT mapping: `steam_api!SteamAPI_RunCallbacks`.
- retained **DISASSEMBLED** until the platform/import layer has an explicit Steam callback binding; no fake no-op implementation is allowed.

## FUN_00401270

- VA `0x00401270`, body ends at `0x004012E8`, size `0x79`.
- uses global `0x0165B6E0`, stack object of `0x258` bytes, and loops `0x46` times while calling `0x0127174E` and `0x004E8340`.
- exact object/global semantics unresolved; retained **DISASSEMBLED** for the next sequential pass.

## Acceptance status for this batch

FAST_PASS source is supplied only for machine bodies whose behavior is sufficiently constrained by the instruction stream and import/CRT target evidence. Complex bodies and Steam thunks stay DISASSEMBLED rather than being replaced with dummy implementations.
