# Sequential decomp evidence — 0x00401000 region

Target SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`

Source evidence: full `.text` linear-disassembly data supplied for the exact RE5DX9 1.1.0 Dev target, cross-checked against PE imports and raw target bytes. This file records only claims supported by those bytes; semantic names remain provisional/blank.

## FUN_00401000

- VA `0x00401000`, RVA `0x00001000`
- body bytes: `E9 57 04 E5 00`
- instruction: `jmp 0x0125145C`
- recovered as aligned-release wrapper.

## FUN_00401010

- VA `0x00401010`, RVA `0x00001010`, size `0xD5`.
- wide format: `L"%S <Build:%S> [ %S ] %S %S"`.
- embedded fragments: `3170`, `RELEASE`, `Thu Apr 16, 2015 06:51:32 PM`, `(CHEATS)`.
- optional path calls `FindWindowW(L"MTFramework", nullptr)` then `SetWindowTextW`.
- deterministic formatting covered by unit test.

## FUN_004010F0

- VA `0x004010F0`, RVA `0x000010F0`, size `0xB2`, `ret 0x10`.
- four callee-cleaned stack arguments are proven by the epilogue.
- allocates/probes a `0x2790`-byte stack frame via helper `0x012514A0`.
- constructs/initializes a local object at stack+`0x210` through `0x00CF50F0`.
- calls `FUN_00401010` with a local `0x104`-WCHAR destination, literal/input at `0x0137A078`, and false for the optional window-title side effect.
- reads the four incoming arguments back from stack offsets after the large frame is established and combines them with globals `0x01567000` and `0x0165A090` before calling `0x00CF59B0`.
- branch at `0x00401153` depends on the boolean return from `0x00CF59B0`.
- false branch: destroys local state with `0x00CF57A0`, returns `1`.
- true branch: uses global `0x0165B9B0`, calls `0x004D4BC0`, then `0x00CF2D10`, then `0x00CF57A0`, and returns the value preserved from `0x00CF2D10`.
- exact semantic types and the contract of `0x00CF59B0`/`0x00CF2D10` are not yet established. It remains **DISASSEMBLED** rather than introducing opaque guessed source.

## FUN_004011B0

- VA `0x004011B0`, size `0x32`, `ret 0x10`.
- four arguments map to first pointer, stride, count, callback.
- invokes callback with the current element in ECX and advances by stride.

## FUN_004011F0

- VA `0x004011F0`, size `6`.
- bytes `B8 00 2E 65 01 C3` -> returns `0x01652E00`.

## FUN_00401200

- VA `0x00401200`, size `0x10`.
- 16-byte aligned allocation wrapper.

## FUN_00401210

- VA `0x00401210`, size `0x0C`.
- forwards second argument to `KERNEL32!OutputDebugStringA`.

## FUN_00401220

- VA `0x00401220`, size `3`: `xor al,al; ret`.

## FUN_00401230 / FUN_00401240

- both are `ret 4` no-op callbacks.

## FUN_00401250 / FUN_00401260

- each body is exactly `FF 25 FC 14 37 01`.
- this is an indirect tail jump through IAT slot `0x013714FC`.
- PE import mapping resolves that slot to `steam_api!SteamAPI_RunCallbacks`.
- both are recovered as source wrappers around the platform import adapter.
- on Win32 the adapter calls the real imported Steam API symbol; non-Windows CI cannot load the target DLL and uses an inert host adapter only for build/test portability.
- status after CI acceptance may be promoted to **FAST_PASS**; this does not imply VERIFIED or MATCHED.

## FUN_00401270

- VA `0x00401270`, RVA `0x00001270`, exact size `0x79` (121 bytes); body ends at `0x004012E8` and is followed by `INT3` padding.
- no direct `CALL` target to this entry was found in the complete linear-disassembly dataset; its entry remains independently bounded by alignment and padding.
- calls `0x0052FE10` on the object stored at global `0x0165B6E0` before the platform operations. The object/class name remains `UNKNOWN`.
- reserves exactly `0x258` stack bytes. The first `0x28` bytes match the fields used from an `XUSER_SIGNIN_INFO`: `signin_state` is read at offset `0x0C` and `user_name` is passed from offset `0x18`.
- tail stub `0x01271754` reads IAT slot `0x013715DC`; the PE import table identifies it as `xlive.dll` ordinal 5267, `XUserGetSigninInfo`.
- calls `XUserGetSigninInfo(0, 1, &signin_info)`. If the return is success and `signin_state != 2`, it emits the exact embedded format `User %s is not logged into LIVE.` with `user_name`.
- the remaining `0x230` stack bytes hold exactly 70 eight-byte achievement records. The loop writes `{user_index=0, achievement_id=i}` for every `i` from 0 through 69.
- tail stub `0x0127174E` reads IAT slot `0x013715E0`; the PE import table identifies it as `xlive.dll` ordinal 5278, `XUserWriteAchievements`.
- each iteration calls `XUserWriteAchievements(1, &achievement, nullptr)` and ignores its return value, then calls `0x004E8340` on the object stored at global `0x0165BBB0` with the same achievement ID. This second object's class and method name remain `UNKNOWN`.
- recovered source preserves the original no-argument entry and models the two unknown global objects plus xlive/logger operations through an injectable services table. Tests verify preparation, sign-in arguments, warning condition, all 70 record values, null overlapped pointer, and all 70 mirrored IDs.
- status is **FAST_PASS** after local MSVC compilation and metadata validation; local test execution is blocked by Device Guard and is deferred to CI. `LINKED` remains false until the original global-object bindings are integrated.

## FUN_004012F0

- VA `0x004012F0`, RVA `0x000012F0`, exact size `0xB7` (183 bytes); body ends at `0x004013A6` and is followed by `INT3` padding.
- the complete linear-sweep candidate table contains a high-confidence direct-CALL target row for this entry. The direct caller at `0x00401D19` pushes `arg3=0`, `arg2=0x46`, and `arg1=ESI`, then preserves the returned count for later use.
- the epilogue is `ret 0x0C`, proving three callee-cleaned 32-bit stack arguments. The recovered contract is `FUN_004012F0(void** out_buffer, uint32_t requested_count, uint32_t unused)`; the third argument is not read by the target body.
- if `requested_count` is zero, the target substitutes `0xFF` before creating the enumerator.
- tail stub `0x01271754` reads IAT slot `0x013715DC`; the PE import table identifies it as `xlive.dll` ordinal 5267, `XUserGetSigninInfo`.
- tail stub `0x01271766` reads IAT slot `0x013715D0`; the PE import table identifies it as `xlive.dll` ordinal 5280, `XUserCreateAchievementEnumerator`.
- tail stub `0x01271760` reads IAT slot `0x013715D4`; the PE import table identifies it as `xlive.dll` ordinal 5256, `XEnumerate`.
- tail stub `0x0127175A` reads IAT slot `0x013715D8`; the PE import table identifies it as `xlive.dll` ordinal 5251, `XCloseHandle`.
- calls `XUserGetSigninInfo(0, 1, &signin_info)` and ignores the result and returned structure.
- calls `XUserCreateAchievementEnumerator(0x434307F7, 0, 0, 0xFFFFFFFF, 0, requested_count, &buffer_size, &handle)` and ignores the return value.
- allocates the enumerator output buffer with target helper `0x01251471` using `(buffer_size, 16)`, preserving the original 16-byte alignment requirement.
- calls `XEnumerate(handle, buffer, buffer_size, &requested_count, nullptr)`, ignores its return value, stores `buffer` through `out_buffer`, and returns the final `requested_count`.
- closes the enumeration handle only when it is neither `0` nor `0xFFFFFFFF`, exactly matching the two no-close branches in the target.
- recovered source models the xlive calls through an injectable services table while preserving constants, argument order, return propagation, buffer ownership, and close-handle rules. Tests cover explicit count, default count, output-buffer alignment, final count propagation, and the `handle == 0` no-close branch.
- status is **FAST_PASS** after local MSVC compilation and metadata validation; local test execution is blocked by Device Guard and is deferred to CI. `LINKED` remains false until the original xlive runtime binding is integrated.

## FUN_00401400

- VA `0x00401400`, RVA `0x00001400`, exact size `0x0A` (10 bytes); body ends at `0x00401409` and is followed by `INT3` padding.
- the complete linear-sweep candidate table contains a high-confidence direct-CALL target row for this entry: RVA `5120`, VA `4199424`, evidence `direct CALL target (linear sweep)`.
- direct caller evidence: linear-disassembly row `4643290,8837594,5,e8213ab9ff,call,0x401400,5120`, i.e. caller VA `0x0086D9DA`.
- exact body:
  - `mov eax, dword ptr [ecx + 4]`
  - `cmp eax, dword ptr [ecx + 8]`
  - `sete al`
  - `ret`
- the use of `ECX` with no stack adjustment proves a no-stack-argument `thiscall` helper. It reads two 32-bit object fields at offsets `0x04` and `0x08`.
- `sete al` writes only the low byte, but the C++ boolean return convention consumes `AL`; recovered source therefore exposes the helper as `bool FUN_00401400(const FUN_00401400_Object* self)`.
- recovered source intentionally adds no null-object guard because the original body dereferences `ECX` immediately. Tests cover equal and unequal field values.
- status is **FAST_PASS** after local source recovery. `LINKED`, `MATCHED`, and semantic class names remain false/unknown until surrounding caller ownership is mapped.

## Acceptance rule

Only machine bodies with sufficiently constrained behavior receive source. Ambiguous functions remain DISASSEMBLED with evidence instead of being replaced by guessed implementations.
