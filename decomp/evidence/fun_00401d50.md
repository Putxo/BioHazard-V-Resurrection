# FUN_00401D50 evidence

## Candidate

- RVA: `0x00001D50`
- VA: `0x00401D50`
- Size: `224` bytes through alignment before next candidate `0x00401E30`.
- Candidate source: V6 ordered candidate after `FUN_00401D10`.

## Disassembly summary

This function is a constructor/initializer for a `0x4C`-byte object allocated by callers. The direct callers allocate `0x4C` bytes and tail-call or call this constructor with `ECX = allocated_object`.

Representative callers:

```asm
00401E3B  push 0x4C
00401E3D  call 0x01251471
00401E43  test eax, eax
00401E47  mov  ecx, eax
00401E49  jmp  0x00401D50

00401F76  push 0x4C
00401F78  call 0x01251471
00401F80  mov  ecx, eax
00401F86  call 0x00401D50
```

## Key body facts

```asm
00401D5C  mov dword ptr [esi], ebx              ; first field = 0
00401D63  lea ecx, [esi + 0x10]
00401D69  mov byte ptr [ecx + 4], bl
00401D70  mov dword ptr [ecx], 0x0137A8DC       ; empty string
00401D76  mov dword ptr [ecx + 0x0C], esi
00401D79  mov dword ptr [ecx + 0x10], 0x00401A70
00401D87  push 0x00401A70
00401D8C  push esi
00401D8D  call 0x00402020
...
00401D91  lea ecx, [esi + 0x24]
00401DA6  mov dword ptr [ecx + 0x10], 0x00401230
00401DB8  call 0x00402060
...
00401DBF  lea ecx, [esi + 0x38]
00401DD4  mov dword ptr [ecx + 0x10], 0x00401240
00401DE3  call 0x004020A0
...
00401DF8  mov dword ptr [0x01655B58], esi
...
00401E10  call dword ptr [0x013714C8]           ; SteamUserStats
00401E16  call vtable slot +0x00
00401E19  mov dword ptr [esi + 4], ebx
00401E1C  mov dword ptr [esi + 8], 0xFFFFFFFF
00401E23  mov eax, esi
00401E28  ret
```

## Recovered behavior

- Clears the object's first field.
- Initializes three callback slots at object offsets `+0x10`, `+0x24`, and `+0x38`.
- The three callback addresses recorded in the object are `0x00401A70`, `0x00401230`, and `0x00401240`.
- Registers each non-null callback through three nearby helper calls.
- Publishes the constructed object to global `0x01655B58`.
- If SteamUserStats and SteamUser are available, and SteamUser slot `+0x04` reports true, calls SteamUserStats slot `+0x00`.
- Sets object counter `+4` to `0` and counter `+8` to `0xFFFFFFFF`.
- Returns `this`.
