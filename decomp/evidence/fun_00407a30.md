# FUN_00407A30 evidence

## Target

- Exact local reference executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`; size 19,977,216 bytes; PE32 x86 image base `0x00400000`.
- Function VA `0x00407A30`, RVA `0x00007A30`.
- Canonical candidate span is 64 bytes, `0x00407A30..0x00407A6F`, because the next independently aligned root starts at `0x00407A70`.
- Native instruction body is 57 bytes through the `ret` at `0x00407A68`; `0x00407A69..0x00407A6F` is `INT3` padding.
- Native body SHA-256: `ecd322ce6a0ca49a17a9702f0e4cd3486bfac7828af2a5bbafd9b8720495d251`.
- GNU objdump and LLVM-style x86 decoding agree on the instruction boundaries below.

## Instruction evidence

```text
00407A30  56                         push esi
00407A31  8B F1                      mov esi,ecx
00407A33  8B 46 28                   mov eax,[esi+28h]
00407A36  83 F8 02                   cmp eax,2
00407A39  76 1E                      jbe 00407A59h
00407A3B  83 F8 03                   cmp eax,3
00407A3E  75 27                      jne 00407A67h
00407A40  E8 0B F8 FF FF             call 00407250h
00407A45  8B 0D D0 1F 6E 01          mov ecx,[016E1FD0h]
00407A4B  8B 01                      mov eax,[ecx]
00407A4D  8B 50 24                   mov edx,[eax+24h]
00407A50  68 80 7F 65 01             push 01657F80h
00407A55  FF D2                      call edx
00407A57  5E                         pop esi
00407A58  C3                         ret
00407A59  68 08 DB 37 01             push 0137DB08h
00407A5E  E8 0D AD FF FF             call 00402770h
00407A63  83 46 28 01                add dword ptr [esi+28h],1
00407A67  5E                         pop esi
00407A68  C3                         ret
00407A69..00407A6F                    int3 padding
```

Exact 57-byte native body:

`56 8B F1 8B 46 28 83 F8 02 76 1E 83 F8 03 75 27 E8 0B F8 FF FF 8B 0D D0 1F 6E 01 8B 01 8B 50 24 68 80 7F 65 01 FF D2 5E C3 68 08 DB 37 01 E8 0D AD FF FF 83 46 28 01 5E C3`

ECX is preserved as the object pointer in ESI and no stack argument is read, establishing a no-stack-argument thiscall-style contract. The only object field accessed is dword `this+0x28`.

## Exact state behavior

- Unsigned states `0`, `1`, and `2`: push literal `0x0137DB08`, call `0x00402770`, increment dword `this+0x28` exactly once, then return.
- State `3`: call `0x00407250`; load global object pointer `[0x016E1FD0]`; resolve virtual slot `+0x24`; call that slot with single pushed token `0x01657F80`; return without changing `this+0x28`.
- Every unsigned state above `3`: return without side effects.
- The `jbe` at `0x00407A39` is unsigned, so the reconstruction uses `std::uint32_t`.

The literal at `0x0137DB08` decodes exactly as:

`CONVERT GAME RESOURCE ...`

No semantic name is assigned to global `0x016E1FD0`, its virtual function at slot `+0x24`, or token `0x01657F80`; the host interface preserves those facts as an opaque manager dispatch.

## Relationship evidence

`0x00407250` is itself a resource-registration batch that repeatedly forwards exact archive/table groups to the already recovered `FUN_004061F0`. That supports describing the state-3 first call as the game-resource conversion batch without inventing a class name.

A raw 32-bit pointer scan of the exact image finds `0x00407A30` stored at rdata VA `0x0137ADD8` among neighboring code pointers. No direct `CALL rel32 0x00407A30` was found in .text. This is consistent with an indirect/virtual entry and with ECX carrying the object pointer, but no concrete class identity is inferred.

## Host-only early exits

The native body has no services structure. The portable reconstruction adds harness-only safety exits for:
- null services;
- missing marker callback on states 0..2;
- a missing state-3 callback, checked before executing either half of the native state-3 pair.

Unit tests exercise those guards separately from native state behavior. They also verify all native state classes, exact marker text, exact state increment semantics, state-3 call order, and token `0x01657F80`.

## Inventory false-positive record: 0x004070E0

The earlier local V6 candidate row `FUN_004070E0` is intentionally not recovered. Direct decoding of the same exact executable shows that `0x004070E0` lies inside the instruction beginning at `0x004070DE` in the real function rooted at `0x004070C0`. It is therefore an inventory false positive, not a valid function boundary. No source, claim, or FAST_PASS entry is created for it.

## Scope

`FUN_00407A10`, `FUN_00407AB0`, and `FUN_00407AD0` are excluded parallel work and are not modified.
