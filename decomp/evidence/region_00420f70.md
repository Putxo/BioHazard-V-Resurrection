# Ordered decompilation evidence — 0x00420F70 through 0x00421600

## FUN_00420F70
- Scans 32 registry buckets in global `0x016E1690`, traversing linked objects and descriptor inheritance chains.
- First searches type token `0x016E3360`; for each match builds temporary descriptor (`0x0119F730`, virtual slot `+0x0C`), searches literal/token `0x0137F138`, and if present logs/dispatches via `0x0119CB00` and `0x00A60340`, setting a found flag.
- If none found, performs second scan for type token `0x016E3D88`.
- Third scan searches token `0x016E3AA0`; if absent across registry, allocates 0x60 object via global allocator, constructs `0x00E38BA0`, registers type 2 in global registry.
- Strong semantics: ensure/find required registry object/type, with diagnostic search.

## FUN_00421110
- Reflection/serialization metadata registration routine for the large subsystem object.
- Builds repeated property descriptors on stack and calls `0x00409750(registry,&descriptor)`.
- Registers fields including owner `+0x594`, embedded `+0x4B0`, `+0x290`, `+0x2BC`, dynamic record array around `+0x36C` with count `+0x368`, and numerous later fields, using literals `0x0137F208`, `0x0137F1F8`, `0x0137F1EC`, `0x0137F1DC`, `0x0137F1C8`, `0x0137F1B8`, etc.
- Body continues to aligned boundary `0x00421600`.

No VERIFIED/MATCHED claim.