# Ordered decompilation evidence — 0x004108F0 through 0x00410CD0

## FUN_004108F0
- Large no-stack-arg routine that ranks/groups active slots using per-slot accumulated value `global+0x6B4`.
- Number of considered slots is 2 or 4 depending on `0x00AC11D0`; when not in that mode only active bits from global mask `+0x478` are included.
- Builds temporary arrays of slot indices and scores, sorts them with deterministic tie-breaking, writes per-slot rank/group to `+0x6B8` and computed companion value from `0x00AC15A0` to `+0x6BC`.
- Reconciles selected/current slot `+0x5DC`, grouping `+0x4C0`, and per-user statistics via `0x00B7B050`, `0x00B7B810` and several `0x004E9470..97C0` refreshes.
- Finishes by resetting per-user structure (`0x00B7A310`) and tail-jumping to `0x00AC07B0` on current-user substructure.
- Strong semantics: rank/result computation and post-round statistics commit.

## FUN_00410CD0
- Large initialization/result routine.
- For current-user ID below `0x64`, seeds global per-slot values using RNG `0x0119F610`: random accumulator `+0x6B4`, random count `+0x690` (1..100), and half-range random/history `+0x69C`.
- Determines a mode selector from global mode, `0x00AC1240`, and byte `+0x79A`.
- Adds pending global amount `+0x68C` into `+0x6B4`, clears pending amount, maps values through `0x00AC15F0` and `0x00AC1650`, storing `+0x6B8/+0x6BC`.
- Then locks owner and iterates active player bits/handles starting at global `+0x490`, validating player services and performing result/statistics updates. Body continues through the next major boundary near `0x004110A0`.

No VERIFIED/MATCHED claim.