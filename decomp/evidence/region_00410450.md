# Ordered decompilation evidence — 0x00410450 through 0x00410810

## FUN_00410450
- `thiscall`, two stack args, `ret 0x08`; disabled when global byte `+0x798` set or second arg >=100.
- Resolves player using first arg, requires valid player and state `+0x2ED8==0`, finds entry ID `0x0E` in player collection `+0x22A0` via `0x00ACCB20`.
- Converts entry through `0x00409800`, maps via global `0x004E5F00`, then if player slot is eligible calls `0x004E4FA0(global_handle)` and `0x0041B800(mapped, second_arg)`, finalizing `0x004E5030`.

## FUN_00410550
- `thiscall`, three stack args, `ret 0x0C`, integer result.
- Same global/player validity gates; if player slot is not eligible returns 1, invalid/global-disabled returns 0.
- Under owner lock selects operation based on first operation arg: routes to `0x0041B7B0`, `0x0041B760` plus several `0x004E93D0..9450` refreshes, or `0x0041B860`; one path queries per-user collection `0x00B7A9D0` and returns result+1.
- Finalizes global handle through `0x004E5030` and returns computed integer.

## FUN_004106C0
- `thiscall`, three stack args, `ret 0x0C`; disabled by global byte `+0x798` or ineligible player slot.
- Under owner lock opens global handle `+0x490[player]`; operation selector 0 calls `0x0041B8B0(value)`, selector 1 calls `0x0041B900(value)`, then finalizes `0x004E5030`.

## FUN_00410770
- `stdcall`, `(slot, delta_float)`, `ret 0x08`.
- If per-slot byte `+0x694` active, decrements timer `+0x6A0` by delta unless global state `+0x678==1` with root flag `0x1000`.
- On expiry and when global `+0x574==+0x578`, calls `FUN_0040BAA0(slot)` and `0x004A0B60(global 0x016E03CC,slot)`; stores clamped timer.

## FUN_00410810
- `thiscall`, one player index, `ret 0x04`; substantial player cleanup.
- Resolves/validates player, calls earlier assignment/entry helpers `FUN_0040BD30` and `FUN_0040BDB0`.
- Invokes player vtable operations (`+0x10C`, `+0x38`) and `0x009BD1C0`; conditionally calls global `0x004749B0` for selected slot.
- Calls `0x009C1170`, removes player through `0x005237A0`, disables via `0x009B6D50(player,0)`.
- Clears owner per-player bytes `+0x450/+0x484/+0x4A8` and sets owner handle `+0x498[index] = -1`.

Next ordered candidate `0x004108F0`. No VERIFIED/MATCHED claim.