# Ordered decompilation evidence — 0x00411640 through 0x00411A90

## FUN_00411640
- `thiscall`, two stack args, `ret 0x08`; active only in global mode 1.
- Validates player and rejects state `+0x2ED8==2`; obtains player slot.
- Chooses two WORD classification values from tables `0x01569E18/24` and `0x01569E1C/28` based on `0x00AC11F0` and whether a related event object resolves to another player slot.
- Falls back to owner per-slot mapping `+0x498` (values >=100 encode related slot), rejects related slot when both slots share global group `+0x4C0`.
- Builds compact event record containing WORD classifications, source/target slot bytes and mode byte; dispatches through `0x004A0A20` when global `+0x574==+0x578`, otherwise `0x004A1BF0(...,0x0B,record,0)`.

## FUN_004117F0
- `thiscall`, one compact record pointer, `ret 0x04`.
- Positive first WORD plus valid source byte adds to mapped per-slot accumulator `+0x6B4`.
- If `0x00AC1210` passes, tracks per-slot aggregate flag/value `+0x6C8/+0x6CC`.
- Second WORD plus valid target byte adjusts mapped `+0x6B4`, clamped nonnegative.
- Record byte `+0x06` can trigger global/current-slot refreshes; when value 2 and source is current selected slot calls `0x0041B430/0x004E9870`.
- In `0x00AC11F0` mode, invokes `FUN_004111C0(mapped,0,1)` and, for record mode 2, `FUN_004110A0(5)`.
- Compact event application/accounting routine.

## FUN_00411990
- `thiscall`; state/resource transition conditioned on current-user ID range and global byte `+0x798`.
- Releases owner object `this+0x140` through global service `0x016E2044`, clears it after setting object flag `0x400`.
- Checks several input/UI/global gates (`0x00471DD0`, `0x004AA600`, `0x004ABCD0`, global byte `0x016E2F24+0x2030`, `0x016E15C8+0x21`, `0x0048A840`).
- On trigger sets `0x016E2F24+0x2030=1` and tail-jumps to `0x00471DE0`.

## FUN_00411A90
- `thiscall`; ensures owner byte state `+0x04 == 0x0F` using the standard zero-DWORD then byte-write pattern.

Next ordered aligned candidate `0x00411AB0`. No VERIFIED/MATCHED claim.