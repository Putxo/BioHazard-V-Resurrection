# Ordered decompilation evidence — 0x00412920 through 0x00412D10

## FUN_00412920
- `thiscall`; current-user/session transition routine.
- For IDs outside `[0x258,0x2BC)` and not `0x323`, disables global service when byte `+0x798` clear, calls player-manager reset `0x00523380(0)`, routes global bytes `+0x29/+0x2A` to sibling handlers `0x00409FE0/0x0040A080`, and invokes `FUN_0040DBE0`.
- When global `+0x798` set and current-user ID is `0x320..0x324`, dispatches event codes through `0x004B6EC0`: observed `0x15`, `0x14`, `0x18`, `0x3E` according to jump table.

## FUN_00412A50 (entry after embedded jump table)
- `thiscall`; synchronized flag reset/setup sequence.
- Clears root flag `0x800` and flag groups `0xFFE02000`; requests group `0x001F9000`.
- Clears bit 1 from four global registry DWORDs `0x016E1690+0x268/+0x288/+0x2A8/+0x2C8`.
- Sets root flag groups `0x00400000`, `0x01000000`, `0x00800000` under synchronization, then continues additional transition setup.

## FUN_00412BF0
- `thiscall`, bool return.
- Calls global vtable slot `+0x28(0,0x20)` every invocation.
- If owner byte `+0x07` nonzero, increments it; returns true once incremented value exceeds 8.
- On first invocation calls `FUN_00412A50`, invokes `0x00D175A0(global 0x016E1FD0)`, then sets counter to 1 and returns false.
- Eight-step/frame wait after setup.

## FUN_00412C40
- `thiscall`, three stack args, `ret 0x0C`.
- If root low flags `&3` clear and global mode not `1/2/5`, normally forwards args through `FUN_0040D050` then `0x004A03F0(global 0x016E03CC,...)`; special `(arg1=0x73,arg2=1)` condition with current-user value `0x497` calls `0x0041BB30(0x498,0)`.
- If root low flags are set, forces owner state byte `+0x04=9` (unless already 9/10) and calls `0x00495640` with selectors 6 and 10.

## FUN_00412D10
- `thiscall`; large substate machine keyed by byte `this+0x06` (0..0x15), using jump/byte tables at `0x004131F8/0x00413214`.
- Initial transition clears/request flags (`0x800`, `0x400000`, `0x01000000`, `0x00800000`), enables player-manager mode `0x00523380(1)`, resets service selector `0x00510B40(-1)`, optionally calls `0x0040AC10(float)`, then advances.
- Subsequent states coordinate global vtable readiness, `FUN_0040F020`, global service steps `0x004A1E60/0x00495640`, timer reset `0x0048C8A0`, and readiness predicate `FUN_0040E0B0`.
- Body continues to next boundary `0x00413280`.

No VERIFIED/MATCHED claim.