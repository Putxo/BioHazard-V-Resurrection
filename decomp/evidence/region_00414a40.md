# Ordered decompilation evidence — 0x00414A40 through 0x00414E60

## FUN_00414A40
- `thiscall`; two-phase owner state at `+0xF4` controlling object `+0x100`.
- When root flag `0x100` active and owner prerequisites pass, disables active players through `FUN_0040D730`, releases old object `+0x100`, creates replacement via `0x00857D40(0x016A104C)`, sets ownership flag `0x2000`, copies owner `+0xF8/+0xFC` to object `+0x40/+0x44`, registers type `0x1B`.
- Requests root flags `0x800/0x8000` when global byte `+0x56C` clear, then increments phase.
- Completion waits on object byte `+0x38`, releases object, clears root `0x100`, optionally clears `0x800/0x8000`, resets phase.

## FUN_00414BF0
- `thiscall`; four-state submachine on byte `+0x06`.
- Initial state requests flag groups `0x800`, `0xFFE02000`, `0x8000`, root flags `1` and `0x200`, resets timer, calls global service `0x004B6570`, initializes owner timer via `0x0040AC10`, advances.
- Next waits for global vtable readiness, calls `0x004E9F90`, resets owner byte `+0x07`, advances.
- Third waits for `FUN_00412BF0()` completion.
- Final configures service selector 4, compares owner `+0x44` against current-user ID, and performs follow-up transition logic.

## FUN_00414D60 (entry after embedded jump table)
- `thiscall`; three-state submachine.
- State 0 calls sibling `0x0040A0F0` twice, writes service field `0x0165C148+0x207D4=0x200`, calls `0x0050B430`, clears global byte `0x016E03CC+0x196C`, advances.
- State 1 waits for `0x005092D0` false, starts global transition selector `0x0C`, resets timer and calls global vtable slot `+0x34`, advances.
- State 2 waits for selector `0x0C` completion, calls `0x00495640`, zeros timer, global vtable slot `+0x28(0,0x20)`, invokes `FUN_0040DD60`, advances outer byte `+0x05` and resets substate.

## FUN_00414E60
- `thiscall`; four-state transition machine.
- Initial state resets global session helpers `0x00AC1150/0x00AC11A0`, global service `0x00473100`, enables `FUN_0040F020(true)`, resets UI vtable, replaces owned object `this+0x118` via factory `0x008304F0(0x016A0E24)`, sets ownership flag `0x2000` and configures fields from current-user ID/mode/root flags, registers type 1.
- Calls huge result/stat routine `FUN_0040FC80`, resets global services, then dispatches mode-specific event codes (observed `0x9A` for mode 0, `0xCE` for mode 3; additional paths continue).
- Body continues to boundary near `0x00415330`.

No VERIFIED/MATCHED claim.