# Ordered evidence: 0x00422260 - 0x00422C85

Decoded directly from the verified RE5DX9 1.1.0 Dev executable.

## FUN_00422260
- thiscall-like owner in ECX plus one stack `const char*`; exits with `ret 4`.
- Copies the input path to a 0x10C-byte local buffer, finds the final `\\`, and works on the basename.
- Requires basename length 7 for its special parse path.
- Builds/refcounts temporary strings, extracts character ranges `[1,3]` then `[5,6]` via `0x00B92640`, converts with `0x00B8C780`, compares the first parsed value to owner field `+0x2C`, and validates the other parsed value/range.
- Failure returns `-1`; success returns the parsed integer in EAX. Exact filename grammar remains unnamed until the referenced literals/callers are resolved.
- Ends at `0x0042242E`; INT3 padding to `0x00422440`.

## FUN_00422440
- Structurally parallel parser to FUN_00422260 beginning at `0x00422440`; same local path/basename handling and refcounted string machinery. Kept separate because its owner-field comparison/accepted grammar must be independently proven.

## FUN_00422620 / lookup region
- Subsequent owner methods traverse pointer collections rooted around owner fields `+0x59C/+0x5A8`, query runtime type chains through vtable slot `+0x10`, and compare type token/global `0x016927C4` before matching a derived name against a temporary string.
- `FUN_004226B0`-family lookup returns the matched object or null and uses `ret 4`.

## FUN_00422780
- Owner method; if `owner+0x594 == 0`, returns null with `ret 0x10`.
- Locks owner synchronization object at `owner+0x08` through imports `0x01371178/0x013710E8`.
- Iterates a pointer array owned by `owner+0x594`; compares four key fields at candidate offsets `+0x08` byte, `+0x09` byte, `+0x0A` word, `+0x0C` byte against four stack arguments.
- Returns matching candidate pointer or null. `ret 0x10` proves four stack arguments.

## 0x00422810-0x00422B26 wrappers
- Ordered short owner methods are synchronization-guarded wrappers around subsystem object `owner+0x594`.
- Several wrappers use three stack arguments and `ret 0x0C`, dispatching to distinct callees including `0x008287B0` and `0x00828880`; null subsystem returns false immediately.
- They are kept as separate candidates rather than merged because each dispatch target represents a different operation.

## FUN_00422B30
- Owner in ECX plus one structure pointer; `ret 4`.
- Copies float/scalar fields from source offsets `0x00,0x10,0x14,0x18,0x20,0x24,0x28,0x30,0x34,0x38,0x3C` into owner fields `+0x5B0..+0x5EC`.
- Clears owner byte `+0x5ED`.
- If owner field `+0x578` is non-null, invokes `0x00664890` with callback/token `0x0041F0C0` and stores result at owner `+0x5F0`.
- This is a configuration/state-copy method; semantic field names remain unresolved.

## FUN_00422BD0
- Owner method plus one stack pointer; `ret 4`.
- Null argument or zero count at argument `+0x64` exits.
- Iterates indices through `0x005497B0`, filters objects by runtime type token/global `0x016B51FC`, follows vtable-derived child/interface pointers, initializes/queries child state via `0x0041D2F0` and `0x00AD0600`, then inserts/binds the result into owner container at `owner+0x5F4` through `0x00425CD0`.
- Preserves ordered traversal and null/type guards. Gameplay-specific class names are unresolved.
