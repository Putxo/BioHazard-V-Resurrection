# Ordered decompilation evidence — 0x00421600 through 0x00421980

## FUN_00421600
- Iterates entries of caller collection (`+0x60` pointer array, count `+0x64`). For entries whose virtual name/type begins with four-byte literal `0x0137F270`, constructs temporary strings and searches marker `0x0137F268` through `0x00B8C8D0`.
- On marker match invokes command parser `FUN_0041E670(entry)`.
- When not matched, chooses alternate marker strings according to global mode 2/5, byte `+0x79A`, predicate `0x00AC11F0`, `0x00AC1210`, `0x00AC1230`, using literals `0x0137F25C`, `0x0137F24C`, `0x0137F23C`, `0x0137F230`, `0x0137F224`, `0x0137F218`; if found invokes same parser.
- Correctly releases temporary ref-counted strings after searches.
- Strong semantics: parse/dispatch command entries selected by current game mode.

## FUN_00421980
- Requires owner object `+0x288` and nonzero selected entry from owner table `+0x1C8[index]`.
- Locks owner, searches global registry linked list for descriptor type `0x016E33F0`, stores found object in owner `+0x28C` and clears its byte `+0x07`.
- Passes selected entry to object `+0x288` through `0x00D9B1A0`, calls `0x00D97B60(...,0)`, and continues object synchronization/update through next boundary `0x00421A50`.

No VERIFIED/MATCHED claim.