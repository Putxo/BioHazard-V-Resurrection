# FUN_00407250 evidence

## Target

- Exact local reference executable SHA-256: `323d1aabccc74505745588097e2b298b14e114393bfc24f6830e98b658e67815`; size 19,977,216 bytes; PE32 x86 image base `0x00400000`.
- Function VA `0x00407250`, RVA `0x00007250`.
- Native body: 207 bytes through plain `ret` at `0x0040731E`.
- Exact body SHA-256: `4244e88b9c4bc975a98d41f5d07e5a0c2ab4c117802ac30d9ccc97e0da1723b0`.
- `0x0040731F` is `INT3`; the next aligned root begins at `0x00407320`.
- No stack argument is read; the function preserves ESI/EDI and returns with plain `ret`, so the host reconstruction uses `cdecl void(void)`.

## Instruction/control-flow evidence

```text
00407250  push esi
00407251  push edi
00407252  xor esi,esi
00407254  push 07h
00407256  push 01567608h
0040725B  push 0Fh
0040725D  push 01567590h
00407262  push 0137B698h
00407267  push esi
00407268  call 004061F0h
0040726D  add esi,1
00407270  add esp,18h
00407273  cmp esi,0Bh
00407276  jl 00407254h
```

This first loop invokes `FUN_004061F0` eleven times with message indices 0 through 10, archive literal `Image\\Archive\\GameResource`, direct table `0x01567590` count 15, and resolved table `0x01567608` count 7.

```text
00407278  mov edi,01567D10h
0040727D  mov esi,01567C10h
00407282  mov eax,[edi]
...
0040728E  call 004061F0h
00407293  add esi,10h
00407299  add edi,4
0040729C  cmp esi,01567D10h
004072A2  jl 00407282h
```

The chapter-result loop performs 16 calls. ESI selects consecutive two-record direct tables at `0x01567C10 + i*0x10`; EDI selects the archive-name pointer array at `0x01567D10`. Each call uses message index -1, direct count 2, resolved table/count (0,0).

The 16 archive names are `ChapterEnd11`, `ChapterEnd12`, `ChapterEnd21`, `ChapterEnd22`, `ChapterEnd23`, `ChapterEnd31`, `ChapterEnd32`, `ChapterEnd33`, `ChapterEnd41`, `ChapterEnd42`, `ChapterEnd51`, `ChapterEnd52`, `ChapterEnd53`, `ChapterEnd61`, `ChapterEnd62`, and `ChapterEnd63`, all under `Image\\Archive\\`.

`0x004072A4..0x004072D0` repeats the same shape for 8 `VersusResult01..08` archives with tables `0x01567D50 + i*0x10` and archive pointer array `0x01567DD0`.

`0x004072D2..0x00407300` repeats the same shape for 8 `MerceResult01..08` archives with tables `0x01567DF0 + i*0x10` and archive pointer array `0x01567E70`.

The fixed final call is:

```text
00407302  push 0
00407304  push 0
00407306  push 2
00407308  push 01567E90h
0040730D  push 0137CC34h
00407312  push -1
00407314  call 004061F0h
00407319  add esp,18h
0040731C  pop edi
0040731D  pop esi
0040731E  ret
```

`0x0137CC34` is `Image\\Archive\\BenchResource`. The final direct table `0x01567E90` contains the two records `id\\jpn\\benchmark\\cap_end01 -> 0x016E2758` and `id\\jpn\\benchmark\\benchmark -> 0x01692E3C`.

## Representative table evidence

The first `GameResource` direct table `0x01567590` has 15 records and the resolved table `0x01567608` has 7 records. The direct records include `stage\\game -> 0x016E2758`, `sound\\bgm\\st_camera -> 0x016E2830`, menu/cockpit/night-scope/die resources, `id\\jpn\\cockpit\\XfSSKeyItem -> 0x016E289C`, and `sound\\se\\wp\\se_wp2b -> 0x016E2908`.

Every chapter/versus/merce direct table consists of exactly two records. Record 0 is always `id\\jpn\\chapter\\cap_end01 -> 0x016E2758`; record 1 is the result-specific path paired with `0x01692E3C`.

## Direct callers

A complete byte-level direct `CALL rel32` scan of the exact `.text` image finds five direct callers of `FUN_00407250`:

- `0x00407597`
- `0x00407773`
- `0x004078ED`
- `0x00407A40`
- `0x00412462`

The first three occur in adjacent resource-loading state paths after `0x004070C0`; the `0x00407A40` caller is the state-3 path of the nearby state handler; `0x00412462` is gated by a non-null result from `0x00489970`.

## Host-only guards and tests

The native 207-byte function contains no early return or conditional exit other than its bounded loop branches. The portable host model adds only a null-services/null-dispatch guard so tests can run without dereferencing engine globals. Tests cover both host-only guard cases and assert the exact ordered sequence of all 44 dispatches: 11 GameResource calls, 16 chapter-result calls, 8 versus-result calls, 8 mercenaries-result calls, and the final BenchResource call.

`FUN_00406CD0`, `FUN_00406DB0`, and `FUN_004070E0` are explicitly excluded parallel work and are not modified.
