# Ordered decompilation evidence — 0x004206D0 through 0x00420A00

- `0x004206D0`: iterates four current-user 0x50-byte records. For active matching records derives classification field `+0x0C` either from root per-slot value/table `0x01568E90` or by scanning three 0x84-byte groups for IDs `0x601/0x606` and mapping bitmask through `0x01568E8C`. Then for 19 service/item IDs, sets/clears bit `0x80` in matching 0x2C-byte entries according to root per-slot mask. Finally normalizes 12 entries, preserving selected state IDs and clearing others depending on global/current-user mode.
- `0x00420960`: simplified reset variant over four current-user records: assigns classification from `0x01568E90`, clears state field `+0x18` across 11 entries, installs final ID `0x137` with state 1.
- `0x00420A00`: large event/resource handler. Resolves caller pair through global collection `0x0165B9DC`, obtains named resource from record string via `0x01199D40`, registers with service `0x0050D610`, parses descriptor twice via `0x004097B0`, requires descriptor high byte class `0x03`, dispatches by 16-bit ID range using jump table `0x00420C00/0x00420C08`; one path sends service commands `0x1202/0x1203`, resolves typed object token `0x016B7978`, remaps WORD IDs for mode 3, and sends further service commands. Body continues to next boundary `0x00420F70`.

No VERIFIED/MATCHED claim.