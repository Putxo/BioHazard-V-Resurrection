# Ordered decompilation evidence — 0x0041E870 through 0x0041EB10

- `0x0041E870`, `0x0041E880`: exact no-op returns.
- `0x0041E890`: returns false.
- `0x0041E8A0`: iterates linked list at global `0x01657F20`; parses three decimal digits from each node string, counts values >=100, then second pass logs/dispatches each >=100 entry through `0x01198F40` and `0x00A60340` with ordinal/count context.
- `0x0041E980`: `(value,index)` setter for owner table `+0x2E8[index]`, only index<32.
- `0x0041E9A0`: `(index,arg)`; if index<32 and mapped handle != -1, invokes two global service operations `0x00E0BFE0` and `0x00E01540`; missing handle logs through `0x00A60700`.
- `0x0041EA00`: index predicate; for mapped handle calls `0x00E0C020(handle)` and returns true only result==1.
- `0x0041EA30`, `0x0041EA60`, entry `0x0041EA90`, `0x0041EAC0`: global wrappers that map player/index through global object `0x016E1FD0+0x3830+0x2E8[index]`, then tail-call service operations `0x00E01490`, `0x00E01410`, `0x00E200A0`, `0x00E0B460` when handle valid; otherwise return with corresponding stack cleanup.
- `0x0041EAF0`: returns whether mapped handle for index is not `-1`.
- `0x0041EB10`: initializes caller output vector to global default vector `0x017602C0`; when index/object chain valid, queries global handle service and attempts to replace output with object-derived vector. Continuation next.

No VERIFIED/MATCHED claim.