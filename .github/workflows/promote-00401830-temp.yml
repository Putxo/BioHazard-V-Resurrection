name: promote-00401830-temp
on:
  push:
    branches:
      - decomp/promote-00401830-ci
permissions:
  contents: write
jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: decomp/promote-00401830-ci
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Promote metadata and regenerate reports
        shell: bash
        run: |
          python - <<'PY'
          import csv
          from pathlib import Path
          fpath=Path('database/functions.csv')
          with fpath.open(newline='',encoding='utf-8') as f:
              rows=list(csv.DictReader(f)); fields=list(rows[0].keys())
          for r in rows:
              if r['stable_id']=='FUN_00401830':
                  r['fast_pass_validated']='true'
                  r['pr']='#80'
                  r['commit']='1b24887d112e06e4772a1e9498ced94a5fdb08f0'
                  r['notes']='three-argument byte-range equality helper; zero length true and first mismatch false; PR #80 CI 5/5 PASS'
                  break
          else:
              raise SystemExit('FUN_00401830 row not found')
          with fpath.open('w',newline='',encoding='utf-8') as f:
              w=csv.DictWriter(f,fieldnames=fields,lineterminator='\n'); w.writeheader(); w.writerows(rows)
          with Path('database/history.csv').open('a',newline='',encoding='utf-8') as f:
              csv.writer(f,lineterminator='\n').writerow(['FUN_00401830','2026-09-10','FAST_PASS','FAST_PASS_VALIDATED','1b24887d112e06e4772a1e9498ced94a5fdb08f0','coordinator','PR #80 completed 5/5 CI checks'])
          PY
          python tools/reports/report.py
          python tools/progress/treemap.py
          python tools/validate/validate.py
          git diff --check
          git rm .github/workflows/promote-00401830-temp.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add database/functions.csv database/history.csv reports/functions.json reports/progress.md reports/progress-treemap.svg
          git commit -m 'Promote FUN_00401830 after PR #80 CI'
          git push origin HEAD:decomp/promote-00401830-ci
