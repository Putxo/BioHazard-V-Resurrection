name: sync-8620-promotion-progress
on:
  push:
    branches:
      - promote/00408620-fast-pass-validated
permissions:
  contents: write
jobs:
  sync:
    if: ${{ !contains(github.event.head_commit.message, '[sync-progress]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: promote/00408620-fast-pass-validated
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python tools/reports/report.py
          python tools/progress/treemap.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reports/functions.json reports/progress-treemap.svg reports/progress.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Synchronize FUN_00408620 promotion reports [sync-progress]'
          git push origin HEAD:promote/00408620-fast-pass-validated
