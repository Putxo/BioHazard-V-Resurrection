# Branching

Base branches: `main`, `integration`. Work uses task branches such as `foundation/<task>`, `inventory/<task>`, `function/FUN_<VA>` or `batch/<range>`. PRs target `integration`. Permanent subsystem branches are not created before evidence and scale justify them.
