# Architecture

The project separates canonical metadata (`database/`), recovered source (`src/`, `include/`), reverse-engineering evidence (`decomp/`), validation (`tests/`, `tools/validate/`) and generated progress views (`reports/`). UNKNOWN is a valid classification until evidence supports refinement.
