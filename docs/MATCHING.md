# Matching

The exact MATCHED criterion is intentionally undefined until the original PC x86 toolchain and reproducibility constraints are investigated. MATCHED remains unclaimed until a reproducible criterion exists.
