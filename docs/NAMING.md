# Naming

Stable function IDs use `FUN_<VA>`. Globals use `GLOBAL_<VA>`, vtables `VTABLE_<VA>`, RTTI `RTTI_<VA>`. Human-readable names may change but stable IDs never lose the original address identity. Do not promote speculative names to confirmed names.
