# GATE 00 — Bootstrap

Phase: 0 — Bootstrap

## Requirements

- [x] Repository exists.
- [x] `main` exists.
- [x] `integration` exists.
- [x] Direct feature work is not performed on `main` or `integration` after bootstrap.
- [x] `.gitignore` blocks local proprietary inputs and common binary game material.
- [x] `docs/MASTER_PLAN.md` exists.
- [x] `local/` is reserved for ignored local-only inputs.
- [x] No decompilation progress is claimed.

## Validation

The repository was initialized from an empty state. The initial commit on `main` is the unavoidable repository bootstrap. `integration` and `foundation/bootstrap` were created from that commit before Foundation work began.

Result: PASS
